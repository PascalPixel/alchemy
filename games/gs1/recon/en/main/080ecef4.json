{
  "schema_version": 1,
  "namespace": "gs1-en-main",
  "address": "0x080ecef4",
  "status": "measured-draft",
  "owner_bytes": 528,
  "score": {
    "measured": "2026-08-27",
    "candidate_bytes": 456,
    "reference_bytes": 528,
    "differing_halfwords": 258,
    "wrong_instructions": 235,
    "class": "wrong",
    "note": "This owner is NOT a menu-poll loop like select_party_member.c; the family clustering here is a call-target/n-gram match, not a structural one. It is a straight-line setup routine (two pointer/void* arguments, a 3-way mode dispatch into Func_080e0524 with message ids 0x4f/0x50, 0x4b/0x4c, or 0x4d/0x4e, then a fixed 21-iteration frame counter loop drawing a decorative border via Func_080ed408/Func_080072f4/Func_080072f0 with row ranges 0-3/4-7/8-11/12-15/16-19 and single-shot triggers at i==18 and i==20) that reads and writes a large struct reached through the adjacent globals at ADDR_03001EEC and ADDR_03001EEC+4 (fields observed at +0x7784, +0x77a4, +0x77a8, +0x7824, +0x7828), matching the same struct apply_shake.c already reaches through ADDR_03001EEC at neighbouring offsets 0x77a0/0x77a4/0x77a8."
  },
  "rejected_experiments": [
    {
      "experiment": "two separate extern-global loads for the ADDR_03001EEC value and the word at ADDR_03001EEC+4",
      "result": "reference loads the base pointer once and does an auto-incrementing `ldmia r3!, {r7}` to fetch the first word, then a second `ldr [r3]` off the incremented pointer for the second word -- rewriting the two loads as `u32 *cell = (u32*)ADDR_03001EEC; base = *cell++; second = *cell;` was closer in shape (candidate dropped from 468 to 456 bytes, moving toward the reference's use of a shared pointer instead of two independent pool constants) but did not reach the reference's exact instruction selection"
    }
  ],
  "notes": [
    "Recovered only from the retained owner assembly, approved local ROM, and the approved compiler.",
    "This is a lower-confidence, single-pass structural draft, not an iteratively tuned one: the branch-by-mode dispatch, the loop bound (21 iterations) and its four row-range buckets, and the Scheduler_RemoveCallback(0x080cd261) tail call are read directly off the retained assembly and are solid; the exact struct-field semantics at offsets 0x7824/0x77a4/0x77a8/0x7784/0x7828, and specifically the guessed condition `M2C_FIELD(base,s32*,0x7824+4) == 1` gating the two Func_080ed408 border-tile calls, are inferred from instruction shape rather than confirmed against a known struct layout and are the weakest part of this draft.",
    "The call `Func_080072f4(base, counter, counter, 0, arg5)` passes its fifth argument through r4 immediately before the `bl`, which is not how this codebase's other recovered callers pass a 5th integer argument (stack, not r4); that calling-convention detail was not resolved here and is a likely source of some of the remaining mismatch, not just field-offset guessing.",
    "Given the scale of open questions, this looks like a source-shape gap worth a dedicated pass (working out the shared 'effects struct' layout from apply_shake.c and this owner's siblings together) rather than a compiler-internals residual reachable by respelling. This candidate claims zero DONE bytes."
  ]
}
