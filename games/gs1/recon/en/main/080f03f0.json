{
  "schema_version": 1,
  "namespace": "main",
  "address": "0x080f03f0",
  "status": "measured-draft",
  "owner_bytes": 328,
  "executable_owner_bytes": 328,
  "candidate_bytes": 348,
  "differing_halfwords": 142,
  "wrong_instructions": 119,
  "family": {
    "template_owner": "main:080c10e8",
    "template_source": "games/gs1/src/battle/presentation/set_actor_modes.c",
    "note": "The assigned set_actor_modes.c template does not match this owner's shape at all (no shared calls, no shared control flow); the real algorithm is a self-contained audio/screen setup + 33-pass fade sequence with its own hardware-register writes, recovered directly from the retained assembly and its own literal pool rather than from any family sibling."
  },
  "domain": "One-shot audio/PPU setup (clears four state flags, configures a channel via Func_080041d8, sets display control, resets two VRAM-ish banks via Func_080f037c, arms two Func_080f0254 phases, writes two sound-control registers, then runs a 33-pass outer loop that alternately calls Func_080f02b0 against a 33-entry table and drives a 17-step linear fade on register 0x04000050) before a final shutdown/cleanup and flag set.",
  "expressed": [
    "clears four byte flags at 0x03001d18/0x03001f58/0x03001ac4/0x03001d08, then calls Func_080040e8 and Func_080041d8(0x080f03c1, 1152)",
    "sets display control (0x04000000=64), resets two banks via Func_080f037c(0x06007800)/(0x0600f800), and arms Func_080f0254(0) then Func_080f0254(1)",
    "writes 0x1f8a to both halves of a register pair at 0x0400000c/0x0400000e, restores display control to 0x1c40, writes 0x0f83 to 0x00002844, calls Func_080f0678, and waits 300 frames",
    "for outer = 0..32 (33 passes): computes an alternating toggle from outer&1, calls Func_080f02b0 against a 33-entry u32 table at 0x080f0a5c indexed by outer with the opposite toggle, then runs a 17-step (inner = 0..16) fade on register 0x04000050 -- an ascending (inner<<8)|(16-inner) ramp on odd toggles, a descending base-256-per-step ramp on even toggles -- waiting 4 frames per step and 0x10b frames between outer passes",
    "after the 33 passes: clears 0x04000050, restores display control to 0x1040 (display-control-address - 0x50), calls Func_0800479c and Func_08004760, sets the first flag back to 1, and returns 0"
  ],
  "bounded_search": {
    "mode": "manual-respell",
    "attempted": 2,
    "exact": false
  },
  "bounded_checks": [
    "the candidate reproduces the reference's full 16-word literal-pool content and order (verified against the retained .s file's own trailing .4byte list) and the complete outer/inner loop bounds (33 x 17 passes) and toggle-driven ramp shape, but compiles 20 bytes larger (348 vs 328); the reference computes several constants (226<<5=0x1c40, 240<<4=0xf00, 130<<5=0x1040, 144<<3=1152, 150<<1=300) via a small-immediate `movs`+`lsls` pair instead of loading them from the pool, while the candidate's equivalent sites pull from an extra literal-pool load, and the resulting pool-size difference is large enough that gcc296 places a second, mid-function literal pool (an inserted branch-and-data block right after the Func_080f0678 call) that the reference's smaller, single-pool layout does not need",
    "re-spelling all five of those constants in the candidate as their explicit shift-of-small-immediate form (226<<5, 240<<4, 130<<5, 144<<3, 150<<1) produced byte-for-byte identical output to the flat-hex-literal version -- gcc296 constant-folds both spellings the same way before choosing pool-vs-immediate, so this is not a source-spelling difference; the compiler's choice of pool load over movs+lsls for these specific values must depend on something else (register pressure or a different value/constant living nearby) that a plain respell does not change"
  ],
  "notes": [
    "This owner shares no call targets or control-flow shape with the assigned set_actor_modes.c template; its actual shape (a fixed 33-entry table walk paired with a 17-step hardware fade ramp) was recovered directly from the retained assembly's own literal pool and loop bounds, cross-checked word-for-word against the ground-truth .s file's trailing constant list.",
    "The residual is concentrated and mechanical (a 20-byte pool-size overshoot triggering a spurious mid-function pool split) rather than a control-flow or algorithmic gap, but the specific source change needed to make gcc296 prefer movs+lsls over a pool load for those five constants was not found within this bounded attempt. This looks like it is still findable -- worth a follow-up that varies which computed values are cached in named locals/registers near those five writes, since the pool-vs-immediate choice is evidently driven by surrounding register/constant pressure rather than the constant's own spelling.",
    "Recovered only from retained owner assembly and the approved compiler. No GS2 evidence, external source, copied instruction bytes, fixed-register variables, inline assembly, or compiler modification was used."
  ]
}
