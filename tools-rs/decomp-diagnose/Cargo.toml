[workspace]

[package]
name = "decomp-diagnose"
version = "0.1.0"
edition = "2021"

[lib]
name = "decomp_diagnose"
path = "src/lib.rs"

[[bin]]
name = "decomp-diagnose"
path = "src/main.rs"

# Differential parity harness: runs the live TypeScript and this port over the
# real candidate corpus and asserts observation-for-observation agreement.
[[bin]]
name = "decomp-diagnose-parity"
path = "src/bin/parity.rs"

[dependencies]
