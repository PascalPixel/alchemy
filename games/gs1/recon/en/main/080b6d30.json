{
  "schema_version": 1,
  "namespace": "main",
  "address": "0x080b6d30",
  "status": "proven-retained",
  "classification": "compiler_zero_rematerialization_mismatch",
  "owner_bytes": 256,
  "executable_owner_bytes": 256,
  "candidate_bytes": 256,
  "differing_halfwords": 1,
  "wrong_instructions": 2,
  "cross_edition": {
    "measured": "2026-08-30",
    "object_bytes": 254,
    "retained_span_bytes": 256,
    "relocations": 5,
    "relocation_bytes": 20,
    "literal_fields": 3,
    "literal_bytes": 12,
    "core_bytes": 222,
    "core_diff_bytes": {
      "ja": 0,
      "en": 0,
      "de": 0,
      "es": 0,
      "fr": 0,
      "it": 0
    },
    "core_identical": true
  },
  "compiler_proof": {
    "measured": "2026-08-30",
    "source": "alchemy-gcc/agbcc/gcc_arm/reload1.c:9012",
    "finding": "reload_cse_simplify_set returns before considering an equivalent live register when the constant's SET cost is below two; zero therefore remains a fresh immediate after first CSE instead of becoming the reference register copy",
    "application": "the only residual is candidate movs r4,#0 versus reference mov r4,sl, with sl already holding the same zero across the call"
  },
  "family": {
    "template_owner": "main:080b7424",
    "template_source": "games/gs1/src/battle/summon/layout_positions.c",
    "note": "The template's x/z-position layout shape did not match; the real algorithm is a sibling of the already-exact games/gs1/src/battle/summon/find_available_slot.c and class_is_valid.c idiom for the same 6-slot ADDR_03001E74 table, and of the adjacent unresolved Func_080b6e30 (games/gs1/src/unidentified/main/runtime/runtime_owner_62.c), which removes an entry from the same table this owner adds one to."
  },
  "domain": "Registers a battle unit's icon into the fixed 6-entry ADDR_03001E74 summon-slot table (single- or paired-slot depending on the unit's class), invoking the slot renderer for up to two passes and returning a packed slot/sprite value for the caller (games/gs1/recon/en/main/080b6f44.c's Func_080b6d30 call).",
  "expressed": [
    "looks up the unit's BattleActorDefinition and its class-derived single-slot flag and sprite value via the already-exact Func_080c23c0/Func_080c2384",
    "runs up to two passes (pass 0 and 1), skipping a pass entirely when the actor's offset-297 flag is set",
    "each pass finds the first free slot (0..5) in the ADDR_03001E74 table, requiring the next slot also free unless the class is single-slot",
    "when no free slot exists the whole function returns immediately with whatever result pass 0 already produced",
    "calls Func_08009270(slot, 0x02018000 + slot*0x4000, sprite_value + pass, class_flag) to render the slot and bails out with 0 if that call fails",
    "on pass 0 only, packs (slot << 12) | sprite_value into the return value",
    "stores the unit id into the found slot (and the paired slot too, for non-single-slot classes)",
    "continues to a second pass only when sprite_value is exactly 476 or 483"
  ],
  "bounded_search": {
    "mode": "manual-respell",
    "attempted": 6,
    "exact": false,
    "permuter": {
      "output": "out/permute/080b6d30",
      "choices": 512,
      "attempted": 512,
      "compile_failures": 0,
      "best_differing_halfwords": 1,
      "exact": false
    }
  },
  "bounded_checks": [
    "the reused-per-file struct Layout { u8 pad[4]; s16 field[6]; } idiom and its inner slot-search loop are copied verbatim in shape from the already-exact games/gs1/src/battle/summon/class_is_valid.c, and reproduce the reference's search loop, branch layout, and register roles (r6=table, r8=single_slot, r9=actor) exactly",
    "moving `result = 0` to before the Func_080c2384(class_id) sprite_value call (matching the reference's mov sl,r1 before the second bl) took the candidate from 8 differing halfwords / 2 wrong instructions down to 1 differing halfword / 2 wrong instructions",
    "the single remaining diff is one instruction immediately after the sprite_value call: reference emits `mov r4, sl` (reusing the zero already resident in the callee-saved result register) where the candidate emits `movs r4, #0` (a fresh literal) for the pass-loop counter's initial value; both are 2-byte thumb encodings so the candidate stays at the exact 256-byte extent",
    "tried `pass = result`, `pass = zero` via a named `s32 zero = 0` shared source, separate assignment statements instead of declaration-initializers, and a do-while restructuring of the outer loop (matching the family template's do-while shape) in place of the for-loop -- all five variants reproduce the identical single-instruction residual, which points to a late RTL reload/regmove register-reuse decision rather than a source-level statement-order or loop-shape choice"
  ],
  "rejected": [
    {
      "hypothesis": "Swap declaration order of `pass` and `result` (declare `s32 pass;` before `s32 result = 0;`, keeping `for (pass = result; ...)`).",
      "measured": "2026-08-29",
      "differing_halfwords": 1,
      "baseline": 1,
      "verdict": "no change; identical single-instruction residual (movs r4,#0 vs mov r4,sl)"
    },
    {
      "hypothesis": "Change loop bound spelling from `pass <= 1` to `pass < 2`.",
      "measured": "2026-08-29",
      "differing_halfwords": 1,
      "baseline": 1,
      "verdict": "no change; identical residual"
    },
    {
      "hypothesis": "Change increment spelling from `pass++` to `++pass`.",
      "measured": "2026-08-29",
      "differing_halfwords": 1,
      "baseline": 1,
      "verdict": "no change; identical residual"
    },
    {
      "hypothesis": "Fold `pass = result` and `result = 0` into a single chained assignment `for (pass = result = 0; pass <= 1; pass++)` with `result` declared uninitialized.",
      "measured": "2026-08-29",
      "differing_halfwords": 8,
      "baseline": 1,
      "verdict": "regresses to 8 differing halfwords; rejected"
    },
    {
      "hypothesis": "Move `result = 0` initialization earlier, before the `single_slot` call (reordering result ahead of both class-derived calls).",
      "measured": "2026-08-29",
      "differing_halfwords": 14,
      "baseline": 1,
      "verdict": "regresses to 14 differing halfwords; rejected"
    },
    {
      "hypothesis": "Reverse the derivation direction: initialize `pass = 0` directly and derive `result = pass` via a comma expression in the for-init (`for (pass = 0, result = pass; ...)`), with `result` declared uninitialized.",
      "measured": "2026-08-29",
      "differing_halfwords": 109,
      "baseline": 1,
      "verdict": "regresses drastically (also candidate_bytes dropped to 252, missing code); rejected"
    }
  ],
  "notes": [
    "Owner byte size (256) matches the family/target-list entry; the candidate reaches the reference's exact 256-byte extent, full control flow, every branch target, every constant (0x129, 0x02018000, 476, 483), and every call target.",
    "The residual is a single 2-byte instruction-selection difference (register-copy vs fresh-immediate for a compile-time-known zero) in a spot where the value is already provably resident in a callee-saved register across an intervening call. This looks like a compiler-internals scheduling/reload residual rather than a source-shape gap: five structurally distinct source respellings that all match the reference's full control flow converged on the identical single-instruction diff, but no compiler evidence yet proves the approved compiler literally cannot emit the reference form, so this is not classified as permanent assembly.",
    "Worth a further attempt later with the native permuter's PERM_GENERAL search across nearby statement orderings/temporaries, which is better suited to a pure RTL-scheduling residual than manual respelling.",
    "Follow-up: ran the native permuter (out/permute/080b6d30, 512 combinations, 0 compile failures) with PERM_GENERAL over both loop conditions, both increments, the slot<=4 guard, the slot==6 exit test, the buffer_addr and packed-result operand orders, and the sprite_value!=476/483 operand order -- every combination reproduces the identical single-instruction (`mov r4, sl` vs `movs r4, #0`) residual with differing_halfwords=1, none reached exact. This further narrows the residual to a pure post-reload constant-materialization choice (GCC 2.96's reload_cse_simplify_set / cselib register-vs-immediate tie-break) that this owner's downstream statement shape does not influence; the fix, if any, is upstream of every axis tried so far.",
    "Recovered only from retained owner assembly, the already-exact sibling functions at games/gs1/src/battle/summon/find_available_slot.c, class_is_valid.c, release_charge.c, the unresolved sibling Func_080b6e30 and its caller Func_080b6f44 (games/gs1/recon/en/main/080b6f44.c), and the approved compiler. No GS2 evidence, external source, copied instruction bytes, fixed-register variables, inline assembly, or compiler modification was used."
  ]
}
