{
 "schema_version": 1,
 "namespace": "main",
 "address": "0x0808b674",
 "status": "compiler-unemittable",
 "owner_bytes": 324,
 "candidate_bytes": 36,
 "differing_halfwords": 162,
 "wrong_instructions": 151,
 "translation_unit": "unknown",
 "compiler_route": "canonical-gcc296",
 "algorithm": "Best-effort reading only (never adopted, see status): an object-table slot initializer. Reads the pointer stored at Data_03001ebc (a base pointer, per the sibling object/table/*.c family), takes state = base + 0x200 as an object slot, zero-fills state's leading 16 bytes (offsets 0/4/8/12, descending store loop matching the object/table/find_last_active_id.c convention of decrementing counters/pointers), copies two 24-byte template blocks from a literal-pool-addressed source into state+0x0 and state+0x218, calls ObjectTable_ClearBattleSlots (main:0808b9f8, already adopted as object/table/clear_battle_slots.c), stores two 16-bit fields into the new slot, looks up several values from an array at literal-pool address 0x02000240 indexed by small even offsets (476/484/488/492 => ids 238/242/244/246 doubled), stores them into the slot, calls sub_0808b3ec twice (once with the slot pointer, once with r10=arg0 and constant 8), reads a per-id table entry via the slot's field_10 to compute a byte flag, does a signed-shift coordinate computation (asrs #20 on two fields at slot+8/slot+16, matching a Q20 fixed-point convention seen elsewhere in this codebase) to index a 2D grid at a literal-pool base, gates on a byte-flag pair (== 253) and a global flag word before calling sub_080091a8 with a coordinate offset, updates slot fields 12/20 from its result, writes a byte constant into slot+85, calls sub_080091e0 and sub_08009080(slot, 12), then falls through into a tail branch `b sub_0808b7c4` into a *different* owner's already-emitted region.",
 "evidence": [
  "games/gs1/asm/0808b674.s carries the authored comment '複数領域に分割された呼出し可能関数の先頭部。後続するリテラルプールを同一ファイルに保持する。' (head portion of a callable function split across multiple regions; the following literal pool is kept in the same file) -- this is the project's own structural classification of the retained assembly, not an inference from this session.",
  "The retained assembly's prologue pushes {r5, r6, r7, lr} and shuffles r8/r10 into r6/r7 to push them too (push{r6,r7}), but the function has no matching pop anywhere in its 324 owned bytes: it falls through its body and its final instruction is a plain unconditional `b sub_0808b7c4` (main:0808b7c4, an already-registered separate owner, 'Fragment_0808b7b8's neighbour) rather than a `bl`/`bx lr`. This is the classic GCC-emitted 'function head' shape for a routine whose tail was placed in a later, separately-addressed region by the linker/overlay layout -- an artifact of how this ROM's build process split large functions across sections, not of any C control-flow construct this compiler route can produce from a single translation unit.",
  "candidate-show (out/cargo-target/release/compiler candidate-show games/gs1/recon/en/main/0808b674.c --owner 0808b674 --align --first) reports topology=uncovered:reference-multiple-owner-entries and class=compiler-unemittable for every body tried, with an identical differing_halfwords=162 against the 324-byte reference regardless of candidate content or size (2, 8, 36, and 76-byte candidates of varying shape and semantics all produced dh=162; only wrong_instructions moved slightly, 151/154/151/182, tracking incidental prologue-size differences in the small comparison window the tool is willing to score before giving up). This reproducibility across structurally unrelated bodies is the classification proof: the harness itself cannot align this owner's reference bytes to any single-function candidate because the true 324-byte reference span (0808b674 code + its retained literal pool at 0808b790, ending exactly at 0808b7b8) abuts a second, independently registered owner boundary (main:0808b7b8, 'Fragment_0808b7b8') that this owner's tail branch flows into -- a genuine multi-region function, which this project's per-owner C candidate model cannot represent.",
  "The tool's own next-step guidance on this run is explicit: 'next=record a classification proof; do not search spellings for a shape the compiler cannot emit' / 'triage=compiler-unemittable playbook=classification-proof' -- confirming this is the documented terminal state for this shape, not an intermediate score to keep optimizing.",
  "The neighboring object/table/*.c family (games/gs1/src/object/table/{get,get_slot_address,destroy_at_index,find_last_active_id,clear_battle_slots}.c) establishes 'extern u8 *Data_03001ebc' as the shared object-table base pointer and 'descending index/pointer store loop with >= sentinel' as this codebase's idiom for zero-fill loops, both used in the best-effort algorithm reading above; ObjectTable_ClearBattleSlots (main:0808b9f8) is confirmed as the callee at the `bl sub_0808b9f8` site by games/gs1/source-paths.json's existing 'object/table/clear_battle_slots.c' registration.",
  "bun tools/allocator-lens/lens.ts 0808b674 was run for allocation residual context: it reports only 9 pseudos and a small 42/36/40 global order for the stub-sized candidate actually compiled (the topology block prevents any larger candidate from being meaningfully scored against the true reference), confirming the remaining work here is topological/structural, not a register-allocation residual."
 ],
 "credible_shapes_tried": [
  "Empty stub `void Func_0808b674(u32 arg0) {}` (candidate=2 bytes): dh=162, wrong_instructions=151, topology=uncovered:reference-multiple-owner-entries. Baseline.",
  "Same stub plus a second empty `void Func_0808b7b8(void) {}` in the same file, hypothesizing the tool wants both halves of the multi-owner span declared together: candidate-show ignored the second function entirely (identical output to the single-stub baseline) -- refuted; the tool only ever scores the named --owner target function, so co-declaring the neighbor owner does not change the topology classification.",
  "`void Func_0808b674(void) {}` with no arguments (candidate=2 bytes): identical dh=162/wrong_instructions=151 to the u32-argument stub -- refuted the hypothesis that the arg0/r0-using prologue shape (mov r10,r0) was the source of the topology mismatch.",
  "`s32 Func_0808b674(u32 a, u32 b, u32 c, u32 d) { return a+b+c+d; }` (candidate=8 bytes, forces a 4-argument prologue): dh=162 (unchanged), wrong_instructions=154 -- refuted the hypothesis that matching argument count/calling convention shape would affect the topology block.",
  "`volatile` 7-local arithmetic filler forcing heavier register pressure (candidate=76 bytes, closest in size to plausible real bodies): dh=162 (unchanged), wrong_instructions=182 -- confirms wrong_instructions tracks incidental candidate/reference-window overlap size while dh stays pinned at 162 regardless, i.e. the comparison never actually reaches an aligned score for the real function body.",
  "Full best-effort semantic reconstruction (struct-based object-table slot zero-fill + ObjectTable_ClearBattleSlots call, the version now committed in this .c file, candidate=36 bytes): dh=162 (unchanged), wrong_instructions=151 -- confirms even a plausible, on-topic reconstruction of the real algorithm's opening lines cannot move past the topology gate, because the gate fires before content comparison is meaningful (the reference span crosses into a second owner's bytes that this file cannot also own)."
 ],
 "compiler_backend_evidence": "Not applicable at this stage: the run never reaches instruction-selection/allocation comparison for the true reference window. topology=uncovered:reference-multiple-owner-entries fires first and is stable across every distinct candidate shape and size tried (2/8/36/76 candidate bytes, four unrelated bodies), which is the tool's mechanism for reporting that a --owner target's registered 324-byte reference extent cannot be matched one-to-one against a single compiled C function because it structurally overlaps a second, independently addressed owner boundary (main:0808b7b8) that this function's un-popped prologue and terminal `b sub_0808b7c4` tail branch flow into. This is consistent with the .s file's own authored comment identifying the owner as 'the head portion of a callable function split across multiple regions.'",
 "remaining": "This owner is classified compiler-unemittable, not a stalled last-mile allocator/scheduling residual: no C source shape reconstructable by this session moved differing_halfwords off 162 or changed the topology=uncovered:reference-multiple-owner-entries / class=compiler-unemittable classification, because the defect is structural (a genuinely multi-region function whose registered reference bytes span two separately addressed owners) rather than an algorithm, type, or spelling defect. games/gs1/asm/0808b674.s remains the retained ground truth and should stay retained assembly; per CONTRIBUTING.md's 'genuinely structural assembly such as a veneer, alignment, or runtime primitive' carve-out, a split-region function head with no intra-owner pop is exactly this kind of case. No further spelling search is expected to change this outcome per the tool's own playbook guidance; future work on this owner should instead investigate whether the reconstruction harness can register a paired multi-region owner (linking main:0808b674 through main:0808b7b8 as one compiled unit) rather than continuing to vary candidate C source.",
 "notes": [
  "make verify was not run to completion/adoption: this owner is not DONE and dh != 0. This file and its .json record a measured, reproducible compiler-unemittable classification, not a finished reconstruction.",
  "No inline assembly, fixed-register variable, compiler barrier, forced instruction scheduling, copied instruction bytes, or compiler modification was used anywhere in this session. games/gs1/asm/0808b674.s remains the retained ground truth and was not edited.",
  "games/gs1/recon/translation-units.json and games/gs1/source-paths.json were read only, per task instructions; neither was edited.",
  "Six materially distinct scored candidates were run through candidate-show (see credible_shapes_tried), each confirming the identical topology-level block; per the tool's own explicit guidance ('next=record a classification proof; do not search spellings for a shape the compiler cannot emit', 'triage=compiler-unemittable playbook=classification-proof') and per reconstruction-method.md's inversion guidance ('do not spend an open-ended search on a tiny residual' / large, structural mismatches 'challenge the overall source shape' rather than invite more spelling permutations), further mechanical spelling attempts were not pursued past this point -- the blocking condition is a fixed data-topology fact (a reference span crossing two registered owner boundaries) that no candidate C body can change.",
  "measured=2026-08-29"
 ]
}
