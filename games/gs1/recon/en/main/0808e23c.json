{
 "schema_version": 1,
 "namespace": "main",
 "address": "0x0808e23c",
 "status": "measured-draft",
 "owner_bytes": 632,
 "executable_owner_bytes": 632,
 "candidate_bytes": 644,
 "differing_halfwords": 222,
 "wrong_instructions": 172,
 "domain": "Battle-command item dispatcher: decodes a packed (item_id | actor<<10) argument, resolves which party member actually uses the item (actor==15 means auto-pick the member whose 15-slot ability list best matches the item), looks up a shared kind==4 battle-effect event for the item, and either runs that event's effect (inline or via a callback) or falls back to the ordinary use-item animation/consumption path shared with the actionId==0x95 special-confirm block in the exact-adjacent BattleCommand_ExecuteSelectedAction.",
 "expressed": [
  "decodes item_id = arg & 0x3ff, actor = (arg>>10) & 0xf from the packed argument, matching main:0808e680's identical decode for an ability id",
  "unconditionally primes Func_08077148(actor) before branching on actor==15, discarding its count result on the non-auto-select path -- matches the same priming idiom in games/gs1/src/battle/party/*.c",
  "actor==15: scans Data_02000240's target-id table (BattleWork, battle_effect_runtime.h) up to Func_08077148's count, and for each candidate counts how many of its 15 object+0xd8 ability slots (masked 0x1ff, same mask as shop/select_use_item.c's Ability_GetAvailability scan) equal item_id, keeping the best-matching actor",
  "actor!=15: counts matches for that one actor directly into the same accumulator",
  "no match anywhere -> UiText_DrawMessage(0x927,1); return -1",
  "Func_0808e14c(item_id) walks the same runtime->events[] table as BattleEffect_FindMatchingEvent (main:0808e4b4, kind==5/ability), but filters kind==4 (item); a hit whose 12-byte record's trailing effect word is nonzero either runs it as a small effect id (Func_0808ddec/BattleRuntime_Reset/Func_08092b94/Func_08092f84/Func_08091750) when < 0x10000, or calls it as a function pointer via the __call_via_r3 veneer with (item_id, actor, slot) otherwise",
  "no event hit: reproduces main:0808e680's actionId==0x95(=149) confirm-prompt block instruction-for-instruction (same Data_02000240+0x400/0x402 <- +0x480/0x482 u16 copy, same Data_03001ebc+0x170=999 sentinel, same Func_08091d84(1) prompt), then always plays the use-item message/animation (Func_08096fb0, a Data_03001ebc+0xcc6 busy-flag toggle around Func_08096810 matching BattleCommand_ExecuteSelectedAction's identical resolving_action toggle around the same RunBattleEffect call), and sets a consumed-item flag when the item's use_type bit 0 is set",
  "tail: conditionally shows message 0x927 again and, when a separate flag is set, calls Func_08077058(actor, slot) -- the same (owner_id, slot) item-removal call item/use.c makes when item->kind==1"
 ],
 "bounded_search": {
  "mode": "source-shape",
  "attempted": 6,
  "exact": false,
  "reason": "Iteratively fixed from an initial hand draft: (1) reordered the two 15-slot ability scans to increment the walking u16 pointer before the match check; (2) moved matches=0 after Runtime_GetObject, restoring the eight-byte frame; (3) made the record's effect signed so the threshold uses bge; (4) cached Data_03001ebc's dereferenced pointer across the action block; and (5) restored the auto-selection entry as `actor = 0; i = 0; if (actor < count)` rather than the superficially equivalent `if (count > 0)`. The decoder's mirrored-guard finding is now closed: candidate and reference have equal conditional-branch topology, and wrong instructions fall from 174 to 172.",
  "not_attempted": "Full byte-exactness was not reached and further micro-ordering of the initial packed-argument decode (item_id/actor) was not chased past the point of diminishing returns for this session; see remaining."
 },
 "remaining": "222 differing halfwords / 172 wrong instructions, candidate 644 bytes vs reference 632, with equal conditional-branch topology. GCC currently rotates the scan carriers as best=r6, count=r7, index=r8 where the reference uses best=r7, count=r8, index=r6; this changes the packed-argument and both 15-slot scan register streams without changing their values. The candidate also emits three literal pools while the reference keeps the same constant set in one terminal pool, accounting for the larger candidate despite six fewer executable instructions. Function-scope versus narrow scan scope, declaration order, initializer versus assignment, split actor masking, actor/index zeroing order, routed/gcc296/old-agbcc families, and the approved scheduling/CSE/regmove axes preserve this cycle or regress. No fixed-register variable, artificial padding, barrier, inline assembly, or owner-only compiler route was introduced.",
 "notes": [
  "The family-matcher's best guess for this owner, games/gs1/src/shop/select_use_item.c (score 7481/10000, below the 7500 transplant threshold), is not this owner's kin: it is a different menu-loop function (item-selection UI) that shares only the object+216 15-slot/0x1ff-mask ability-availability idiom and the object+216 selector*2 addressing pattern with this owner's ability scan. This owner's actual family is the exact-adjacent battle-command cluster at main:0808e4b4/main:0808e680, confirmed by an identical packed-argument decode, an identical actionId==0x95/action_id==149 confirm-prompt sequence (same globals, same sentinel 999, same Func_08091d84(1) call), an identical Data_03001ebc+resolving-flag toggle around RunBattleEffect (Func_08096810, battle/effects/run_effect.c), and an identical Func_08096fb0(action_id,0)/Func_08077058(actor,slot) call shape shared with item/use.c's Item_Use.",
  "Runtime_GetObject (Func_08077008), Item_GetData/Item_Get (Func_08077018), GameFlag_Clear/Set/IsSet (Func_080770d0/c8/c0), UiText_DrawQuantity/DrawMessage (Func_08015120/08015040), UiWork_FinalizePending (Func_08015140), BattleRuntime_Reset (Func_080916b0), and BattleEffect_CleanupSceneObjects (Func_08097194) are already-named symbols reused as-is (types.h / source-paths.json); Func_08092b94, Func_08092f84, and Func_08091750's single-argument (or no-argument) signatures were taken directly from their already-adopted sources (script_owner_9.c, run_action_and_wait.c, battle_owner_53.c), not guessed.",
  "Func_0808ddec, Func_08077148, Func_0808e14c, and the __call_via_r3 target held in event->effect have no evidence-backed names yet and are kept as Func_ADDR.",
  "Recovered only from retained owner assembly (games/gs1/asm/0808e23c.s and 0808e14c.s), the approved local GS1 ROM, already-adopted exact sources (games/gs1/src/battle/party/*.c, games/gs1/src/battle/presentation/run_action_and_wait.c, games/gs1/src/battle/effects/run_effect.c, games/gs1/src/item/use.c), the games/gs1/include shared headers, the two exact-adjacent measured-draft siblings' evidence trail (main:0808e4b4, main:0808e680), and the approved compiler. No GS2 evidence, external source, copied instruction bytes, fixed-register variable, inline assembly, compiler barrier, or compiler modification was used. Zero DONE bytes are claimed."
 ]
}
