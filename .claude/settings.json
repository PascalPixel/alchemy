{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r \".tool_input.command // \\\"\\\"\" | grep -Eq \"decomp_diagnose[^|]*\\|[[:space:]]*(grep|rg|head|tail|awk|sed|cut|wc)\" && printf \"%s\" \"{\\\"hookSpecificOutput\\\":{\\\"hookEventName\\\":\\\"PreToolUse\\\",\\\"permissionDecision\\\":\\\"deny\\\",\\\"permissionDecisionReason\\\":\\\"decomp_diagnose emits the agent-brief contract: budget, assignment, and the stop rule (one structural hypothesis, 30 minutes, stop after three non-improving scores, no permutation or compiler-mode search). Piping it into a filter discards that contract. Run it unpiped and read the whole brief.\\\"}}\" || true",
            "timeout": 10,
            "statusMessage": "Checking decomp_diagnose brief is not filtered"
          }
        ]
      }
    ]
  }
}
