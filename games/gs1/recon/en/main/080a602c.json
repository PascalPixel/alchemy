{
  "schema_version": 1,
  "namespace": "main",
  "address": "0x080a602c",
  "status": "measured-draft",
  "owner_bytes": 168,
  "candidate_bytes": 168,
  "differing_halfwords": 46,
  "wrong_instructions": 60,
  "classification": "inventory-owner-slot-refresh",
  "family": {
    "id": "template-main-080a355c",
    "template_owner": "main:080a355c",
    "template_source": "games/gs1/src/inventory_menu/prepare_owner_items.c",
    "note": "Genuinely related in domain (same InventoryMenu state block, same 'owner slot at menu+2+offset, owner id table at menu+0x208, item collection at menu+0x1c8' shape) but this owner does different, additional direct field writes (icon->[5]=1, icon->[12]=0 (u16), a second object's [5]=13) before the owner-index branch, and ends by re-reading the cursor pointer and calling Func_080a17c4/WaitFrames(1) directly rather than through the template's exact call sequence. Reused only the general offset/branch shape, not the template body verbatim."
  },
  "notes": [
    "Read the raw assembly directly (not adapted line-for-line from the template): computed cursor_offset=party_slot*4+20, dereferenced Data_03001f2c into 'menu', wrote icon[5]=1 and icon[12](u16)=0, wrote (*(menu+540))[5]=13, read byte_val=*(menu+537), computed offset=party_slot+28, read owner_index=*(s8*)(menu+offset), wrote (menu+2)[offset]=byte_val, branched on owner_index==-1 (zero it and combined_offset=0, else combined_offset=owner_index*2 and call Func_080a1ac0(owner_index*24-10,16)), read obj_id at menu+combined_offset+520, called Func_08077008, Func_080a68ec(obj,menu+456,2), wrote menu[536]=badge, called Func_080a60d4(menu+520,menu+456), re-read the cursor pointer, called Func_080a17c4, WaitFrames(1), returned Func_080a60d4's result.",
    "First candidate (menu dereferenced before computing cursor_offset): candidate=168 bytes, differing_halfwords=48, wrong_instructions=62. Swapping the two initial statements so cursor_offset=party_slot*4+20 is computed before `menu = Data_03001f2c` moved the front of the function closer to the reference's register schedule: differing_halfwords=46, wrong_instructions=60 (kept as the draft).",
    "Remaining divergence is concentrated in the first third of the function (prologue through the owner_index branch): the reference preserves party_slot in r4 immediately after loading the Data_03001f2c pool address and before shifting it, and materializes several small literals (notably 0, used three times: icon[12], the owner_index==-1 branch's *(menu+offset)=0, and combined_offset=0) differently than my candidate's register/pool allocation. The tail of the function (from the Func_080a1ac0 call through the WaitFrames(1)/return) already matches byte-for-byte, so the remaining gap is a register-pressure/scheduling difference in the front half rather than a control-flow or field-offset error.",
    "This reads as a source-shape gap (the field writes and branch structure are already correct) rather than a hard compiler-internals wall -- worth another pass that tries splitting the many locals into fewer live ranges (e.g. reusing one scratch pointer variable for the repeated 'menu + N' pattern instead of separate named locals) to better match the reference's tight register reuse.",
    "Adoption requires 0 differing_halfwords."
  ]
}
