{
  "span_bytes": 76,
  "score": {
    "measured": "2026-08-24",
    "candidate_bytes": 76,
    "reference_bytes": 76,
    "wrong_instructions": 0,
    "differing_halfwords": 5,
    "class": "compiler-scheduling-retention"
  },
  "domain": "Enables unmasked scheduler callbacks whose address lies in the 0x02 overlay range, while preserving interrupt-master state.",
  "expressed": ["complete owner extent and literal pool", "20-entry overlay-address scan", "mask-bit test and state-high enable bit", "interrupt-master save, disable, and restore"],
  "unexpressed": [],
  "verified_against_reference": ["byte count 76 = 76", "normalized instruction multiset, control flow, memory operations, constants, and pools agree", "the 68-byte relocation-independent core is identical across all six GS1 editions"],
  "blocked_by": "two independent operations are scheduled differently: the -1 negation crosses the scheduler-table literal load, and the constant-one move crosses the mask test; all rows resynchronize immediately",
  "rejected": [
    "10,000-candidate classic heat-guided ordinary-C walk: 9,756 compiled, exact 0, no candidate improved the five-halfword baseline",
    "scheduler-on GCC 2.96 and old-agbcc change broader instruction shape or extent"
  ],
  "notes": ["The byte at callback+3 equals 2 exactly when the stored callback address is in the 0x02 overlay range; no stronger callback-kind claim is made.", "No external source, copied instruction bytes, inline assembly, fixed-register variable, compiler barrier, or compiler modification was used."],
  "retention": "keep_structured_asm"
}
