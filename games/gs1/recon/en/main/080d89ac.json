{
  "schema_version": 1,
  "namespace": "main",
  "address": "0x080d89ac",
  "status": "measured-draft",
  "owner_bytes": 2024,
  "candidate_bytes": 2016,
  "differing_halfwords": 961,
  "wrong_instructions": 904,
  "classification": "structural-reconstruction",
  "resolved_asm_classification": {
    "kind": "compiler_output",
    "retention": "c_candidate",
    "confidence": "likely"
  },
  "notes": [
    "2026-08-30: replaced two major m2c artifacts with ordinary source constructs established by exact siblings: the 0x03001eec cursor now emits the reference ldmia load, Func_080de2f8 receives its two output pointers as real fifth and sixth arguments, and the local two-entry rectangle callback array is invoked through typed six-argument calls. The candidate grew from 1960 to 2016 of 2024 bytes and the differing-halfword count fell from 996 to 961. The higher aligned wrong-instruction count reflects the now-present call arguments shifting still-unrecovered later blocks, not removed semantics.",
    "2026-08-30: promoted the named BattleEffectA owner from the prepared aggregate-aware drafting wave. After repairing its concrete C type failures, the canonical compiler produces 1960 bytes for the 2024-byte reference; topology differs and 996 halfwords differ, so this remains a measured draft and claims zero DONE bytes.",
    "The repairs are mechanical recovery facts: a halfword display-register store, signed halfword actor-id reads, the pointer returned by Func_080b5098, and typed u8/u16 presentation-table reads. No instruction bytes, inline assembly, fixed-register variables, compiler barriers, or external decompilation sources were introduced.",
    "The function is a large six-mode battle presentation routine over the 0x03001eec work block. It initializes display and palette state, advances a long phase loop, updates party-linked battle objects, and draws effect resources through call-via trampolines. The existing BattleEffectA name is retained until its dispatch role has stronger semantic evidence.",
    "The next reconstruction pass must model the r3, r4, r5, and r7 call-via sites as typed indirect calls, then recover the 128-byte reference frame layout and shared battle-work aggregates before allocator work."
  ]
}
