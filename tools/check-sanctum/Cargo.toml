[workspace]

[package]
name = "check-sanctum"
version = "0.1.0"
edition = "2021"

[lib]
name = "check_sanctum"
path = "src/lib.rs"

[dependencies]
serde_json = { version = "1", features = ["preserve_order"] }
