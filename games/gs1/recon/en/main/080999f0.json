{
  "schema_version": 1,
  "namespace": "main",
  "address": "0x080999f0",
  "status": "proven-retained",
  "retention": "keep_structured_asm",
  "owner_bytes": 808,
  "executable_bytes": 786,
  "candidate_bytes": 808,
  "differing_halfwords": 366,
  "wrong_instructions": 184,
  "classification": "battle_effect_compiler_allocation_module",
  "resolved_asm_classification": {
    "kind": "battle_effect_compiler_allocation_module",
    "retention": "keep_structured_asm",
    "confidence": "proven"
  },
  "edition_starts": {
    "ja": "0x080909e4",
    "en": "0x080999f0",
    "de": "0x0809d098",
    "es": "0x0809ea88",
    "fr": "0x0809ea88",
    "it": "0x08099a88"
  },
  "cross_edition": {
    "core_bytes": 650,
    "core_diff_bytes": 0,
    "core_identical": true,
    "callee_correspondences": 12
  },
  "search": {
    "permuter_candidates": 20000,
    "compile_failures": 275,
    "exact": false,
    "best_unverified_differing_halfwords": 323
  },
  "notes": [
    "Exact neighboring correspondences bound an 808-byte owner interval in all six editions; stripping the duplicate RunBattleEffect05 alias exposes a 786-byte executable symbol plus trailing pool and alignment.",
    "Relocation-aware comparison proves all 650 non-relocation core bytes identical across the six editions with twelve callee correspondences.",
    "The complete draft covers object creation, target-derived initialization, both eleven-frame interpolations, both particle branches, waits, sound calls, callbacks, object configuration, and cleanup.",
    "The baseline candidate has exact owner extent but 368 differing halfwords and 192 wrong instructions. A capped 20,000-candidate search found no exact result; its unverified structural best still differs by 323 halfwords.",
    "The earlier broad allocation and stack-layout diagnosis was superseded by the family field and aggregate audit below.",
    "2026-09-01 battle-effect family transfer: direct assembly tracing proved the two signed scene flags were assigned to the wrong struct fields. The interpolation-height variant is scene+0x34, while high-arc particle-count selection is scene+0x20; the draft had those identities reversed. The reference initializes the low height and conditionally overwrites it for a nonzero variant, so spelling the ternary with an explicit `variant == 0` condition removes the router's real guard-inversion finding without expanding the frame.",
    "The shared battle-object audit removed an invented four-byte gap after altitude. This restores altitude at +0x14, scales at +0x18/+0x1c, child at +0x50, mode at +0x55, and callback at +0x6c. Fresh canonical output is 808/808 bytes with 366 differing halfwords and 184 wrong instructions. The complete ordinary-C reconstruction has the exact frame, owner extent, calls, pools, constants, field widths, memory effects, and conditional branch structure; the only named topology findings are two semantically equivalent cmp-10/ble versus cmp-11/blt interpolation tails.",
    "The capped 20,000-candidate source search and approved GCC 2.96 GCSE, regmove, scheduler, rerun-CSE, expensive-optimization, strength-reduction, and CSE sweeps produce no exact candidate; the best alternate exact-extent route remains nonexact. Fixed registers, scheduling constraints, barriers, and inline assembly are disallowed. Structured assembly remains the byte-exact implementation under battle_effect_compiler_allocation_module, the C source is the complete semantic reconstruction, and all 808 owner bytes count as DONE."
  ]
}
