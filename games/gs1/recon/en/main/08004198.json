{
  "span_bytes": 64,
  "score": {
    "measured": "2026-08-24",
    "candidate_bytes": 64,
    "reference_bytes": 64,
    "wrong_instructions": 0,
    "differing_halfwords": 2,
    "class": "compiler-scheduling-retention"
  },
  "domain": "Finds a callback in the 20-entry scheduler table while preserving interrupt-master state.",
  "expressed": ["complete owner extent and literal pool", "20-entry callback scan", "not-found result -1", "interrupt-master save, disable, and restore"],
  "unexpressed": [],
  "verified_against_reference": ["byte count 64 = 64", "normalized instruction multiset, control flow, memory operations, constants, and pools agree", "the 56-byte relocation-independent core is identical across all six GS1 editions"],
  "blocked_by": "the approved compiler schedules the negation of the -1 result before the scheduler-table literal load; the reference schedules that independent load first",
  "rejected": [
    "10,000-candidate classic heat-guided ordinary-C walk: 9,772 compiled, exact 0, no candidate improved the two-halfword baseline",
    "routed GCC 2.96 at -O1/-O2/-O3/-Os preserves the same residual; scheduler-on GCC 2.96 and old-agbcc change broader instruction shape or extent"
  ],
  "notes": ["Recovered only from retained owner assembly, approved ROM, in-repository scheduler callers, and the approved compiler.", "No external source, copied instruction bytes, inline assembly, fixed-register variable, compiler barrier, or compiler modification was used."],
  "retention": "keep_structured_asm"
}
