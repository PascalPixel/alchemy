{
  "schema_version": 1,
  "namespace": "main",
  "address": "0x080a7f44",
  "status": "exact",
  "owner_bytes": 240,
  "executable_owner_bytes": 238,
  "candidate_bytes": 240,
  "differing_halfwords": 0,
  "wrong_instructions": 0,
  "classification": "exact-c",
  "resolved_asm_classification": {
    "kind": "compiler_output",
    "retention": "c_candidate",
    "confidence": "likely"
  },
  "cross_edition": {
    "editions": ["ja", "en", "de", "es", "fr", "it"],
    "relocation_normalized_core_bytes": 222,
    "core_diff_bytes": 0,
    "core_identical": true
  },
  "notes": [
    "Moves a character one position in the selector order, rebuilds the runtime order through the remove/add callbacks, and refreshes the stored character count.",
    "2026-08-27: bounded search dropped differing_halfwords from 114 to 28 (candidate_bytes now matches owner_bytes exactly, 236->240). Two structural fixes drove the gain: (1) the early direction check was over-eager -- reference recomputes 'adjacent_index' a second time, right at the swap, by re-testing 'direction == 1' rather than carrying one value computed at the top; rewriting the swap as two fully duplicated branches (no shared adjacent_index variable) made the swap, both loops calling Func_08077168/Func_08077150, and the final Func_08077158 call plus epilogue match the reference byte-for-byte (state pointer now lands in r8 exactly as in the reference, matching the caching idiom described for other owners in this corpus). (2) Tried caching &state->character_count and hoisting the clear-loop into a pointer form (u32 *p = &reordered[13]; p >= reordered; p--) -- both looked locally better but cost more overall: the pointer clear-loop makes gcc emit an extra pre-loop guard test (for-loop rotation duplicates the comparison when the compiler can't prove the pointer range non-empty) that a plain 'for (index = 13; index >= 0; index--)' avoids outright, so the plain index form scores better in aggregate despite keeping a redundant r6 counter alongside the address-decrementing store pointer that the reference elides via a cached 'ip' bound.",
    "2026-08-27: remaining 28-halfword residual is concentrated in two spots verified by an extensive bounded search (~20 independent source rewrites, all re-scored): (a) the array-clear loop keeps a redundant down-counter (r6) instead of reference's single pointer-vs-cached-ip test; every pointer-only rewrite tried (for, do-while, hybrid index+pointer, signed casts) either reproduced this same counter or introduced a worse pre-loop guard elsewhere. (b) the character_ids-copy loop (reordered[index] = state->character_ids[index]) compiles to a value-cached descending loop (character_count loaded once, counted down) instead of the reference's address-cached ascending loop (character_count re-read through a hoisted pointer each iteration) -- the exact ascending/address-cached shape *does* appear for the two structurally identical count-up loops that call Func_08077168/Func_08077150, so the difference is not the loop-control idiom itself; it reproduced identically across for/while/do-while, index-only/pointer-only/dual-pointer, shared/private loop variables, count>0-guard-then-loop, and explicit u32 casts, suggesting it tracks something about the callee-free store-only loop body rather than any tried source phrasing. Did not meet the permanent-residual gate (no compiler-pass evidence collected, only repeated candidate-show outcomes), so this stays a c_candidate pending either a source insight not yet tried or a recorded compiler-evidence classification.",
    "2026-08-29: repair pass targeting the same 28-halfword residual, focused on the two mechanisms that helped elsewhere in this corpus (constant-hoist placement, statement-position-across-calls) plus the swap-branch register mismatch (else-branch temporary loads via r0 in the candidate vs r6 in the reference). None improved on baseline dh=28; all rejected: (1) else-branch swap using a separate 'previous_temporary' local instead of reusing 'temporary' -- no change (dh=28). (2) else-branch swap reordered to write reordered[selected_index-1] first then reordered[selected_index] -- much worse (candidate grew to 248 bytes, dh=100). (3) clear-loop rewritten as 'u32 *clear_ptr = reordered+13; do {*clear_ptr=0;} while(--clear_ptr>=reordered);' (do-while pointer form, no separate counter) -- worse (dh=82); reconfirms prior pointer-loop rejections under a new do-while phrasing. (4) hoisting 's32 character_count = state->character_count;' at the top of the shared block wrapping both the clear loop and the copy loop -- worse (candidate shrank to 236 bytes but dh=100; loop shapes elsewhere destabilized). (5) same character_count hoist but scoped only to the copy loop's bound via a '(s32)state->character_count' cast in place -- no change (dh=28). (6) copy-loop condition rewritten as 'state->character_count > index' (operands swapped) -- no change (dh=28). (7) copy-loop body routed through an explicit 'u16 character_id' temporary before the u32 store -- worse (dh=29, wrong_instructions jumped to 40). (8) local variable declaration order swapped ('temporary' declared before 'index') -- no change (dh=28). (9) copy-loop RHS wrapped in an explicit '(u32)' cast -- no change (dh=28). Confirms the residual is a stable local optimum for source-level rephrasing; leaving baseline in place unchanged. 8 additional source variants tried this pass (bringing the corpus total to ~28), none surfaced a new lead.",
    "2026-08-29 (follow-up, coordinator-directed up-counting/address-cached retry): tried the specific up-counting pointer-vs-cached-end idiom for the clear loop and an address-cached-base idiom for the copy loop, individually and jointly, since allocation is whole-function. Clear loop, up-counting forms (all standalone, baseline copy loop kept): 'u32 *p=reordered,*end=reordered+14; do{*p++=0;}while(p<end);' -> worse (dh=86); same with 'end' declared before 'p' -> no change from the p<end form (dh=86, so declaration order of the two locals does not affect codegen here); same with 'while(p!=end)' -> no change (dh=86). All three up-counting forms reproduce the same shape and are firmly worse than the down-counting baseline (dh=28) -- rejected as a family, superseding note (25)'s untested up-counting gap. Copy loop, address-cached-base idiom: 'const u8 *count_ptr=&state->character_count; for(index=0;index<*count_ptr;index++) reordered[index]=state->character_ids[index];' (baseline clear loop kept) is the standout finding of this pass -- it flips the diff classification from structural-topology to topology=equal/missing-extra-code and reproduces the reference's address-cached reload of character_count inside the loop body (ldrb through a pointer register each iteration) almost exactly, but the overall score still regresses (dh=92, wrong_instructions=32) because the fix leaves one instruction-count difference versus the reference (the reference reloads the count_ptr address a SECOND time, via a fresh 'ldr'/'add' pair, right after the loop's zero-trip guard, instead of reusing the single register the candidate computes before the guard -- i.e. the reference does NOT common-subexpression-eliminate the address across the guard test and the loop preheader, but our shared 'count_ptr' variable does get CSE'd by the compiler into one load). Tried to break that CSE without giving up the address-cached shape: plain 'state->character_count' in the condition (no named pointer at all, otherwise identical block wrapping) -- collapses straight back to the original structural-topology/dh=28 shape, confirming the *address-cached* topology match is caused specifically by taking an explicit '&state->character_count' into a pointer local, not by the surrounding block. No source phrasing found that keeps the address-cached shape while forcing gcc to re-materialize the address a second time (tried: end-pointer form 'const u16 *s=ids,*e=s+count; for(;s<e;s++,index++)' -- worse, dh=99, structural-topology again, loses the address-cached shape entirely). Joint combinations (address-cached copy loop + each clear-loop variant): with the do-while descending pointer clear loop -> dh=95; with the classic 'for(p=reordered+13;p>=reordered;p--)' clear loop -> dh=91 (this form reintroduces the previously-documented extra pre-loop guard, now visible as a bcc/bcs pair bracketing the loop, on top of the copy-loop's residual double-load gap) -- both worse than baseline, confirming the two residuals do not cancel out when combined; the whole-function allocation shifts penalize the combination more than either fix helps. Verdict: baseline (dh=28, structural-topology on both loops) remains the best known state; the address-cached copy-loop idiom is the closest partial match found to date (topology=equal) but is a strict regression on the whole-function score and was not adopted. Recorded for any future attempt: the missing piece is reproducing the reference's *duplicate* address load after the guard rather than CSE'ing it, which no tried variable/scope/cast combination achieved.",
    "2026-08-30: closed byte-exact at 240/240 bytes. Marking character_count volatile reproduces the reference's repeated count loads; storing the final refreshed count through a plain u8 lvalue avoids a redundant volatile read. A non-empty descending do-while clear expressed as pointer versus stack-base reproduces the reference loop without the old counter. The final two-halfword allocation floor closed by reusing the finished loop index as the swap carrier, matching GCC 2.96's r6 lifetime. No register variable, compiler override, barrier, inline assembly, or scheduling control was used."
  ]
}
