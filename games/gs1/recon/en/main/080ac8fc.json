{
  "schema_version": 1,
  "namespace": "main",
  "address": "0x080ac8fc",
  "status": "measured-draft",
  "owner_bytes": 264,
  "candidate_bytes": 256,
  "differing_halfwords": 113,
  "wrong_instructions": 132,
  "classification": "djinn-active-slot-list-builder",
  "family": {
    "id": "template-main-080ae7fc",
    "template_owner": "main:080ae7fc",
    "template_source": "games/gs1/src/djinn/count_turns.c",
    "note": "Same djinn-record domain (record obtained via Func_08077008, two parallel 4-row 32-bit bitmask arrays at record+264 and record+248, 20-bit inner loop, active-bit-else-secondary-bit test matching the template's `if(active&mask) ... else if(row[0]&mask) ...`), but this owner does not count matches -- it packs each match into a u16 output list (row<<5 | bit | id<<8, with an extra 0x8000 flag bit set only for primary-field matches) and either scans all 4 rows (row_select==-1) or just one caller-selected row."
  },
  "notes": [
    "Read the raw assembly directly across both branches. High confidence on the algorithm: record=Func_08077008(id); for the -1 case, loop row=0..3 reading active=rows[row] (record+264+4*row) and secondary=record+248+4*row (confirmed row-varying by tracing r9, which increments by 4 each outer iteration and feeds the secondary-field offset via a snapshot into a fixed register each pass -- an easy misread on a first pass since the textual instruction looked like a constant +248); for each bit 0..19, if active has the bit set, packed=(row<<5)|bit|0xFFFF8000|(id<<8) (the pool constant 0xffff8000 is only ORed in this branch, confirmed against the single `.4byte 0xffff8000` at the end of both the -1 and selected-row code paths); else if secondary has the bit, packed=(row<<5)|bit|(id<<8) (no flag bit). The row_select!=-1 branch is the same per-bit logic restricted to one row, with the flag bit again reserved for the active-field match.",
    "First candidate compiled with the field extraction, packing, and both branches structurally present, but the register roles diverge from the very start of the prologue (id lands in r7/r9 here vs the reference's r5/fp), suggesting the two locals `record` and `id` are being prioritized in the opposite order from the source -- likely fixable by reordering which value is captured into a local first, but not chased further given time.",
    "This is the most complex owner in this batch (264 bytes, two full nested-loop branches); the algorithmic reconstruction is my best-confidence read of the bit-packing semantics, but the remaining gap is large enough (132 of roughly 120 total instructions) that register/stack scheduling differences likely compound across the whole function rather than being isolated to one spot. Recommend re-verifying the exact parameter order/count of Func_08077008's caller-visible signature and trying the opposite local-declaration order before another measurement.",
    "Adoption requires 0 differing_halfwords."
  ]
}
