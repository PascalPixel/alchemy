{
  "schema_version": 1,
  "namespace": "main",
  "address": "0x0801ef68",
  "name": null,
  "status": "measured-draft",
  "owner_bytes": 288,
  "candidate_bytes": 234,
  "differing_halfwords": 143,
  "wrong_instructions": 179,
  "classification": "window-gauge-or-underline-draw (low confidence)",
  "domain": "Takes a struct pointer (fields read at +8, +10, +12, +14, matching a UI window-like record) and a flags word; walks a signed-byte control string at a fixed pool address (0x080371c4) accumulating an offset, and for each in-range offset draws a run of 'segments' tiles into *Data_03001e8c using distinct end-cap tile values (0xF018/0xF00F seen in the reference literal pool) versus a middle value; a second, separately-guarded block draws a fixed 3-part strip using tile values 0xF080/0xF081/0xF082. Reads/writes base[0xEA3] and base[0xEA5], both near the template's own RENDER_ALT_OFS=0xEA2 offset, suggesting this owner is a sibling UI-window drawing routine sharing the same per-window render-state byte block as the template and as 0801e318.",
  "notes": [
    "The generated family report clustered this owner with template-main-0801e260 (clear_tile_attributes_in_rect.c) at 8032 bp. As with the rest of this batch, the reference assembly has none of the template's tile-clear bit logic; the only real overlap is the shared Data_03001e8c global and a broadly similar 4-high-register prologue shape common across this compiler's larger owners. This is a family-clustering false positive that gave essentially no usable control-flow starting point.",
    "This is the LOWEST-confidence dossier in this assignment batch. The retained candidate is an honest but only partially-grounded reading of the control flow (string-driven run-length draw with distinct end-cap tiles, plus a second fixed-pattern strip draw) built from a single manual disassembly pass; the candidate compiled to only 234 bytes against a 288-byte reference (a large size gap, not just a scheduling difference), and the full register set the reference uses (fp/sl/r9/r8 all pushed and live) versus the candidate's much lighter register pressure shows the real source keeps meaningfully more live state across the loop than this draft models. The exact field roles at window+8/+10/+12/+14, the true semantics of the flags&1 / flags&2 branches, the string-table type and its terminator convention, and the two end-cap tile values' exact assignment (0xF018 vs 0xF00F were assigned to first/last by pattern-matching the reference's two distinct pool constants, not independently confirmed) are all unverified.",
    "This is a clear source-shape gap needing dedicated reconstruction from a fresh, careful disassembly (ideally with the tool's --asm / instruction-level trace against the reference rather than manual reading alone), not a good target for further family-transplant attempts against the assigned template. Given the low confidence, no further respelling experiments were attempted against this draft -- the priority was flagging the real uncertainty honestly rather than iterating on a base whose overall shape is not yet trustworthy."
  ],
  "rejected_experiments": []
}
