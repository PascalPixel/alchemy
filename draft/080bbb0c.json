{
  "owner": "main:0x080bbb0c",
  "span_bytes": 6332,
  "score": {
    "measured": "2026-08-19",
    "candidate_bytes": 6292,
    "reference_bytes": 6332,
    "wrong_instructions": 1464,
    "differing_halfwords": 2885,
    "class": "wrong",
    "matched_prefix": 11,
    "note": "insns files are written by --asm only; measure after --asm, never off a bare --align",
    "insn_diff_rows": 807,
    "insn_diff_rows_normalized": 111,
    "frame": "100 == reference",
    "walk_window_rows": 12,
    "insn_diff_rows_nop_filtered": 66,
    "note2": "seven of the remaining rows are movs r0,r0 alignment pads before the reference's jump tables and pools; they cannot be written in C and reappear automatically at size parity",
    "our_side_rows": 53,
    "candidate_insns": 2462,
    "reference_insns": 2483,
    "raw_rows": "272 ours / 285 reference"
  },
  "shape": {
    "calls": null,
    "branches": null,
    "loops": null,
    "memory_ops": null
  },
  "expressed": [
    "prologue and first argument block only",
    "case 11/0 pp subtract-floor tails with shared pp_store (.L85)",
    "case 2/12 heal add-cap-rederive shape",
    "case 2/3/6-7-9 shared hp_tail (.L77)",
    "case 6/7/9 full body: item kind-switch (uninitialised kind on items 4,5,10,11,16,17), per-nibble tables, cur-based floor",
    "effect-switch case order matching reference emission (64,4,61/62,63,59,58,9..6,13..10,5,56,57,3,17..14,18..)",
    "nibble case 0 moved to reference position (after case 2)",
    "effect ids rebound to the jump table (17 were scrambled), texts and fields verified against assembled pool words",
    "heal 61/62 u16/s16 dual-read shape with single Div",
    "63 max-held-in-register shape",
    "effect 4 status_141 block",
    "case 3 hit-arm after body with text 0x854",
    "27/69 true texts (0x84f/0x850/0x84c, 0x85f/0x85e)",
    "TEXT_SIDE as per-arm pushes (crossjump forms the shared tail; bhi/bls variance is branch-range)",
    "case 4/5 subtract-shape KO tail with own dealt/store",
    "slots walk as dual byte-offset walkers (off, j) matching both inductions",
    "saved list walked as u16",
    "effect tail re-reads action->effect, no n variable",
    "drain caps re-derive via u16 field pair",
    "single gated offset (zero when object->state==255)",
    "hit seeded from target_results, no result variable",
    "case 4/5: no power gate; KO arms inline in 4/5, 3, 6/7/9",
    "TAKE_PWR without a variable in the five re-reading bodies",
    "BUFF_CLEAR clears 12c,12d,12e,12f in order",
    "sleep-wake gate as nested ifs (fold-const kept off), text 0x883",
    "shared =1/=2 store tails (.L205/.L207) reproduced by plain stores + crossjump",
    "declaration block in reference spill order (early decl = high slot): frame 104->100, 16/21 slot usage counts exact",
    "multi-target: tid local, n=0, while((u32)++n<=19), full expression per iteration",
    "kind at function scope between power and cmd (ref slots: power=12 kind=8 cmd=4)",
    "DK_ATTACK: defense and action->power each read once into a local (def r1->fp, apwr r5 across the call), raw-proven",
    "multi-target scan compares target_id (sl) directly, no tid local",
    "saved-slot refresh: do-while, count-- before the call, *q++",
    "all 60 message ids are absolute Value_<hex> symbols: the reference pools 0x820/0x850/0x860/0x870, which thumb_shiftable_const proves a literal never does",
    "multi-target loop index via nested s32 idx (copy-prop folds it; emission unchanged, recorded)",
    "actor drain heals (EFX_DRAIN_HP/_HALF/_PP): add-cap-rederive through a block-scope heal local, single store at the end, full-test re-reads the max field -- size now equals the reference exactly",
    "ADJUST_ATKDEF/ADJUST_RES carry the turns field as an offset constant assigned to a local at the top: the assignment lives in a call-saved register across both pushes (306/308/310 exceed the movs immediate, so const-prop keeps the pseudo). r6 usage 84 -> 161 against the reference's 156; the attack handler now matches to two scheduler rows",
    "turns store is *(u8 *)((u8 *)target + toff) = 7 through that local",
    "do{}while(0) macro wrappers (CLAMP_MOD, TAKE_BONUS, APPLY_GUARD, TEXT_SIDE) emit NOTE_INSN_LOOP_BEG/END pairs that survive to sched2 and act as same-register scheduling barriers; bare-brace macros remove them. KO push pairs now match the reference order (r0 before r1 at the second push), tid-copy before cur=0",
    "dependence-table method validated end to end: rtl-schedule table + -fsched-verbose=5 traced the tie to insn 8949's barrier-induced dependence on the r1 copy",
    "falloff 5/6/8 select really is a switch: balanced tree, one selector load (earlier rejection was measured against stale insns)",
    "HEAL_60: volatile u16 mentions give the reference's four independent loads; maxv read once ahead of the if; div result added directly into heal (both restore-7 and heal-60)",
    "power routed through dmg at the three CalcPower sites: dmg = action->power; dmg = Battle_CalcPower(dmg, ...) -- the reference's ldrh into r5 plus copy to r0 is the accumulator, and the ldrh outranks the 128 build in the dependence table because dmg's chain is longer",
    "ADJUST_RES does NOT carry the early toff: the reference's res handlers build 310 after the pushes (movs #155 per body, crossjump shares only the lsls/strb tail). With the plain member store back, all four res bodies exist separately exactly as the reference lays them out. The early-toff pattern is ATK/DEF only.",
    "stub map: 13 of 14 out-of-line b-stubs already identical to the reference in content and predecessor count",
    "cross-jump depth rule measured: the final jump2 pass (after sched2) merges identical tails but never across a call insn -- the res bodies' movs #155 stays per-body for exactly that reason",
    "the reference's atk family is itself mixed (two early-toff in r6, two rematerialized late): one uniform toff macro under varying per-body register pressure",
    "walk: jsave local (the reference's lr pseudo) with i/jsave/j initialized before the 254-test, matching the reference's pre-if init cluster. Found via a 32-variant matrix scored on the walk window: base2/woff/val toggles are inert or harmful; preinit+jsave alone lifts global coloring (wrong 1555 -> 1481, best yet)",
    "walk 254-recheck reads through a union member (union Cell). gcse's exp_equiv_p refuses MEMs whose MEM_ALIAS_SET differ, and lang_get_alias_set gives union accesses set 0 while the plain s16 cast carries the short set -- the PRE hoist that fabricated our ldrh cache cannot unify them. Walk window 26 -> 14 rows; the jsave-in-lr copy (movs r6, lr shape) materializes",
    "found255 store index through a block local t = jsave + 102: arithmetic definitions survive copy propagation where plain copies fold, giving the reference's movs/adds/strh-indexed store",
    "hit assignment through a first local with per-arm stores: first = plan->target_results[slot]; if (first == -1) hit = HitCheck(...); else hit = first. The old shape stored hit before the test, spilling the ldrsb across the call; the reference defers the store into each arm. Clears the ldrsb spill and the HitCheck argument-order rows",
    "EFX_AGI_SET_DOWN4 stores -4 through an s32 local: the reference builds movs #4/negs in a register (sharable with the clamp constant) where the literal 252 truncates at expand",
    "the two damage-emphasis TEXT sites decide the text in a variable and push once (TEXT_SIDE_V): the reference's arms are argument-only stubs feeding one shared push, which crossjump cannot have made because the match would cross the bl. Per-site measurement keeps the other ten sites per-arm",
    "attack KO arm order: if (cur > 0) FINISH else KO at the attack case only (siblings measured better KO-first)",
    "the two emphasis sites interleave cur -= dmg between the text select and the push -- hand-written order the reference's arm blocks carry the subtraction in",
    "session arc: 236 -> 111 normalized reference rows across ~30 committed source facts; the fix-cascade-reread loop is the working method and each cycle costs one landscape shift",
    "all five subtract-then-text tails interleave: message select, subtraction, single push -- attack, case-6 emphasis, drain, PP damage, case 3. Found as one family across two reread cycles; wrong 1551 -> 1459",
    "multi-target scan is a goto loop with an idx local: no loop notes, so no strength reduction and no hoisting; the 748 rebuilds per iteration exactly as the reference emits (a stale-era rejection reversed by truthful measurement)",
    "scan head probe indexes through hidx = 748 (const-prop keeps constants wider than a movs immediate)",
    "case-6 KO text interleaves cur = 0; the attack subtracts between UNIT and VALUE -- per-site drift mapped one site at a time",
    "the four interleave sites subtract AFTER the text push in the source; the scheduler hoists the subs into the call setup and the movs #4 wins its tie at every site as the reference shows",
    "message-site census (per-id ldr counts against the assembled reference) is the body-merge oracle: it pinned the def merge to the DEF_UP pair and exposed the crit text arithmetic",
    "DEF_UP1 assigns its turns offset late; the shorter range wins the call-saved register, the body differentiates, jump2 stops merging -- the def bodies split",
    "the crit text is MSG_CRITICAL with += 1 for the player side; 0x823 is never loaded, matching the reference's single load and adds #1",
    "def-down pair splits with late turns offsets at both sites: all five 154-builds present",
    "pool-value census is fully clean (222=222 words, every 0x8xx and offset value loads equally); remaining deficits are copies, shared push tails, and seven alignment nops",
    "modifier family early/late map complete: ATK_DOWN1 and ATK_UP2 hold offsets across the pushes, the other six store plainly -- six pair-combinations measured, DOWN2+UP1 decisive",
    "permuter walk finds: two init orderings in the summon block adopted; the walker's reachable neighborhood at current weights is clean after ~53k evaluations"
  ],
  "unexpressed": [
    {
      "op": "attack-KO FELLED arm share",
      "count": 9,
      "where": "ref loads FELLED twice (attack's arm jumps into case 3's suffix through the pushes to hp_tail); ours three times. Suffixes are source-identical; the merge waits on register identity"
    },
    {
      "op": "def-body merge",
      "count": 9,
      "where": "same gate"
    },
    {
      "op": "walk head",
      "count": 7,
      "where": "ip copy, second 100, 255-cache; ~15 spellings measured"
    },
    {
      "op": "scan-2 rebuild and 077188/077190 scheduling",
      "count": 10,
      "where": "texture"
    },
    {
      "op": "remaining copy/pair texture",
      "count": 52,
      "where": "register identity following the above"
    }
  ],
  "verified_against_reference": [
    "seven-instruction register-save prologue matches exactly"
  ],
  "blocked_by": "not blocked -- barely started",
  "rejected": [
    {
      "shape": "APPLY_PP_HIT compare-first in cases 0/11",
      "wrong": "reference subtracts then floors at zero"
    },
    {
      "shape": "HEAL_CUR cap-before-add in cases 2/12",
      "wrong": "reference adds, caps, re-derives display delta from the field"
    },
    {
      "shape": "walk pseudos jsave/probe/woff matching ref lr/ip + second 100",
      "wrong": 1788,
      "note": "coloring churn +306; ref provably has them, retry after other regions settle"
    },
    {
      "shape": "goto loop to suppress strength reduction",
      "wrong": 1574,
      "note": "goto did not stop SR; walker persists either way"
    },
    {
      "shape": "scan-2 walking tbl itself to break the CSE fold",
      "wrong": 1826,
      "note": "fold persists, plus churn"
    },
    {
      "shape": "muls operand order via source operand order (kind*max vs max*kind, dmg*tbl vs tbl*dmg)",
      "wrong": 1552,
      "note": "emission order is unchanged by source order -- rtx canonicalization picks it from pseudo numbering; the two sites stay swapped vs ref. switch vs else-if for the 5/6/8 falloff is also emission-identical"
    },
    {
      "shape": "walk pseudos jsave/probe/woff, second attempt after slots+r6 converged",
      "wrong": 1702,
      "note": "copy-prop folds all three again (no lr/ip in output); ref's movs lr,r4 / movs ip,r1 must come from reload or a source shape not yet found. Region is ~15 rows"
    },
    {
      "shape": "walk probe recomputed from work in the else-arm",
      "wrong": 1555,
      "note": "gcse folds it; output byte-identical"
    },
    {
      "shape": "volatile 255-probe in the walk",
      "wrong": 1577,
      "note": "blocks too much, +13 normalized rows"
    },
    {
      "shape": "def-body split via local pressure",
      "note": "toff allocation is the global allocno lottery: our atk toffs held r6 at commit 5d4afa4e6 and lost it again after unrelated edits; ref itself is mixed 3-early/5-late across the eight bodies. The split follows global coloring convergence, not local spelling"
    },
    {
      "shape": "-fno-gcse routed for the whole TU",
      "note": "fixes the walk fingerprint (single ldrsh, ip pseudo appears) but is globally worse: 872 vs 742 normalized bin rows. The reference was compiled with gcse on; its no-fold shapes have another source"
    },
    {
      "shape": "scan probe via BattleWorkPtr cell re-read",
      "wrong": 1455,
      "note": "best global scores ever, but the emitted loop reads the cell where the reference reloads its spilled work local -- wrong instructions aligned by luck, rejected"
    },
    {
      "shape": "volatile on the walk 254-recheck",
      "wrong": 1565,
      "note": "walk window 26->24, global flat; compasses disagree, parked"
    },
    {
      "shape": "permuter campaigns",
      "note": "16,500 candidates over halfword, instruction, and coloring-weighted fitness: zero improving mutations. Improving moves are coordinated multi-statement changes"
    },
    {
      "shape": "walk-engine permuter, 30000 candidates on the 66-row landscape (seed 77, keep-prob 0.6, pret walk engine)",
      "wrong": "no improvement: best=baseline 1146 rows / 2885 halfwords; 0 compile failures; run out/perm-w27"
    }
  ],
  "note": "Largest target in the list. No permanence marker anywhere in the span -- no Thumb multiple-transfer, no SWI, no veneer idiom, nothing in asm/classification.json -- so every byte is reachable as C.",
  "parking_lot": [
    "Pascal 2026-08-19: reduce the tooling from 162k lines to ~40k. Biggest masses: coverage-map 9.3k, permuter 5.9k, build-assets 5.4k, full-c-progress 4.5k, per-asset data crates ~2-4k each x dozens. Likely levers: merge the ~50 asset-builder crates into one table-driven builder, collapse the metrics/coverage stack, dedupe the disasm/canonicalizer copies across candidate-show/overlay/permuter. Separate campaign; do not start mid-owner."
  ]
}