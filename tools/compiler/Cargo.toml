[workspace]

[package]
name = "compiler"
version = "0.1.0"
edition = "2021"

[dependencies]
candidate-show = { path = "../candidate-show" }
dashboard-server = { path = "../dashboard-server" }
permuter = { path = "../permuter" }

[[bin]]
name = "compiler"
path = "src/main.rs"
