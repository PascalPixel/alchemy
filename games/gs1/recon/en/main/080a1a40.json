{
 "schema_version": 1,
 "namespace": "main",
 "address": "0x080a1a40",
 "status": "measured-draft",
 "owner_bytes": 126,
 "executable_owner_bytes": 126,
 "candidate_bytes": 124,
 "differing_halfwords": 38,
 "wrong_instructions": 35,
 "cross_edition": {
  "object_bytes": 126,
  "literal_bytes": 28,
  "relocation_normalized_core_bytes": 98,
  "core_diff_bytes": 0,
  "core_identical": true,
  "starts": {
   "ja": "0x080989ec",
   "en": "0x080a1a40",
   "de": "0x080a4840",
   "es": "0x080a6234",
   "fr": "0x080a6240",
   "it": "0x080a1a40"
  }
 },
 "domain": "Positions a battle-effect sprite from its anchor record, phase-specific coordinate tables, and caller offsets while updating its packed X attribute.",
 "expressed": [
  "selects phase-specific X and Y base offsets from the active render phase",
  "adds the anchor grid coordinates, caller offsets, and eight-pixel bias",
  "stores the full X and Y coordinates in the sprite runtime record",
  "updates the packed nine-bit X field while preserving its upper attribute bits",
  "mirrors the low Y byte into the sprite attribute byte",
  "reads the mutable render phase independently for the X and Y coordinate snapshots"
 ],
 "bounded_search": {
  "mode": "screen-only",
  "attempted": 0,
  "exact": false,
  "reason": "The phased, symbol-indexed candidate is two bytes short with 38 differing halfwords and remains outside exact-extent and <=20-halfword admission."
 },
 "notes": [
  "Independent object analysis bounds this owner at 126 executable bytes and proves all 98 literal-normalized core bytes identical across the six approved GS1 editions.",
  "2026-09-01: naming the two absolute coordinate tables as indexed arrays restores the reference register-offset ldrb form; staging X and Y separately restores their observed read phases; and expressing the packed attribute merge as distinct narrowing operations restores the 0xffff, 0x01ff, and -0x200 literal family.",
  "Taking the sprite and anchor field snapshots in one source phase makes their lifetimes overlap the state pointer. GCC then assigns state, anchor, and sprite to the reference r5, r6, and r4 roles, reducing the candidate from 54 to 38 differing halfwords and from 65 to 35 wrong instructions while growing it from 116 to 124 of 126 bytes.",
  "The remaining residual begins with phase-address scheduling and its coupled mask carrier, then continues through packed-field carrier allocation and literal-pool placement. It is still broad rather than a localized compiler floor. No permanent classification is claimed, and zero DONE bytes are claimed.",
  "Recovered only from retained owner assembly, approved local GS1 ROM correspondence, recovered battle-effect caller 080ab5e4, and the approved compiler. No GS2 evidence or external source was used."
 ]
}
