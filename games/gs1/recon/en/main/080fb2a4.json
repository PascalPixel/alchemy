{
  "schema_version": 1,
  "namespace": "main",
  "address": "0x080fb2a4",
  "status": "measured-draft",
  "owner_bytes": 40,
  "executable_owner_bytes": 40,
  "candidate_bytes": 32,
  "differing_halfwords": 20,
  "wrong_instructions": 22,
  "classification": "update-music-player-pitch",
  "resolved_asm_classification": {
    "kind": "compiler_output",
    "retention": "c_candidate",
    "confidence": "likely"
  },
  "cross_edition": {
    "editions": ["ja", "en", "de", "es", "fr", "it"],
    "starts": {
      "ja": "0x080f22a4",
      "en": "0x080fb2a4",
      "de": "0x080fc0a4",
      "es": "0x080fdaa4",
      "fr": "0x080fdaa4",
      "it": "0x080fb2a4"
    },
    "relocation_normalized_core_bytes": 34,
    "core_diff_bytes": 0,
    "core_identical": true
  },
  "bounded_search": {
    "mode": "classic-heat-walk",
    "candidates": 20000,
    "exact_candidates": 0,
    "best_readable_candidate_bytes": 40,
    "best_readable_differing_halfwords": 8,
    "best_readable_wrong_instructions": 10,
    "best_unverified_differing_halfwords": 7
  },
  "bounded_checks": [
    "re-examined the retained assembly directly this session: the reference's extra 8 bytes are entirely its push{r4,lr}/pop{r4}/pop{r0}+bx r0 frame, needed because it keeps the truncated pitch value resident in r1 across the ldrh of pitch_scale (forcing pitch_scale into a fresh r4); this candidate's register allocator instead reuses r1 for pitch_scale and keeps pitch in r3, needing no extra register and compiling as a leaf function (bx lr, no frame). Declaring the truncated value as `s32 pitch = (u16)value` instead of `u16 pitch = value` produced byte-identical output, confirming this is the same register-reuse-choice residual as the prior two respellings, not a new lead",
    "all six statement-order permutations of pitch truncation, scale load, and identity load were compiled; the only 40-byte improvement loads pitch_scale before validating the identity",
    "direct identity access, local identity, register hints, nested assignment, duplicate guarded loads, and approved optimization-pass toggles did not close the residual",
    "an explicit gcc296 scheduling route regressed to 15 differing halfwords",
    "the 20,000-candidate walk found no exact output; its seven-halfword generated best changes the multiplication input and is not a semantic candidate",
    "2026-08-27: re-verified the measured baseline (candidate-show: candidate=32 reference=40 differing_halfwords=20, class=wrong, wrong_instructions=22) against the current committed source, then tried two further source respellings not previously logged: (a) swapping the multiply operands to `(player->pitch_scale * pitch) >> 8`, and (b) hoisting `player->pitch_scale` into an explicit `u16 scale;` local read after the `player->pitch = pitch;` store (same read position as the reference, just named). Both compiled byte-identical to the existing 32-byte leaf candidate -- GCC 2.96 normalizes the commutative multiply before RTL and the local-vs-inline field read makes no allocation difference. This is the same register-choice residual already isolated: the reference allocator commits a fresh callee-saved r4 to pitch_scale and keeps pitch resident in r1 (forcing the push/pop frame), while this compiler's allocator instead keeps pitch in a fresh caller-saved r3 and reuses the freed r1 slot for pitch_scale, staying leaf. No source-level lever discovered this session moves that choice; treating it as a confirmed floor for this owner, consistent with the session's broader 'final register-letter swap not controllable via source reordering' finding (see main CLAUDE.md dead-end catalog), here manifesting as leaf-vs-framed instead of a same-size register swap.",
    "2026-08-31 bounded family follow-up: changing the second parameter from `s32 value` plus a local `u16 pitch` to a direct `u16 pitch` parameter produced byte-identical 32-byte output. GCC 2.96 performs the same entry truncation and constructs the same allocation regardless of whether the narrowing is expressed in the function type or the body.",
    "2026-08-31 bounded family follow-up: qualifying the music-player state as volatile produced 40 bytes / 17 instructions, but not the reference shape: volatile aggregate assignment requires an otherwise-absent `ldrh` before each halfword store, yielding 10 differing instructions. The ROM has plain stores, so this state is not volatile at this interface; the matching frame length is incidental and not an adoption lead."
  ],
  "notes": [
    "Stores a validated music-player pitch and derives its scaled pitch from the player's 8.8 fixed-point scale.",
    "Correct field widths eliminate opcode mismatches, but the honest guarded source compiles as a shorter leaf because the scale does not remain live in a callee-saved register.",
    "The best readable 40-byte diagnostic has the reference instruction multiset but retains an eight-halfword linked allocation/scheduling residual and speculatively reads pitch_scale before the identity guard. That access change is not justified by the reference semantics, so it was not retained.",
    "The residual exceeds the narrow permanent-assembly gate. Retained assembly remains authoritative and this draft claims zero DONE bytes.",
    "Recovered only from retained owner assembly, approved local GS1 ROM correspondence, and the approved compiler. No GS2 source or data was inspected or changed."
  ]
}
