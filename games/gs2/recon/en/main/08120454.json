{
  "schema_version": 1,
  "namespace": "gs2-en-main",
  "address": "0x08120454",
  "name": "BattleResolveAction",
  "status": "measured-draft",
  "owner_bytes": 8300,
  "score": {
    "measured": "2026-08-31",
    "candidate_bytes": 8168,
    "reference_bytes": 8300,
    "differing_halfwords": 4018,
    "wrong_instructions": 2483,
    "class": "missing-extra-code",
    "command": "compiler candidate-show --target gs1 --flags -mthumb-inline-register-call --owner 08120454 --size 8300 --rom roms/gs2-en.gba games/gs2/recon/en/main/08120454.c",
    "note": "Raw halfwords remain phase-dominated by displaced pools. The evidenced inline-call route first improved 3914 -> 3912 halfwords. Reconstructing the state/command lifetime made the complete post-copy call, state load, and command-pointer store instruction-identical. The PP-restoration call audit then removed a duplicated Math_Div block: effect 0x4d retains its own call while 0x4e and the default arm now share the one tail present in the ROM. This exposes a 132-byte structural deficit rather than masking twelve bytes with surplus code; raw/global alignment metrics are not a safe selection metric for this local repair."
  },
  "shape": {
    "architecture": "Twelve-ROM master: games/gs1/recon/ja/main/080b2b0c.c is the shared body, written against ~60 BATTLE_* hook macros (362 uses). GS1 editions bind the hooks to GS1 semantics; games/gs2/recon/ja/main/battle_delta.h rebinds them with the sequel's grown logic (extra effect/heal/KO/guard cases) plus per-edition literal headers and ~200 per-edition callee retargets. BATTLE_SECOND_ACTOR reshapes struct BattlePlan in games/gs1/include/battle_types.h.",
    "constraint": "The master body must stay byte-exact for gs1-en: measured 6332/6332 at 0 differing halfwords when wrapped as Func_080bbb0c against roms/gs1-en.gba. Every GS2-side repair must live in the hook bodies, the delta headers, or GS2-only conditionals -- never in unguarded shared text.",
    "gs1_status": "main:080bbb0c is production-exact and relink-proven byte-exact in all six GS1 editions (exact-main-builds, location_methods asaaaa). Six of the twelve ROMs stand.",
    "gs2_editions": "All six GS2 editions are core-identical for this owner (games/gs2/recon/cross-edition.json): one exact edition source closes all six through the existing literal/retarget headers."
  },
  "unexpressed": [
    {
      "op": "register allocation",
      "where": "entry region: candidate permutes r1/r2/r5 against the reference and parks sl one instruction late; the added GS2 hook bodies shift pressure and LUIDs across the whole owner"
    },
    {
      "op": "literal pool placement",
      "where": "pools sit at different sites (first large divergence around the 0x780 branch cluster); the reference's pool anchors imply different branch-range tensions than the candidate's still-missing state/effect blocks produce"
    },
    {
      "op": "state and effect control-flow extent",
      "where": "the corrected command-pointer lifetime reveals a real 120-byte deficit. The first state block still lacks the reference's distinct command-pointer refresh tails, and later deficits must be localized by aligned call and branch sequences rather than compensated with redundant setup"
    }
  ],
  "notes": [
    "Accepted compiler route: `-mthumb-inline-register-call` restores GCC's historical `mov lr, rN; .hword 0xf800` Thumb indirect-call lowering. The GS2 EN ROM contains 1,828 instances of that signature while GS1 EN contains none, so the route is GS2-only and default-off in agscc. At this owner's single site it reproduces the four reference bytes exactly.",
    "The JA-native GS1 candidate (games/gs1/recon/ja/main/080b2b0c.c wrapped as Func_080b2b0c) measures 6332/6332 at 272 differing halfwords against roms/gs1-ja.gba; since the production EN object relinks byte-exact at the JA address, those 272 halfwords are JA-side binding/literal gaps in the wrapper, not core differences.",
    "Recovered only from retained/located owner bytes in the approved local ROMs, the adopted GS1 EN source, in-repository headers, and the approved compiler.",
    "This candidate claims zero DONE bytes."
  ],
  "rejected_experiments": [
    {
      "experiment": "shipped reload/allocation flag probe: -fno-regmove, -fno-schedule-insns, -fno-schedule-insns2, -fno-cse-follow-jumps, -fcaller-saves, -fno-caller-saves, and call-used r5/r6 variants",
      "result": "No flag improved the whole owner. Most were byte-neutral at 3912 halfwords, -fno-schedule-insns2 and -fno-cse-follow-jumps regressed, and the call-used r5/r6 routes trigger GCC 2.96 internal errors. No per-TU flag route is adopted."
    },
    {
      "experiment": "split the first state command pointer from the later cmd variable",
      "result": "It restores some tail stores but keeps the early pointer in r5 instead of the reference stack slot, measuring 8224 bytes / 3973 halfwords / 2615 wrong instructions. The shared lifetime is closer and retained."
    },
    {
      "experiment": "combine -mthumb-inline-register-call with the experimental -mminipool-first-barrier route",
      "result": "The compiler segfaults while compiling this owner. This is a recorded miss, not evidence for the reference's literal-pool route; the pool option is not adopted for this translation unit."
    },
    {
      "experiment": "remove the canonical -mthumb-interwork flag",
      "result": "Neutral on the call model: GCC still emits `_call_via_r3`; 8272/8300 bytes, 3914 differing halfwords, 2570 wrong instructions. This is not the missing route."
    },
    {
      "experiment": "enable shipped -mcaller-super-interworking",
      "result": "GCC changes the call to `_interwork_call_via_r3`, confirming the site is controlled by the Thumb indirect-call backend, but that is not the ROM's inline register-call sequence and the verifier correctly has no canonical address for the absent veneer. Not adopted."
    },
    {
      "experiment": "declare 0x03000730 as a direct absolute function instead of a function pointer",
      "result": "Regressed to 8252/8300 bytes, 3961 differing halfwords, and 2597 wrong instructions; GCC emits a direct branch rather than the ROM's register call. Reverted."
    },
    {
      "experiment": "BATTLE_HEAL_PREP with all arms sharing one Math_Div through a goto tail (num/den computed per arm)",
      "result": "Over-merges: the candidate falls to 8272 bytes as the sign-extend epilogue is shared too; the reference shares the divide call site but keeps per-arm preparation. 3917 halfwords, 2618 wrong instructions. Reverted."
    },
    {
      "experiment": "else arm spelled with direct both-width loads (maxu then maxv via casts of &target->max_hp)",
      "result": "GCC folds the second load into a sign-extend of the first; the reference emits two loads (ldrh then ldrsh). The GS1 pointer-walk idiom defeats the fold and is kept for that arm only."
    }
  ],
  "expressed": [
    "The PP-restoration cluster now has the reference's two-call topology: effect 0x4d keeps its separately sign-extended Math_Div result, while effect 0x4e and the default arm stage numerator/divisor values and branch to one shared Math_Div tail. The external-call sequence no longer contains the candidate-only fourth divide; this removes 12 bytes of surplus code and exposes the true remaining deficit.",
    "BATTLE_PLAN_LOADS now creates range before target_id. This reproduces the reference range->r1 reload and extends the exact entry prefix by one instruction; the remaining target-id r5 carrier is an uncovered local-reload decision.",
    "The active GS2 state block now establishes cmd after the state load, letting GCC schedule the inline call, state address/load, and command-pointer store exactly as the ROM. It cuts wrong_instructions 2570 -> 2439 and exposes rather than masks the remaining 120-byte control-flow deficit.",
    "BATTLE_HEAL_PREP direct-access respell: the 0x4c/0x47/0x46 arms now read max_hp through direct casts, emitting the reference's movs #52 / ldrsh reg-offset + ldrh immediate forms instead of pre-adding 52 into the base; the else arm keeps the GS1 pointer walk to preserve its unfolded double-width load. Cluster verified locally against the reference; wrong_instructions 2608 -> 2574.",
    "Remaining in this cluster: the reference shares one Math_Div call site across the calling arms (arms branch to a common 0x1458 tail with the divisor staged per arm) while the candidate duplicates the call; the exact sharing boundary excludes the maxu sign-extend, so a plain goto over-merges."
  ]
}
