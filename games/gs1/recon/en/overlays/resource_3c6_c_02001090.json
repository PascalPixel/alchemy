{
  "span_bytes": 300,
  "score": {"measured":"2026-08-24","candidate_bytes":300,"reference_bytes":300,"wrong_instructions":18,"differing_halfwords":117,"class":"compiler-cse-allocation"},
  "shape": {"calls":37,"loops":0,"memory_operations":0,"reference_high_register_moves":0},
  "verified_against_reference": [
    "complete deterministic reconstruction accounts for all 37 calls, every argument, the entry literal, and the exact 300-byte owner extent",
    "the direct-expression source hoists repeated 0x100 construction into r5, adding r5 save/restore, while the reference has zero high-register moves and rebuilds 128 shifted left once at each site",
    "separately scoped named constants preserve the same approved-compiler common-subexpression allocation"
  ],
  "retention": "keep_structured_asm",
  "note": "No-loop call script retained because the approved compiler shares repeated constants across calls while the ROM deliberately reconstructs them."
}
