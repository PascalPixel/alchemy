#!/bin/sh
set -eu
psynergy_root=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
exec cargo run --offline --quiet --release --manifest-path "$psynergy_root/tools/psynergy/Cargo.toml" --target-dir "$psynergy_root/out/cargo-target" --bin psynergy -- "$@"
