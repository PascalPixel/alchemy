{
  "schema_version": 1,
  "namespace": "main",
  "address": "0x08021d88",
  "status": "proven-retained-assembly",
  "owner_bytes": 114,
  "candidate_bytes": 114,
  "differing_halfwords": 4,
  "wrong_instructions": 0,
  "classification": "compiler_local_scheduling_mismatch",
  "resolved_asm_classification": {
    "kind": "compiler_local_scheduling_mismatch",
    "retention": "keep_structured_asm",
    "confidence": "proven"
  },
  "domain": "Draws one encoded character cell, records its source value and render command, and preserves the cell style bits while replacing the character code.",
  "evidence": [
    "The ordinary-C reconstruction emits the complete 114-byte owner with identical instructions, registers, calls, literals, memory operations, and branch topology outside two localized adjacent ordering swaps.",
    "At the entry call setup, the candidate completes the independent entry-pointer add before the four-byte frame adjustment; the reference adjusts the frame first. Both streams immediately resynchronize before argument setup.",
    "After the call, the candidate reloads the saved base before shifting the independent store-offset constant; the reference shifts first. Both streams immediately resynchronize before forming the store address.",
    "GCC pass 23 sched2 shows both choices as ready-list decisions. rank_for_schedule in alchemy-gcc/agbcc/gcc_arm/sched.c falls through equal priority and dependency class to original INSN_LUID order; the source cannot directly order a generated prologue adjustment against an independent expression."
  ],
  "bounded_source_probe": {
    "tool": "tools/shape-search/search.ts",
    "depth": 8,
    "legal_variants": 10,
    "best_differing_halfwords": 4,
    "verdict": "miss"
  },
  "cross_edition": {
    "editions": ["ja", "en", "de", "es", "fr", "it"],
    "relocation_normalized_core_bytes": 94,
    "core_identical": true,
    "raw_owner_bytes_identical": true,
    "starts": {
      "ja": "0x08021dec",
      "en": "0x08021d88",
      "de": "0x08020a4c",
      "es": "0x08020dc4",
      "fr": "0x08020d2c",
      "it": "0x08021d30"
    }
  },
  "provenance": "Recovered only from retained GS1 assembly, the local six-edition ROM set, in-repository declarations, and the approved compiler and its shipped dumps. No external game source, copied instruction bytes, fixed-register variable, inline assembly, compiler barrier, or compiler modification was used.",
  "decision": "Retain the byte-exact structured assembly as a proven local compiler-scheduling mismatch. The complete ordinary-C reconstruction remains the semantic source artifact and all 114 owner bytes count as DONE."
}
