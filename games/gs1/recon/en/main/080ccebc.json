{
  "schema_version": 1,
  "namespace": "gs1-en-main",
  "address": "0x080ccebc",
  "status": "measured-draft",
  "owner_bytes": 584,
  "score": {
    "measured": "2026-08-31",
    "candidate_bytes": 588,
    "reference_bytes": 584,
    "differing_halfwords": 271,
    "wrong_instructions": 216,
    "class": "structural-topology",
    "note": "Fresh late-battle playbook pass. The four apparent Func_080072f4 calls are now modeled as ordinary six-argument calls through *(DrawRectangleFn *)0x03001F08: the retained bytes load r4 from that cell immediately before entering 0x080072f4, the r4 slot of the _call_via_rN trampoline, and pass width and height in the two stack argument slots. The calls are therefore draw_rectangle(second, base, x, y, shade, shade), matching the exact member_orbit family idiom. The frame-10 Func_080d6888 call was also corrected from the old draft's reversed convention to (member_id, 7, -1, member_index, 8), directly matching the retained r0-r3 and stack argument setup. These semantic repairs reduced wrong instructions 244->216 and made the byte extent nearly exact (544->588 versus 584), while differing halfwords stayed 271. The remaining residual is honestly structural-topology: the candidate uses a 24-byte frame versus the reference's 32 and diverges in the initial work/canvas/local-position allocation before the first effect-position call. No exact sibling shape names that missing lifetime, so this pass stops rather than inventing padding or forcing allocation."
  },
  "rejected_experiments": [
    {
      "experiment": "read the shared struct base directly from ADDR_03001EEC vs loading ADDR_03001EF0 first and subtracting 4",
      "result": "reference loads ADDR_03001EF0's address into a register, keeps that same register as `second` (used later as Func_080072f4's first argument via `mov r0,fp`), and reaches the +0x7828-holding base struct by subtracting 4 from it rather than loading ADDR_03001EEC separately; matching that order was needed to get the same prologue register set (fp/sl/r9/r8, matching the reference exactly) instead of an extra/missing saved register",
      "class": "accepted"
    },
    {
      "experiment": "pass `base` (the ADDR_03001EEC-rooted struct pointer) as Func_080072f4's first argument",
      "result": "the retained assembly's `mov r0,fp` before each Func_080072f4 call uses the OTHER cached pointer (`second`, from ADDR_03001EF0), not `base`; switching the argument to `second` is structurally correct even though the resulting score did not change much, because the call's other three arguments and the surrounding loop shape still differ"
    }
  ],
  "notes": [
    "Recovered only from the retained owner assembly, approved local ROM, and the approved compiler.",
    "This is a single-pass structural draft, not iteratively tuned: the loop count/shape, the four draw-call rows, the averaging idiom, and the scheduler bracket are read directly off the assembly and are reasonably solid. The `count`/`Func_080d6888` inner block, the exact Scheduler_AddOrUpdateCallback second argument (written here as a bare 0x480 magic number, `144<<3`), and the +0x59 message id passed to Func_080e0524 are transcribed from the instruction operands without independent confirmation of their semantic meaning.",
    "The rectangle-callback and Func_080d6888 argument conventions are now established from retained register provenance and the exact member_orbit family. The remaining gap begins earlier, in stack-local lifetime and prologue structure; this candidate still claims zero DONE bytes."
  ]
}
