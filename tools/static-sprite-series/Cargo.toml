[workspace]

[package]
name = "static-sprite-series"
version = "0.1.0"
edition = "2021"

[lib]
name = "static_sprite_series"
path = "src/lib.rs"

[dependencies]
extract-resource = { path = "../extract-resource" }
import-asset = { path = "../import-asset" }
serde_json = { version = "1", features = ["preserve_order"] }
