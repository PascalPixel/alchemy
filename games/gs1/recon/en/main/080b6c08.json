{
 "schema_version": 1,
 "namespace": "gs1-en-main",
 "address": "0x080b6c08",
 "status": "measured-draft",
 "owner_bytes": 136,
 "score": {
  "measured": "2026-08-26",
  "candidate_bytes": 140,
  "reference_bytes": 136,
  "differing_halfwords": 57,
  "wrong_instructions": 68,
  "class": "wrong",
  "note": "The reference's first scan walks a pointer (adds r2,#88 base then adds r2,#2 per element) with BOTH load widths present per element (movs rZ,#0; ldrsh [r2,rZ]; ldrh [r2,#0]); its second scan walks the base while keeping the 100 offset constant in a register. The candidate keeps base+walking-offset for both and folds the unsigned read into an extend of the signed one."
 },
 "rejected_experiments": [
  {
   "experiment": "s16* walking pointer for scan one and base-walk for scan two",
   "result": "160 bytes, 79 halfwords: Thumb ldrsh has no immediate form, so the pointer spelling forces zero-register materialization per use and the loop shape degrades"
  },
  {
   "experiment": "unsigned-before-signed dual-read order (the heal-prep fold lever)",
   "result": "identical 57 halfwords, 68 wrong instructions; the fold does not flip here"
  }
 ],
 "notes": [
  "Recovered only from the retained owner assembly, approved local ROM, and the approved compiler.",
  "This candidate claims zero DONE bytes."
 ]
}
