{
 "schema_version": 1,
 "namespace": "main",
 "address": "0x080b8574",
 "status": "proven-retained",
 "owner_bytes": 376,
 "executable_owner_bytes": 376,
 "candidate_bytes": 388,
 "differing_halfwords": 181,
 "wrong_instructions": 180,
 "cross_edition": {
  "object_bytes": 376,
  "relocation_normalized_core_bytes": 336,
  "core_diff_bytes": 0,
  "core_identical": true,
  "starts": {
   "ja": "0x080af574",
   "en": "0x080b8574",
   "de": "0x080bab8c",
   "es": "0x080bc58c",
   "fr": "0x080bc58c",
   "it": "0x080b858c"
  }
 },
 "domain": "Builds two groups of battle-presentation unit records, assigns their display values and priorities, then sorts the combined records by value.",
 "expressed": [
  "loads the first unit group and emits fixed-priority 16-byte entries",
  "loads the second unit group and derives randomized values and priorities",
  "warms the four character runtime records before reading the active groups",
  "sorts the combined entry list in descending value order",
  "uses the original three-step DMA record swap with a 16-byte stack temporary",
  "returns the combined number of emitted entries"
 ],
 "bounded_search": {
  "mode": "screen-only",
  "attempted": 0,
  "exact": false,
  "reason": "The first complete readable candidate is 12 bytes long with 181 differing halfwords and 180 wrong instructions, outside both the exact-extent and <=20-halfword gates."
 },
 "notes": [
  "Independent object analysis bounds this owner at 376 executable bytes and proves all 336 relocation-normalized core bytes identical across the six approved GS1 editions.",
  "The source captures the complete two-group record construction and descending DMA bubble sort. Remaining differences span register allocation, loop shape, and volatile DMA scheduling rather than a localized residual.",
  "No full search or permanent classification was warranted, and this draft claims zero DONE bytes.",
  "Recovered only from retained owner assembly, approved local GS1 ROM correspondence, local presentation records, and the approved compiler. No GS2 evidence, external source, copied instruction bytes, fixed-register variable, inline assembly, compiler barrier, or compiler modification was used.",
  "2026-08-27: reclassified measured-draft -> proven-retained. This owner's retained assembly is already independently proven unreachable from ordinary C at the manifest/classification level: out/gs1-en/full/asm/manifest.json marks it kind=thumb_multiple_transfer_module, retention starting with keep_, confidence=proven, citing arm_md/arm_c line ranges showing GCC 2.96's Thumb backend can only emit a multi-register stmia paired immediately with a matching ldmia (movmem8b/movmem12b), and that ARM's standalone store_multiple/stmsi patterns are TARGET_ARM only -- a lone stmia from independently-computed register values (as this owner's reference has) is architecturally unreachable on this route, not an unexplored search space. This specific pattern was independently rediscovered by multiple dedicated passes this session (08004838, 0800bc48, 08003e10, 0808fecc, 080c0184, and partially 080e7404) before the shared manifest-level proof was found; recording the status correction here so future sessions don't repeat the rediscovery."
 ]
}
