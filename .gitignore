.DS_Store
assets/**/preview*.png
.cache/
.cmatch*/
toolchain/
toolchains/
scratch/
work/
out/
build/
builds/
dist/
cmatch/
comparisons/
compiler-output/
diffs/
disassembly/
dumps/
m2c/
objdump/
reports/
analysis/
private/
rom/
roms/
*.gba
*.rom
*.bin
!assets/maps/**/metatiles.bin
!assets/maps/**/metatile_attributes.bin
*.elf
*.o
*.a
*.d
*.dmp
*.dump
*.raw
*.map
*.sym
*.lst
*.dis
*.exe
*.dll
*.dylib
*.so
*.ips
*.bps
*.ups
*.bsdiff
*.diff
*.patch
*.xdelta
*.xdelta3
*.sav
*.zip
*.tar
*.tgz
*.gz
*.7z
*.log

# GCC pass dumps are named after the source (for example,
# 08120454.c.12.life) and are emitted into the compiler's working directory.
# Ignore them repository-wide so a diagnostic compile from any directory cannot
# flood Git status. The digit after `.c.` keeps ordinary numbered C sources live.
*.c.[0-9]*.*
tools/scratch/*
!tools/scratch/README.md

# Rust tooling
target/
*.rlib
*.rmeta
*.pdb

# Worktree convenience symlink to the shared ROM set.
/roms

# Adoption's per-overlay lock (see OverlayLock in tools/overlay-adopt). Lives
# beside the tracked assembly it guards, so it must never become committable.
assets/code/*.adopt-lock
