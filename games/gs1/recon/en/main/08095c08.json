{
 "schema_version": 1,
 "namespace": "main",
 "address": "0x08095c08",
 "status": "proven-retained",
 "owner_bytes": 456,
 "executable_owner_bytes": 456,
 "candidate_bytes": 456,
 "differing_halfwords": 7,
 "wrong_instructions": 0,
 "classification": "battle_effect_compiler_scheduling_mismatch",
 "resolved_asm_classification": {
  "kind": "battle_effect_compiler_scheduling_mismatch",
  "retention": "keep_structured_asm",
  "confidence": "proven"
 },
 "cross_edition": {
  "editions": [
   "ja",
   "en",
   "de",
   "es",
   "fr",
   "it"
  ],
  "relocation_normalized_core_bytes": 396,
  "core_identical": true,
  "starts": {
   "ja": "0x0808cbf0",
   "en": "0x08095c08",
   "de": "0x080992a4",
   "es": "0x0809ac94",
   "fr": "0x0809ac94",
   "it": "0x08095c94"
  }
 },
 "domain": "Updates one of the 24 caller-created effect slots through radial movement, a hidden delay, a second radial target, and final cleanup.",
 "expressed": [
  "initial radial target selection from the slot origin",
  "copying the linked source object's two mode bits to the particle object",
  "target-arrival state transitions and the 40-frame callback delay",
  "hiding and restoring the particle before targeting the source position",
  "the state-five cleanup path and both locally evidenced audio cues"
 ],
 "bounded_search": {
  "safe_heat_walks": {
   "runs": 4,
   "requested_candidates_per_run": 5000,
   "attempted_candidates": 20000,
   "compile_failures": 0,
   "exact_candidates": 0,
   "best_differing_halfwords": 11
  },
  "classic_exact_only_heat_walk": {
   "requested_candidates": 10000,
   "attempted_candidates": 10000,
   "compile_failures": 142,
   "exact_candidates": 0,
   "best_differing_halfwords": 7,
   "best_wrong_instructions": 0,
   "best_shape": "a do-while-zero spelling around the origin-X assignment"
  },
  "current_approved_compiler_classic_heat_walks": {
   "runs": 2,
   "seeds": [101, 907],
   "requested_candidates_per_run": 20000,
   "attempted_candidates": 40000,
   "compile_failures": 587,
   "exact_candidates": 0,
   "best_differing_halfwords": 7,
   "verdict": "generic AST mutation axis closed after two independent runs without improvement"
  }
 },
 "compiler_axes": {
  "tied_at_readable_baseline": [
   "routed",
   "gcc296",
   "-fno-schedule-insns",
   "-fno-gcse",
   "-fno-rerun-cse-after-loop",
   "-fno-cse-follow-jumps",
   "-fno-cse-skip-blocks",
   "-fno-sched-interblock",
   "-fno-sched-spec",
   "-fno-sched-spec-load",
   "-fno-sched-spec-load-dangerous",
   "-fno-strength-reduce",
   "-O3"
  ],
  "regressed": [
   "-fno-schedule-insns2",
   "-fno-regmove",
   "-fno-expensive-optimizations",
   "old-agbcc"
  ]
 },
 "residual": [
  {
   "scope": "state-zero transition into linked-object mode-bit copying",
   "candidate": "increments and stores the state byte before copying the source pointer from r9 to r1",
   "reference": "copies the source pointer first, then increments and stores the independent state byte",
   "equivalence": "the register move neither reads nor writes the state pointer or state byte",
   "resynchronization": "both streams have identical registers and memory before loading source->linked_object"
  },
  {
   "scope": "state-two origin capture and object mode-bit clearing",
   "candidate": "loads the object flag before materializing -13 and stores the masked flag before adding 71 to the independent effect pointer",
   "reference": "materializes -13 before the independent object-flag load and forms effect+71 before storing the masked object flag",
   "equivalence": "the swaps exchange only independent register work and an object-byte store with an address-only add; all values and memory writes are identical",
   "resynchronization": "both streams match again before storing 4 to effect->flags"
  }
 ],
  "rejected_experiments": [
  {
   "experiment": "spell the state-two object-flag clear through the already-live object_flags carrier used by the exact state-zero and state-one blocks",
   "result": "GCC emits the same 456-byte candidate and the same seven differing halfwords; the explicit temporary is coalesced before scheduling and does not change either residual hunk"
  },
  {
   "experiment": "sweep GCC 2.96's shipped post-reload scheduler sub-options across both retained family members",
   "result": "enabling and disabling interblock scheduling, speculative scheduling, speculative loads, and dangerous speculative loads is byte-neutral for both owners (7 and 5 differing halfwords); the family mismatch is not controlled by those route flags"
  },
  {
   "experiment": "name an alias of source before incrementing the state byte",
   "result": "the alias is eliminated and the seven-halfword schedule is unchanged"
  },
  {
   "experiment": "preload source->linked_object through a named carrier before incrementing the state byte",
   "result": "moves both the high-register copy and dependent load ahead of the increment, disturbs the low-register roles, and regresses to 11 differing halfwords / 12 wrong instructions"
  }
 ],
 "notes": [
  "The retained ordinary-C reconstruction emits the complete 456-byte owner extent, all calls, branches, registers, memory operations, constants, and literal words. A do-while-zero lifetime around the state-two origin-X capture reproduces the best known seven-halfword schedule; only the two localized scheduling hunks differ.",
  "The seven-halfword shape was independently rediscovered from the recorded classic-search verdict and is now the canonical candidate. No searched ordinary-C source emits the retained order exactly.",
  "Relocation-aware comparison of the independently assembled retained owner proves all 396 unmasked core bytes identical across the six GS1 editions.",
  "The caller at 0x08095dd0 installs this callback on 24 effect slots and later advances those slots to states two and five; the name and domain do not infer an effect identity beyond that local behavior.",
  "Recovered only from retained owner assembly, approved local GS1 ROMs, local callers and effect-slot types, and the approved compiler. No GS2 evidence or external game source was used."
 ]
}
