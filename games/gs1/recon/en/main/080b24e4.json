{
 "schema_version": 1,
 "namespace": "main",
 "address": "0x080b24e4",
 "status": "measured-draft",
 "owner_bytes": 484,
 "candidate_bytes": 476,
 "differing_halfwords": 226,
 "wrong_instructions": 148,
 "classification": "shop-item-purchase-confirm-loop",
 "domain": "Given a list window and item window already created by a caller, checks a fixed item's price against the player's money, then (if affordable) runs a party-member selection loop that confirms or cancels the purchase for the selected unit before deducting the price and exiting.",
 "notes": [
  "Clustered against the byte-exact Shop_SelectPartyMember template by the family scan; the retained assembly's redraw-flag/party-member-list/input-poll loop (the 'if (redraw) {...}' block, the NEW_KEYS&1 confirm / NEW_KEYS&2 cancel checks, and the REPEAT_KEYS 0x20/0x10 selection adjust) is a close structural match to the template, wrapped in an outer price-vs-money affordability gate that has no template counterpart.",
  "Recovered from the retained owner's control flow and its literal pool, which resolves to ADDR_03001F2C (shop pointer), an undocumented u16 field at shop+0x39e (read, temporarily set to 0xe4, and restored on every exit path), the documented party_member_count (0x3a7) and party_member_ids (0x36e) fields, a price table at 0x080b4146 indexed by a signed byte read from address 0x02000240+0x11c, a money field at 0x02000240+0x118, and several undocumented shop-relative u16 fields (0xc9e, 0xca6) plus small non-shop-relative integer constants (0xcc3, 0xcc3+1, 0xca1, 0xcc5) passed directly to Func_080b0574/Func_080b04dc as message/resource ids.",
  "Two concrete respellings measurably improved the match and are kept: (1) reordering the local declarations/initializers to the sequence the reference actually computes them in -- shop pointer, then the shop+0x39e field save, then redraw=1, then the price/money block, then unit_id=0 and selection=0 last, immediately before the affordability check -- which alone dropped wrong_instructions from 174 to 154; (2) introducing an explicit 'u8 *party_state = (u8 *)0x02000240;' local instead of writing the two SHOP_PARTY_STATE-region reads as two independent absolute-address casts. The independent-cast form let the compiler constant-fold each read into its own single absolute pc-relative load (0x0200035c and 0x02000358), which does not match the reference's shape of loading 0x02000240 once and computing both offsets from that one shared base register; the shared-pointer-variable form recovered that shape and dropped wrong_instructions to 152 with candidate_bytes moving from 468 to 472 (owner is 484).",
  "2026-08-30: resolved three behavioral gaps directly from the retained instruction stream and literal pool. The full-bag and alternate failure branches pass message ids 0x0c9e and 0x0ca6, not zero; the success branch deducts the saved item price rather than calling Func_08077028 a second time and negating its availability result; and Func_080b1470 receives (item_window, unit_id, shop_field), not (item_window, shop_field, unit_id). These corrections bring the candidate to 476/484 bytes, 226 differing halfwords, and 148 wrong instructions.",
  "Creating price before saved_field, while retaining the reference's execution order, applies the allocator decoder's named swap_declarations(price,saved_field) repair. It places saved_field at sp+0 and price at sp+4 as in the reference. A persistent named shop-field pointer reaches the reference's 12-byte frame but spills the wrong values and regresses wrong instructions to 158-160, so it was rejected rather than retained for its coincidental size.",
  "The remaining gap is structural, not a last-instruction compiler floor: the reference spills item_window at sp+8, keeps list_window in r9, and emits redraw/input blocks in source order with one trailing pool; the candidate keeps item_window in r9, list_window in sl, and reorders the loop blocks around an early literal pool. Sequential versus else-if input spelling and explicit window aliases are byte-neutral. Further work must recover the missing lifetime or control-flow fact that causes the item-window spill; it should not force the register or scheduler.",
  "Exact-C adoption remains gated on an independently compiled zero-difference object."
 ]
}
