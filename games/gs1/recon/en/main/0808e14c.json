{
  "schema_version": 1,
  "namespace": "main",
  "address": "0x0808e14c",
  "status": "measured-draft",
  "owner_bytes": 240,
  "score": {
    "measured": "2026-08-31",
    "candidate_bytes": 240,
    "reference_bytes": 240,
    "differing_halfwords": 85,
    "wrong_instructions": 86,
    "class": "missing-extra-code",
    "note": "A direct retained-assembly audit corrected the shared facing-word carrier from u16 to s16, matching the reference's sign extension after the 0x800 mask. The exact-size, equal-topology candidate improves from 89 differing halfwords / 100 wrong instructions to 85 / 86. Narrowing dirCheckEnabled to s16 is byte-neutral and documents the same width; splitting the 0xf000 mask into a later compound assignment regresses to 93/88 and was reverted. The remaining pool placement and allocation residual is not yet bounded, so retained assembly stays authoritative and this draft claims zero DONE bytes."
  },
  "notes": [
    "2026-08-31: read games/gs1/asm/0808e14c.s directly and confirmed the reference loads trigger->facing both signed and unsigned: its direction-mask carrier uses signed halfword semantics while matchId uses the unsigned low byte. The source now expresses those two views explicitly.",
    "Source recovered by a prior agent in this session whose full working notes were lost to an interrupted session; only the .c file survived in its worktree.",
    "No other Golden Sun decompilation project, symbol map, or disassembly was consulted by this commit."
  ]
}
