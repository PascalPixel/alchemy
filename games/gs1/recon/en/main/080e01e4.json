{
  "schema_version": 1,
  "namespace": "main",
  "address": "0x080e01e4",
  "status": "measured-draft",
  "owner_bytes": 832,
  "score": {
    "measured": "2026-08-27",
    "candidate_bytes": 828,
    "reference_bytes": 832,
    "differing_halfwords": 298,
    "wrong_instructions": 316,
    "class": "wrong",
    "note": "Third pass: fixed the same 'Func_080072f4 is not a real function' modeling error found and fully derived in games/gs1/recon/en/main/080dc1ec.json's score.note (full derivation there: it is the r4 entry of the _call_via_rN trampoline bundle at games/gs1/asm/080072e4.s, so every `Func_080072f4(dest,src,x,y,callback,w,h)` call was really an indirect call through the function pointer in r4). Rewrote all three call sites in this owner as direct indirect-calls matching DrawRectangleFn's real 6-argument signature -- `callbacks[0](draw_destination, work, x, y, 20, 40)` (orbiting-marker draw), `callbacks[0](draw_destination, (u8*)work+800, rx, ry, 40, 64)` (member-ring draw), and `callback_ptr[1](draw_destination, (u8*)extra_target+Data_080ede48[idx-1], y, h, idx, idx<<1)` (final 512-particle scan) -- and removed the fictional `void Func_080072f4(s32,s32,s32,s32,DrawRectangleFn,s32,s32)` prototype. differing_halfwords 319->298, wrong_instructions 334->316 -- both improved, unlike the mixed result on 080dc1ec/080d82b0/080d59b0 (their dh rose slightly even as wrong_instructions dropped sharply); still not exact. Second pass's note below (renamed) remains otherwise accurate.",
    "prior_note_2026_08_27": "Second pass, evidence-grounded against games/gs1/asm/080e01e4.s rather than a permutation search. Found and fixed a genuine structural bug in the first draft: the reference's labels (.L5/.L6/.L7/.L8/.L9) show the work[0x77A8]=4 write and the Func_080d6888/Func_080b5088 member_id loop are NOT run once per outer 96-pass frame -- they are nested inside the innermost 'ring height > 95' burst conditional, i.e. they only run on a pass that also just burst 32 fresh particles. Moving that block from after the 8-member do-while to the tail of the burst-if dropped the candidate size from 840 back toward 832 and, as a side effect, moved `work` off a stray `sl` allocation onto the same `r9` the reference uses for the whole function (confirmed byte-identical at every `add rX, r9` / `mov r1, r9` site). Also fixed: the prologue now reads work/draw_destination through a heap_cache+cursor pair (heap_cache = (void**)0x03001EEC; cursor = heap_cache; work = *cursor++; draw_destination = *cursor; extra_target = heap_cache[2];) matching the member_orbit/run.c idiom exactly (byte-identical ldmia sequence); the id argument to all three Func_080e0524 calls is now (s32)&Value_00000073/00000090/00000089 rather than a plain int literal -- the reference loads all three ids from the literal pool (never `movs r0,#imm`), matching the already-adopted Value_000000b4 idiom in puff_arc/run.c, and all three calls are now byte-identical; the member-ring draw call (the second Func_080072f4 site) uses callbacks[0], not callbacks[1] -- both the orbiting-marker and the member-ring draws share the same first blit routine, only the final 512-particle scan uses the second one; and the innermost 32-particle burst loop now uses its own `burst` counter instead of reusing the outer `i`, which let the reference's separate r4-counter idiom stop colliding with `member`'s live range (differing_halfwords 341->319, candidate size 840->836)."
  },
  "family": {
    "assigned_template_owner": "main:08099160",
    "assigned_template_source": "games/gs1/src/battle/effects/orbiting_particles/run.c",
    "template_note": "Same mismatch as the other three owners in this assignment: no callees shared with RunOrbitingParticleEffect, and a control-flow shape (fixed 96-pass outer loop, an 8-member inner loop gated by both a per-member activation threshold and a hard cap of 8, then a 512-slot shared particle scan every pass) that does not resemble the template's single 15-iteration spawn loop at all.",
    "actual_sibling_evidence": "Confirmed member of the 0x03001eec 'battle work' subsystem family documented in games/gs1/recon/en/main/080e7404.json, 080d59b0.json, 080d82b0.json and 080dc1ec.json: identical Func_080cd594(mode)/Func_080e0524(id,target,f,f)/Func_080041d8(0x080CD261,0x480)/Func_08004278(0x080CD261)/Func_08002dd8(id)/Func_080cdbc0() shape, the same *(void**)0x03001EEC/0x03001EF0/0x03001EF4 work/draw_destination/extra_target globals as 080d82b0, and the same Func_080cef64(flag, out_pair) call as 080dc1ec. Per the already-adopted games/gs1/src/unidentified/main/runtime/runtime_owner_21.c, this is not dynamic callback resolution: Func_080cef64 unconditionally copies Data_03001e50[46] then Data_03001e50[47] (the kind-46/47 allocator-cache slot values, already populated by earlier Func_080ed408 calls) into out_pair[0]/out_pair[1]; flag only selects which Func_080ed408 load parameters populated those slots beforehand. This owner's callbacks[2] array is correctly sized (2 words) and both entries are genuinely read back downstream as draw callbacks -- unlike 080dc1ec's single-word `callback` local, which undersized the same two-word write by one word until corrected. This confirms the shared family header should model Func_080cef64 as a two-word allocator-cache-slot copy, not a callback-table resolver."
  },
  "rejected_experiments": [
    "A leftover dead-code assignment (`member_id_offset = 36 + member * 2` inside the wrong loop, a copy-paste artifact from re-deriving the member_id_offset stepping used by the second, count-driven loop) was caught and removed before the first scoring pass since it did not correspond to any instruction in the reference and only accidentally left the score unchanged when present.",
    "Tried modeling the second Func_080072f4 site (member-ring draw) with `callbacks[1]`, matching the final 512-particle-scan site: this looked plausible (two distinct blit routines, one per draw 'kind') but the reference's own instruction stream contradicts it -- both the orbiting-marker draw and the member-ring draw load their 5th argument from the SAME stack slot (`sp+32` in the reference's 40-byte frame) that holds `callbacks[0]`; only the 512-particle scan differs, and it reaches `callbacks[1]` through a saved pointer (`ldr r0,[sp,#20]; ldr r4,[r0,#4]`) rather than a second direct array read. Reproducing that split -- `callbacks[0]` at both early sites, `callback_ptr[1]` (a retained `DrawRectangleFn *callback_ptr = callbacks;` passed to Func_080cef64 and re-dereferenced later) at the last one -- is what makes all three Func_080072f4 5th-argument loads match in shape.",
    "Tried leaving `work`/`draw_destination`/`extra_target` as three independent `*(void**)literal` reads (the original shape): the reference's actual prologue is a `heap_cache = (void**)0x03001EEC; cursor = heap_cache; work = *cursor++; draw_destination = *cursor;` pair (byte-identical `ldmia r3!,{r1}` sequence) with `extra_target` read separately off the retained `heap_cache` base (`heap_cache[2]`, `ldr r2,[r2,#8]`) rather than its own literal pool entry. The three-independent-loads shape cannot produce the reference's `ldmia`/register-offset instructions no matter what register `work` lands in.",
    "Tried a plain `for (i = 0; i != 32; i++)` reusing the outer-scope `i` for the innermost particle-burst loop (nested inside the 8-member ring loop): this makes the compiler treat `i`'s pseudo as spanning from the top-level ring-seed loop all the way to the bottom-level 512-particle scan, which collides with `member`'s live range across the burst and forces extra spills. Giving the burst loop its own block-scoped `burst` counter (never reused elsewhere) removed that collision and measurably improved the score (differing_halfwords 341->319, candidate size 840->836); the reference's own burst-loop counter is a low register (r4) entirely separate from the sl-based counter it reuses for the ring-seed/member/final-scan loops, which is consistent with treating it as its own variable rather than a shared `i`.",
    "Tried moving `target_slot`/`member_id_offset` from function scope into the target_slot-loop's own block (matching the pattern that helped `burst`): this had zero effect on the score, confirming that block *scoping* alone does not change this compiler's register allocation -- what mattered for `burst` was giving it a genuinely distinct variable identity, not narrower lexical scope."
  ],
  "notes": [
    "Recovered only from the retained owner assembly (games/gs1/asm/080e01e4.s), the approved local EN ROM, games/gs1/recon/en/main/080e7404.c, 080d59b0.c, 080d82b0.c, 080dc1ec.c and their dossiers, in-repository headers, and the approved compiler.",
    "This candidate claims zero DONE bytes.",
    "Semantic summary: the most elaborate of the four sub-effects. Sets hardware alpha-blend register 0x04000052, resolves a pair of draw callbacks via Func_080cef64, opens three effect registrations via Func_080e0524 (against extra_target, work, and work+0x320), seeds a 32-slot ring at work+0x7080 with randomized radii/heights and a 512-slot shared particle pool's lifetime sentinel, then runs 96 animation passes. Each pass draws one orbiting marker at a fp-derived sine/cosine angle, then walks up to 8 fixed member slots, drawing and advancing each ring entry once its per-slot activation threshold (`fp >= member*4+8`) is crossed. IMPORTANT CORRECTION from the first draft: once a given ring entry's height crosses 95 and a burst of 32 fresh particles is spawned into that member's private sub-range, the SAME pass -- gated behind that burst, not once per outer frame -- also stamps `work[0x77A8]=4` and, if the target has any real party members, cues Func_080d6888/Func_080b5088 for every one of them from the target's own count field (confirmed from the reference's own labels: the `.L5` convergence point that all three inner guards funnel to sits AFTER this block, and the block itself is only reachable by falling out of the burst loop). Finally the pass scans all 512 shared particle slots, drawing and aging every slot whose lifetime sentinel is not -1.",
    "The member-activation gate (`fp >= member * 4 + 8`) and the hard 8-member cap on the ring/particle-burst loop (independent of the target's real member count, which only gates the separate Func_080d6888/Func_080b5088 loop) are both taken directly from the reference's literal comparisons rather than simplified, since the point is reproducing the compiled shape.",
    "Three of the four open questions carried over from the other owners in this family are now resolved for this owner specifically and worth propagating: (1) the Func_080e0524 id argument is `(s32)&Value_XXXXXXXX`, not a plain int literal, at every call site in this owner (matches the already-adopted Value_000000b4 idiom in puff_arc/run.c) -- 080d59b0/080d82b0/080dc1ec's still-open Func_080e0524 calls should be checked against the same idiom; (2) the work/draw_destination/extra_target prologue is a heap_cache+cursor pair, not three independent absolute reads, whenever all three globals are read together -- 080d82b0's identical three-global prologue is the most likely other beneficiary; (3) `DrawRectangleFn callbacks[2]`-style locals are read through a retained pointer variable (`callback_ptr[1]`), not a second direct array index, for whichever call site is compiled latest/deepest in the function -- seen here and structurally likely in 080dc1ec's `callback_pair[2]`.",
    "Two open questions are NOT resolved and remain genuinely stuck after real attempts, not low-confidence guessing: (a) the observed 5th-argument-in-r4 calling convention at every Func_080072f4 site (dest/src/x/y in r0-r3, callback in r4, w/h on the stack) -- this owner's candidate instead spills all of callback/w/h to the stack (a plain 4-register-then-stack ABI), and no C-level reshaping tried (argument order, a wrapper local, block scoping) changed that; this looks like it needs either a compiler-level answer or a source trick none of the four sibling agents have found yet. (b) A persistent one-word (4-byte) stack-frame overshoot (`sub sp,#44` vs the reference's `#40`): the reference keeps its outer 96-pass loop counter in the real `fp`/r11 register for the whole function and reuses `sl` for the (disjoint-lifetime) ring-seed/member/final-scan counters; this candidate's compiler instead puts the 8-member loop's `member` counter in `fp` and spills the outer counter to a stack slot. Declaring `member` before/after `fp`, block-scoping the target_slot loop's locals, and reordering the loop tail statements (member++ before member_offset before ring+=28, matching the reference's exact order) were all tried and had no effect on this tie-break; how the second Func_080072f4 argument (src) should be typed once a shared work-block header exists remains open too -- here it alternates between a plain work-relative offset (`work`, `work+800`) and an extra_target-relative table lookup (`extra_target + Data_080ede48[idx-1]`), exactly mirroring the split already seen in 080d59b0/080d82b0/080dc1ec.",
    "No GS2 or external source was used; no other Golden Sun decompilation project was consulted."
  ]
}
