{
 "schema_version": 1,
 "namespace": "main",
 "address": "0x08091890",
 "status": "measured-draft",
 "owner_bytes": 328,
 "score": {
  "measured": "2026-08-27",
  "candidate_bytes": 324,
  "reference_bytes": 328,
  "differing_halfwords": 108,
  "wrong_instructions": 60,
  "class": "wrong",
  "command": "compiler candidate-show games/gs1/recon/en/main/08091890.c --owner 0x08091890 --size 328",
  "note": "Family sibling of the just-adopted owner/refresh_ratios_on_flag.c (template main:08078144), but the retained assembly's field-offset identity (0x14/0x16/0x34/0x36/0x38/0x3A -- the exact OwnerRatioState layout) is closer to the already-adopted owner/refresh_active_ratios.c (main:0807808c, which does the same *(0x38)=*(0x34); *(0x3A)=*(0x36); double self-copy). The function is arg0 = character id: it calls Func_08077168(id) and Event_ClearInvalidPackedValues() (both void statements), fetches obj=OwnerState_GetFar(id), does the double self-copy + both ratio-pair recomputations (matching refresh_active_ratios.c's proven idiom exactly, including the *(s16*)(obj+0x3A)<<14 inline-read for the second ratio rather than a named variable), clears obj->0x131, then counts (via Func_08077148 -- the far-call veneer, NOT the Party_CountActiveOwners/Func_080795fc near route, per the precedent warning already recorded in 080b6a60.json) how many of the party's active_owners (Data_02000240.active_owners, offset 0x1F8) have a nonzero 0x38 field; if none do, it forces the party's current_owner (Data_02000240.current_owner, offset 0x1F4) object's 0x38 field to 1 and recomputes its ratio pair the same way, discovered by writing v38=1 as a named local (not a bare literal store) so the compiler forwards the register instead of re-deriving a shared 0x4000 constant -- this specific respelling took wrong_instructions from 149 (raw template copy) to 70 (struct layout + offsets fixed) to 60 (this v38-forwarding fix)."
 },
 "remaining": [
  {
   "op": "register color divergence",
   "count": 60,
   "where": "the double-copy ratio-pair block immediately after obj=OwnerState_GetFar(id) reuses r5 (dead after the id argument's last use) for the clamp temp/v14 instead of the reference's r3, and a symmetric ~4-byte gap around the flagCount=0 initializer (candidate emits movs r2,#0; mov r8,r2 where the reference reuses the already-zero r3 directly via mov r8,r3) -- both are pure GCC 2.96 register-allocator coloring differences, not content differences: the same idiom, byte-for-byte, already compiles to r3 in refresh_active_ratios.c's own adopted context (out/gs1-en/claimed/obj/0807808c.s), so the divergence is driven by this function's surrounding call/live-range shape, not the block's own source shape"
  }
 ],
 "rejected_experiments": [
  {
   "experiment": "complete shipped-toggle route probe, followed by a field-cluster check across 08090658, 080912b8, 08091890, 08091a58, 08093874, 08093fa0, 08094154, and 080941e0",
   "result": "-fno-regmove, both scheduling toggles, both CSE toggles, and -fno-gcse are flat at 324/108; -fno-strength-reduce regresses to 336/166. -fno-expensive-optimizations alone improves this owner to exact extent 328/328 and 104 differing halfwords, but every combination with the other flat toggles stays 328/104. Across the eight-owner field cluster, the flag improves only 08091890; all seven siblings are flat or regress. This is an owner-specific coincidence rather than a multi-owner translation-unit route, so no override was registered and canonical flags remain authoritative."
  },
  {
   "experiment": "split the reused obj pointer into a block-scoped member pointer at the active-owner loop, following the decoder's split-lifetime hint",
   "result": "strong regression to 308 candidate bytes, 162 differing halfwords, and 96 wrong instructions; reverted. The annotation-free permuter cannot apply this repair automatically because obj has multiple assignments."
  },
  {
   "experiment": "obj->0x38 = 1; then v38 = *(s16*)(obj+0x38); (read-back through memory) for the current_owner block's forced-full ratio",
   "result": "regresses to 70 wrong instructions -- the compiler proves the just-stored literal 1 is a compile-time constant and CSEs the resulting 1<<14 with the unrelated 0x4000 clamp-ceiling constant used elsewhere in the function, producing a 128<<7 encoding instead of the reference's 1<<14 register-forwarding encoding"
  },
  {
   "experiment": "*(s8*)(obj+0x131) = flagCount = 0; (chained assignment, to force flagCount and the byte-store literal to share one register)",
   "result": "regresses sharply to 138 wrong instructions -- the chained-assignment form changes codegen well beyond the targeted store, not just a local register choice"
  },
  {
   "experiment": "flagCount = 0; *(s8*)(obj+0x131) = 0; (swap statement order, plain literal both sides)",
   "result": "62 wrong instructions, slightly worse than the current 60"
  },
  {
   "experiment": "flagCount = 0; *(s8*)(obj+0x131) = flagCount; (read flagCount back for the store instead of a fresh literal)",
   "result": "72 wrong instructions / 110 differing halfwords -- worse"
  },
  {
   "experiment": "reordering local variable declarations (moving count/index/left/flagCount to the end of the decl list, after t/v14/v16/v34/v36/v38/v3A/one)",
   "result": "byte-identical output -- this GCC 2.96 build's pseudo-register numbering follows first-use order in the generated RTL, not textual declaration order, so decl reordering has no effect"
  },
  {
   "experiment": "wrapping *(s8*)(obj+0x131)=0; in a do{...}while(0) block (matching the do-while(0) idiom that was load-bearing for the double-copy block)",
   "result": "byte-identical output -- no effect here"
  }
 ],
 "notes": [
  "2026-08-31: changed v34 from s32 to s16 to match the retained ldrsh access and the already-exact Owner_RefreshActiveRatios sibling. This is byte-neutral at 324/108/60 and therefore improves semantic typing without being presented as a score gain. v38=s16 was also tested and was byte-neutral, but reverted because it is a computed carrier rather than a stored-field declaration.",
  "Recovered only from the retained owner assembly (games/gs1/asm/08091890.s), the approved local ROM, the assigned template games/gs1/src/owner/refresh_ratios_on_flag.c, the closer-matching already-adopted sibling games/gs1/src/owner/refresh_active_ratios.c and its compiled object out/gs1-en/claimed/obj/0807808c.s (used to confirm the double-copy idiom's proven register behavior in isolation), games/gs1/include/owner_state.h and party_state.h (OwnerState_GetFar=Func_08077008, Data_02000240.active_owners at 0x1F8, Data_02000240.current_owner at 0x1F4), games/gs1/include/types.h (FixedPoint_Ratio=Func_080022ec, Event_ClearInvalidPackedValues=Func_08091858), the sibling dossier games/gs1/recon/en/main/080b6a60.json (which documents the Func_08077148 far-call-veneer-vs-Party_CountActiveOwners distinction reused here), and the approved compiler via tools/compiler candidate-show.",
  "Func_08077168's return value and Event_ClearInvalidPackedValues's argument are both unobserved in the retained assembly (no register setup between the two `bl`s, and r0 is explicitly reloaded with id before the next call), so both are called as bare statements with their real signatures left undetermined beyond the existing s32 Func_08077168(s32) declaration used elsewhere in the tree.",
  "This looks like a source-shape gap only in the narrow sense that the *content* is fully understood and expressed; the residual is a compiler-internals register-coloring quirk (proven by the same idiom compiling differently in this function's context vs. refresh_active_ratios.c's), which needs permuter-style search across GCC scheduling/allocation flags rather than further manual respelling.",
  "This candidate claims zero DONE bytes."
 ]
}
