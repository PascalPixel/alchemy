{
 "schema_version": 1,
 "namespace": "main",
 "address": "0x08026080",
 "status": "mapped",
 "measured": "2026-08-29",
 "map": {
  "head": "0x08026080",
  "head_symbol": "Region_08026080",
  "head_file": "games/gs1/asm/08026080.s",
  "head_prologue": "push {r5,r6,r7,lr}; mov r7,r11; mov r6,r10; mov r5,r9; push {r5,r6,r7}; mov r7,r8; push {r7}; sub sp, #324 -- full r8-r11 save, 324-byte frame. Note: this head is tagged with the call-graph-reconstructed comment rather than an explicit FunctionHead_ symbol/global, but its genuine push+high-reg-save+sub-sp shape is the real entry.",
  "true_epilogue": {
   "file": "games/gs1/asm/08026b44.s",
   "note": "Ends `... movs r0,#1 / bl sub_080030f8 / ldr r0,[sp,#68] / add sp, #324 / pop {r3,r5,r6,r7} / mov r8,r3 / mov r9,r5 / mov r10,r6 / mov r11,r7 / pop {r5,r6,r7} / pop {r1} / bx r1`. Exact mirror of the head's 324-byte frame and four-high-register save; uses r1 (not r0) as the return-veneer register, but the frame teardown shape is identical. This is the sole real epilogue."
  },
  "span": {
   "start": "0x08026080",
   "end_exclusive": "0x08027114",
   "size_bytes": 4244,
   "derivation": "end = start of next asm file games/gs1/asm/08027114.s, immediately following 08026b44.s which owns the epilogue (its trailing data is annotated '08026e72断片を所有者へ併合。プール尾部の資料語。' -- a merged fragment's pool tail retained in the same file). size = 0x08027114 - 0x08026080 = 0x1094 = 4244."
  },
  "member_regions_in_order": [
   {"addr": "0x08026080", "symbol": "Region_08026080", "file": "games/gs1/asm/08026080.s", "role": "real prologue + setup, call-graph-reconstructed region tag despite being the true head"},
   {"addr": "0x0802691c", "symbol": "Continuation_0802691c", "file": "games/gs1/asm/0802691c.s", "role": "branch continuation, header comment: '複数領域関数の分岐継続部' (multi-region function's branch-continuation part), in-file literal pool"},
   {"addr": "0x08026b44", "symbol": "Region_08026b44", "file": "games/gs1/asm/08026b44.s", "role": "converging tail ending in the real, shared epilogue; trailing bytes are an explicitly annotated merged-in fragment pool tail"}
  ]
 },
 "domain_evidence": [
  "0802691c's own header comment explicitly labels it '複数領域関数の分岐継続部' (branch-continuation part of a multi-region function) -- direct textual confirmation this belongs to the same function as its head.",
  "08026b44.s carries an internal comment '08026e72断片を所有者へ併合。プール尾部の資料語。' (fragment at 08026e72 merged into the owner; pool-tail reference words) noting the project has already partially merged a sub-fragment's literal pool into this file -- consistent with this being the terminal region of a multi-part function rather than a standalone owner.",
  "sub_08017a64 and sub_080030f8 calls inside 08026b44's body/tail are plain forward calls to already-registered owners elsewhere in the corpus, not further fragment veneers -- no evidence of additional cluster members beyond the three listed."
 ],
 "scoring_command": "out/cargo-target/release/compiler candidate-show games/gs1/recon/en/main/08026080.c --owner 08026080 --size 4244",
 "notes": [
  "Map only, per task scope -- no C source drafted, no translation-units.json edited.",
  "Head file uses the 'call-graph reconstructed, code+data mixed' comment style rather than an explicit FunctionHead_ label; classified as the true head purely from its genuine push/high-reg-save/sub-sp prologue shape and the absence of any earlier candidate branching into it (grep across the corpus for '08026080' finds no external references)."
 ]
}
