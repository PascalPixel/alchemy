{
  "schema_version": 1,
  "namespace": "main",
  "address": "0x080eb754",
  "status": "measured-draft",
  "owner_bytes": 2444,
  "candidate_bytes": 2292,
  "differing_halfwords": 1193,
  "wrong_instructions": 1101,
  "classification": "structural-topology",
  "resolved_asm_classification": {
    "kind": "compiler_output",
    "retention": "c_candidate",
    "confidence": "likely"
  },
  "notes": [
    "2026-08-31: corrected the two nine-entry ROM displacement tables at 0x080eef56 and 0x080eef5f from u8 to s8, matching their use as signed screen-coordinate offsets in both projection loops. Also made the existing stack-array end comparison type-explicit; that cast is emission-neutral.",
    "The m2c draft had collapsed three stack aggregates at sp+0x64/sp+0x94/sp+0xa0 into four-byte scalars and split the eight-byte position pair at sp+0x5c into two independent assignments, allowing GCC to delete the unused second word. Restoring their evidenced 0x30/0x0c/0x10 and 8-byte extents, reversing the mechanically generated local declarations to match GCC 2.96's stack-slot order, and expressing the pair as one aggregate copy recovers the reference's exact 176-byte frame and exact key slots (work@sp+0x50, object@sp+0x4c, canvas@sp+0x44). Candidate grows from 2264 to 2292 of 2444 bytes; differing halfwords improve 1194 to 1193. Raw wrong instructions rise 1067 to 1101 because the newly restored frame changes downstream register allocation, but this is a structural correction backed directly by stack offsets, not a raw-score guess.",
    "2026-08-30: promoted the second-largest no-candidate main-ROM owner after repairing the aggregate-aware m2c draft's concrete invalid type expressions. A fresh canonical compile produces 2264 bytes for the 2444-byte reference; topology differs and 1194 halfwords differ, so this remains a measured draft and claims zero DONE bytes.",
    "Repairs were mechanical: resolve the work pointer at 0x03001eec, use typed GBA register halfword stores, add typed u8/u16 ROM-table loads, and model member-id reads as signed halfwords at dynamic offsets. No instruction bytes, inline assembly, fixed-register variables, barriers, or external decompilation sources were used.",
    "The function is a large battle-presentation routine with a 0x03001eec work block, 64-entry and 320-entry particle pools, palette and window effects, member-triggered emissions, and a 96-frame main loop. A permanent name is deferred until caller and dispatch evidence identifies the presentation."
  ]
}
