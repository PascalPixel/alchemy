{
  "schema_version": 1,
  "namespace": "main",
  "address": "0x0801fd34",
  "status": "proven-retained",
  "classification": "compiler_local_scheduling_floor_module",
  "owner_bytes": 80,
  "retention": "c_candidate",
  "domain": "Build four animated palette entries from a fixed-point phase.",
  "provenance": "Drafted from the retained owner assembly and the repository's aggregate-aware m2c wave output; no external source was used.",
  "score": {
    "measured": "2026-08-30",
    "candidate_bytes": 80,
    "reference_bytes": 80,
    "differing_halfwords": 4,
    "wrong_instructions": 0,
    "class": "scheduling-floor",
    "compiler_flags": [
      "-fno-schedule-insns2"
    ],
    "note": "The shipped scheduler flag removes the canonical route's loop-tail reordering and reduces the residual from ten to four halfwords. The remaining two independent ordering pairs preserve every instruction and register role; ordinary C and the approved scheduler routes cannot request their reference order."
  },
  "rejected": [
    {
      "shape": "split the phase load and index-offset calculation into named temporaries",
      "result": "preserved the 80-byte extent but swapped the r2/r3 roles and regressed from four to seven differing halfwords"
    },
    {
      "shape": "reuse the later packed-color local as the pre-call phase carrier",
      "result": "preserved the 80-byte extent but changed the whole packed-color register family and regressed to fourteen differing halfwords"
    },
    {
      "shape": "distribute the 0x300 multiplication across the phase and index terms",
      "result": "grew to 100 bytes and changed loop topology; reverted"
    },
    {
      "shape": "annotation-free finite permuter under the canonical route",
      "result": "allocator decoder reported no unique source shape, so no unbounded search was started"
    }
  ]
}
