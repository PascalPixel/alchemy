[workspace]

[package]
name = "early-runtime-data"
version = "0.1.0"
edition = "2021"

[lib]
name = "early_runtime_data"
path = "src/lib.rs"

[dependencies]
canonical-json = { path = "../canonical-json" }
import-asset = { path = "../import-asset" }
serde_json = { version = "1", features = ["preserve_order"] }
