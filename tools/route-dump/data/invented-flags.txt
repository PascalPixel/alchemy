# Compiler options this fork invented. gcc 2.96 has none of them.
#
# EMPTY as of 2026-08-16, and it must stay that way.
#
# HARD RULE: never add to this file. A byte match reached by inventing a
# compiler option is not a reconstruction, it is a switch that hides one.
# pret/pokeemerald builds Pokemon Emerald with ZERO invented options: its
# per-file overrides are compiler SELECTION (old_agbcc, agbcc_arm) and
# stock flags (-O, -O2, -mthumb-interwork, -ffreestanding).
#
# 120 entries were removed at once when routing was cut to the pret shape.
# The modes still exist in alchemy-gcc; nothing routes them. Each is now
# either dead code to delete, or a real Camelot behaviour to make
# unconditional with no option attached.
