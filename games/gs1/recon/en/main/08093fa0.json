{
  "schema_version": 1,
  "namespace": "main",
  "address": "0x08093fa0",
  "status": "measured-draft",
  "owner_bytes": 436,
  "candidate_bytes": 428,
  "differing_halfwords": 181,
  "wrong_instructions": 93,
  "family": {
    "template_guess": "battle/effects/orbiting_particles/run.c",
    "template_guess_score": 7383,
    "template_guess_confidence": "low",
    "template_guess_verdict": "rejected -- shape-only prologue match, unrelated actual semantics; no shared callee, field offset, or constant with this owner",
    "real_sibling": "main:08093e28 (games/gs1/recon/en/main/08093e28.c, itself an unregistered measured-draft, dossier classification 'grid-effect-placement')",
    "real_sibling_basis": "same object accessor (Object_GetById(Data_02000240.object_id)), same Data_02000240.mode_1f2 one-shot flag idiom, same 8+ (x>>16 & 0xFFF0)/(z>>16 & 0xFFF0) tile-snap-with-negative-guard arithmetic, same 128-wide-row two-table kind-byte comparison (this owner compares Data_02010000 against Data_0200fe00, i.e. the table 0x200 BEFORE 0x02010000, where 08093e28 compares Data_02010000 against Data_02010200, the table 0x200 AFTER -- consistent with a small family of sibling owners each comparing an adjacent parallel tile layer), same Func_08009220(position*) placement probe, same ObjectMotion_SetPositionAndCommit/CommitCurrentPositionAndActivate reposition pair, and near-identical address (0x08093fa0 is 0x178 bytes after 0x08093e28)."
  },
  "domain": "Reads the active battle-effect object (Object_GetById(Data_02000240.object_id)), snaps its ground-plane x/z to an 8px-centered 16px grid cell, and toggles a one-shot state (Data_02000240.mode_1f2) between two behaviors: when inactive, it verifies the destination cell's kind byte agrees between two parallel 128-wide tile-attribute tables (0x02010000 vs 0x0200fe00) and that a placement probe (Func_08009220) accepts the object's exact current position, then re-anchors the object at the snapped grid cell, runs a short dispatch/wait animation sequence (commands 6, 7, 12) with an upward velocity_y ramp, and marks the flag active; when already active, it repositions the object via a direct 4-argument set-position call (Func_08009150) offset by +0x80000 on y and -0x100000 on z, commits/activates the position, snaps terrain_height to the current y, and clears the flag.",
  "notes": [
    "Every call target, struct field offset, and shared global was independently traced from games/gs1/asm/08093fa0.s by hand (PC-relative literal-pool arithmetic, effective_addr = align4(instr_offset+4)+imm) before any tooling was run, then cross-checked against games/gs1/include/object_runtime.h (x/y/z/terrain_height/velocity_y/speed_limit/animation/animation_kind/flags/action_flags all match struct ObjectRuntime's real offsets exactly) and games/gs1/include/battle_effect_runtime.h (mode_1f2 at 0x1f2, object_id at 0x1f4 of struct BattleWork Data_02000240 match exactly -- 0x02000434 in this owner's literal pool is simply &Data_02000240.object_id).",
    "Object_GetById, BattleRuntime_Reset (Func_080916b0), ObjectMotion_SetPositionAndCommit (Func_08092158), ObjectMotion_CommitCurrentPositionAndActivate (Func_080923c4), and BattleRuntime_WaitIfModeZero (Func_0809163c) are already-adopted exact sources in games/gs1/src/; this owner is a genuine caller of all five. Func_08009080 is a long-call veneer (games/gs1/asm/08009080.s) to 0x0800c300 = already-adopted object/dispatch/apply_argument_to_children.c (ObjectDispatch_ApplyArgumentToChildren); Func_080091e0 is a veneer to 0x0800c528 = already-adopted-but-unnamed games/gs1/src/unidentified/main/runtime/runtime_owner_17.c, whose body (`if ((arg0[0x54]&0xf)==1) (*(u8**)(arg0+0x50))[0x26]=arg1;`) is the exact write-back counterpart of this owner's own read of the same object+0x50->+0x26 byte, confirming the read/modify/write relationship. Func_080030f8 and Func_08009150/Func_08009220 remain unadopted (their games/gs1/asm/*.s retained files still exist); WaitFrames(s32) is the project-wide established call convention for Func_080030f8 (used with an explicit frame-count argument in dozens of other adopted files, e.g. games/gs1/src/battle/run_simple_presentation.c, battle/motion/initialize_actor_records.c), even though one already-adopted zero-argument call site (battle/runtime/wait_if_mode_zero.c) declares it as WaitFrames(void) -- both are correct for their own call site's compiled bytes, which this project already tolerates.",
    "Ran the m2c structural workbench (games/gs1/recon/en/main/08093fa0.c registered as source-paths.json main:08093fa0 -> unidentified/main/battle/battle_owner_69.c, with a matching '.global battle_owner_69' alias added to games/gs1/asm/08093fa0.s immediately above '.global Func_08093fa0'). m2c's independently-recovered control flow, field offsets, and call targets match this hand-trace exactly with zero discrepancies -- strong independent corroboration of the semantic reconstruction. objdiff reports 79.9% instruction match (207 instructions, 109 unchanged, 63 argument-only mismatches, 2 opcode mismatches, 1 replacement, 18 deletions, 16 insertions) against the pre-goto draft.",
    "Residual is register allocation and cold-path block layout, not algorithm, control flow, types, or call arguments. The clearest concrete example: at the tile-kind mismatch check, the reference compiles the guard as `beq.n <success>; b.n <shared fail tail>` (2 instructions, 4 bytes) while every source spelling tried here (mismatch-first early-return, match-first with else, explicit `goto fail;` to a single shared label) compiles to the shorter, logically-equivalent `bne.n <shared fail tail>` (1 instruction, 2 bytes) -- both reach the same single shared fail tail (confirmed: both the tile-mismatch check and the Func_08009220 probe-failure check jump to the identical tail address), so the tail-sharing itself is correctly reproduced, but the reference's own choice to emit a redundant extra branch here (rather than the shorter direct form GCC otherwise prefers) was not reproducible by any legal source restructuring tried. This 2-byte gap, replicated at a small number of similar sites, accounts for the 8-byte (428 vs 436) size shortfall. This matches CONTRIBUTING.md's documented class of diffs with 'no source-level explanation in some cases' (declaration order, alias sets, scheduling, and cross-jumping changing RTL independent of source shape), and is the same unresolved class already recorded on the closest sibling 08093e28.json (113/188 halfwords wrong there despite equal-length output).",
    "Experiments tried on the residual, all logged: (1) polarity of both the tile-mismatch guard and its equivalent structuring (mismatch-first-return vs match-first-with-else vs goto-to-shared-label) -- goto measurably reduced differing_halfwords from 183 to 181 versus plain match-first-with-else, and matches or beats every other polarity tried; kept goto. (2) moving the `variant = 1` declaration earlier (before grid_x/grid_z) to mirror the reference's apparent evaluation order -- measurable but small effect, kept. (3) inlining `variant &= 0xfe` / `variant |= 1` directly as call arguments instead of a separate assignment statement -- not tried further after m2c's own recovery showed no distinct source-level need (the register writeback appears to be the compiler's own choice regardless of C spelling, consistent with the local-alloc tie-break class in note above). Did not force register assignment, use inline asm, or add unevidenced compiler flags.",
    "Recovered only from retained GS1 assembly (games/gs1/asm/08093fa0.s), the already-adopted exact sources named above, the closest measured-draft sibling 08093e28.c/.json in this repository, the project's own object_runtime.h/battle_effect_runtime.h headers, the approved compiler's own candidate-show/workbench output, and the m2c structural workbench run against this repository's own retained assembly and type context. No GS2 evidence, other decompilation project, external source, copied instruction bytes, fixed-register variable, inline assembly, compiler barrier, or compiler modification was used.",
    "Adoption gate not met: differing_halfwords=181, not 0, and candidate_bytes=428 != owner_bytes=436. Registered games/gs1/source-paths.json main:08093fa0 -> unidentified/main/battle/battle_owner_69.c (matching this project's established convention for a definitely-battle/object-subsystem owner whose exact semantic role -- most plausibly a submerge/emerge or grid-anchor toggle for a battle-effect object -- is not yet confidently nameable) and added the required '.global battle_owner_69' alias to games/gs1/asm/08093fa0.s; no games/gs1/src/ file was created since the draft is not exact."
  ]
}
