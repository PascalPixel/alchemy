{
  "schema_version": 1,
  "namespace": "main",
  "address": "0x080912b8",
  "status": "measured-draft",
  "owner_bytes": 474,
  "score": {
    "measured": "2026-08-31",
    "candidate_bytes": 442,
    "reference_bytes": 474,
    "differing_halfwords": 224,
    "wrong_instructions": 202,
    "class": "missing-extra-code",
    "command": "compiler candidate-show games/gs1/recon/en/main/080912b8.c --owner 0x080912b8 --size 474",
    "note": "The corpus drafting wave classified the generated source as type_width_mismatch. The retained assembly proves that each 12-byte display record is first initialized with word stores at +4 and +8, then updated through byte fields at +4/+5/+9 and halfword fields at +6/+8. Encoding those widths removes all three width fingerprints and reduces wrong instructions from 210 to 202; the remaining 32-byte size deficit is now honestly routed as missing_extra_code."
  },
  "remaining": [
    {
      "op": "reconstruct missing or extra code",
      "count": 202,
      "where": "The branch topology is equal, but the candidate is 442 bytes versus the 474-byte owner. The leading divergence shows absent lifetime-preserving moves and an 8-byte stack-frame deficit; later literal-pool placement also differs. Reconstruct the missing source statements before allocator work."
    }
  ],
  "rejected_experiments": [
    {
      "experiment": "Extract both display-record initializers into a typed helper and use a uniform semantic record abstraction.",
      "result": "Regressed to 264 bytes with different topology because GCC 2.96 inlined and rescheduled the helper across the owner."
    },
    {
      "experiment": "Rewrite the whole function as clean direct-pointer C while preserving the recovered behavior.",
      "result": "Produced 454 bytes but changed block topology; the generated control-flow skeleton is currently the stronger reconstruction basis."
    }
  ],
  "notes": [
    "Recovered only from games/gs1/asm/080912b8.s, the approved local ROM, the aggregate evidence header, the drafting-wave candidate, and the approved GCC 2.96 scoring route.",
    "The semantic role is a scheduled display-record updater for two adjacent terrain-height samples. A public function name is intentionally deferred until callers or neighboring translation-unit evidence establish the original subsystem vocabulary.",
    "This candidate claims zero DONE bytes."
  ]
}
