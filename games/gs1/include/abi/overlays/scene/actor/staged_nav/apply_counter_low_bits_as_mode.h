#ifndef ALCHEMY_ABI_OVERLAYS_SCENE_ACTOR_STAGED_NAV_APPLY_COUNTER_LOW_BITS_AS_MODE
#define ALCHEMY_ABI_OVERLAYS_SCENE_ACTOR_STAGED_NAV_APPLY_COUNTER_LOW_BITS_AS_MODE

/* Binding layer — not production source. Address / far-call ABI only. */

void Func_02003fa2(u8 *actor, s32 mode);

#define Actor_Apply Func_02003fa2

#endif
