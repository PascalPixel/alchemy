{
  "format": 1,
  "comment": "Owners withdrawn from routine work: both bounded search axes are spent and the owner still does not reproduce. Data for the check, not documentation -- it keeps a small number of owners out of the queue without putting a record of dead ends in front of every contributor. An entry is removed the moment its owner becomes exact. The opposite register is exact/provisional.json, which holds owners that DO match but whose source was written toward the bytes.",
  "unmatchable": [
    {
      "owner": "08002f10",
      "floor_halfwords": 6,
      "axes": [
        "compiler",
        "shape",
        "expressiveness"
      ],
      "reason": "Blocked on the store merge, not on a reading. The reference writes three consecutive words as `stmia rB!, {r0, r1, r2}` and then `subs rB, #12`, undoing the writeback into a register that is dead at the next instruction. gcc 2.96 Thumb cannot emit that: the store_multiple peepholes in arm.md are TARGET_ARM only, and Thumb's sole multi-register store is the block move, which always pairs stmia with an ldmia from memory -- a by-value struct already sitting in r1-r3 is spilled to the stack and reloaded before the stmia. old_agbcc does the same. So no C source reaches this sequence, and no compiler emits a dead compensating subs; it reads as a hand-written kick macro. The remaining differences here are the register assignment that ascending-order merge forces, downstream of the same cause."
    },
    {
      "owner": "08002fb0",
      "floor_halfwords": 11,
      "axes": [
        "compiler",
        "shape",
        "expressiveness"
      ],
      "reason": "Blocked on the store merge, not on a reading. The reference writes three consecutive words as `stmia rB!, {r0, r1, r2}` and then `subs rB, #12`, undoing the writeback into a register that is dead at the next instruction. gcc 2.96 Thumb cannot emit that: the store_multiple peepholes in arm.md are TARGET_ARM only, and Thumb's sole multi-register store is the block move, which always pairs stmia with an ldmia from memory -- a by-value struct already sitting in r1-r3 is spilled to the stack and reloaded before the stmia. old_agbcc does the same. So no C source reaches this sequence, and no compiler emits a dead compensating subs; it reads as a hand-written kick macro. The remaining differences here are the register assignment that ascending-order merge forces, downstream of the same cause."
    },
    {
      "owner": "080037d4",
      "floor_halfwords": 13,
      "axes": [
        "compiler",
        "shape",
        "expressiveness"
      ],
      "reason": "Blocked on the store merge, not on a reading. The reference writes three consecutive words as `stmia rB!, {r0, r1, r2}` and then `subs rB, #12`, undoing the writeback into a register that is dead at the next instruction. gcc 2.96 Thumb cannot emit that: the store_multiple peepholes in arm.md are TARGET_ARM only, and Thumb's sole multi-register store is the block move, which always pairs stmia with an ldmia from memory -- a by-value struct already sitting in r1-r3 is spilled to the stack and reloaded before the stmia. old_agbcc does the same. So no C source reaches this sequence, and no compiler emits a dead compensating subs; it reads as a hand-written kick macro. The remaining differences here are the register assignment that ascending-order merge forces, downstream of the same cause."
    },
    {
      "owner": "08003e10",
      "floor_halfwords": 12,
      "axes": [
        "compiler",
        "shape",
        "expressiveness"
      ],
      "reason": "Blocked on the store merge, not on a reading. The reference writes three consecutive words as `stmia rB!, {r0, r1, r2}` and then `subs rB, #12`, undoing the writeback into a register that is dead at the next instruction. gcc 2.96 Thumb cannot emit that: the store_multiple peepholes in arm.md are TARGET_ARM only, and Thumb's sole multi-register store is the block move, which always pairs stmia with an ldmia from memory -- a by-value struct already sitting in r1-r3 is spilled to the stack and reloaded before the stmia. old_agbcc does the same. So no C source reaches this sequence, and no compiler emits a dead compensating subs; it reads as a hand-written kick macro. The remaining differences here are the register assignment that ascending-order merge forces, downstream of the same cause."
    },
    {
      "owner": "08004838",
      "floor_halfwords": 6,
      "axes": [
        "compiler",
        "shape",
        "expressiveness"
      ],
      "reason": "Blocked on the store merge, not on a reading. The reference writes three consecutive words as `stmia rB!, {r0, r1, r2}` and then `subs rB, #12`, undoing the writeback into a register that is dead at the next instruction. gcc 2.96 Thumb cannot emit that: the store_multiple peepholes in arm.md are TARGET_ARM only, and Thumb's sole multi-register store is the block move, which always pairs stmia with an ldmia from memory -- a by-value struct already sitting in r1-r3 is spilled to the stack and reloaded before the stmia. old_agbcc does the same. So no C source reaches this sequence, and no compiler emits a dead compensating subs; it reads as a hand-written kick macro. The remaining differences here are the register assignment that ascending-order merge forces, downstream of the same cause."
    },
    {
      "owner": "080049e8",
      "floor_halfwords": 15,
      "axes": [
        "compiler",
        "shape",
        "expressiveness"
      ],
      "reason": "Blocked on the store merge, not on a reading. The reference writes three consecutive words as `stmia rB!, {r0, r1, r2}` and then `subs rB, #12`, undoing the writeback into a register that is dead at the next instruction. gcc 2.96 Thumb cannot emit that: the store_multiple peepholes in arm.md are TARGET_ARM only, and Thumb's sole multi-register store is the block move, which always pairs stmia with an ldmia from memory -- a by-value struct already sitting in r1-r3 is spilled to the stack and reloaded before the stmia. old_agbcc does the same. So no C source reaches this sequence, and no compiler emits a dead compensating subs; it reads as a hand-written kick macro. The remaining differences here are the register assignment that ascending-order merge forces, downstream of the same cause."
    },
    {
      "owner": "08004a28",
      "floor_halfwords": 6,
      "axes": [
        "compiler",
        "shape",
        "expressiveness"
      ],
      "reason": "Blocked on the store merge, not on a reading. The reference writes three consecutive words as `stmia rB!, {r0, r1, r2}` and then `subs rB, #12`, undoing the writeback into a register that is dead at the next instruction. gcc 2.96 Thumb cannot emit that: the store_multiple peepholes in arm.md are TARGET_ARM only, and Thumb's sole multi-register store is the block move, which always pairs stmia with an ldmia from memory -- a by-value struct already sitting in r1-r3 is spilled to the stack and reloaded before the stmia. old_agbcc does the same. So no C source reaches this sequence, and no compiler emits a dead compensating subs; it reads as a hand-written kick macro. The remaining differences here are the register assignment that ascending-order merge forces, downstream of the same cause."
    },
    {
      "owner": "08005340",
      "floor_halfwords": 15,
      "axes": [
        "compiler",
        "shape",
        "expressiveness"
      ],
      "reason": "Blocked on the store merge, not on a reading. The reference writes three consecutive words as `stmia rB!, {r0, r1, r2}` and then `subs rB, #12`, undoing the writeback into a register that is dead at the next instruction. gcc 2.96 Thumb cannot emit that: the store_multiple peepholes in arm.md are TARGET_ARM only, and Thumb's sole multi-register store is the block move, which always pairs stmia with an ldmia from memory -- a by-value struct already sitting in r1-r3 is spilled to the stack and reloaded before the stmia. old_agbcc does the same. So no C source reaches this sequence, and no compiler emits a dead compensating subs; it reads as a hand-written kick macro. The remaining differences here are the register assignment that ascending-order merge forces, downstream of the same cause."
    },
    {
      "owner": "08005394",
      "floor_halfwords": 15,
      "axes": [
        "compiler",
        "shape",
        "expressiveness"
      ],
      "reason": "Blocked on the store merge, not on a reading. The reference writes three consecutive words as `stmia rB!, {r0, r1, r2}` and then `subs rB, #12`, undoing the writeback into a register that is dead at the next instruction. gcc 2.96 Thumb cannot emit that: the store_multiple peepholes in arm.md are TARGET_ARM only, and Thumb's sole multi-register store is the block move, which always pairs stmia with an ldmia from memory -- a by-value struct already sitting in r1-r3 is spilled to the stack and reloaded before the stmia. old_agbcc does the same. So no C source reaches this sequence, and no compiler emits a dead compensating subs; it reads as a hand-written kick macro. The remaining differences here are the register assignment that ascending-order merge forces, downstream of the same cause."
    },
    {
      "owner": "080053e8",
      "floor_halfwords": 15,
      "axes": [
        "compiler",
        "shape",
        "expressiveness"
      ],
      "reason": "Blocked on the store merge, not on a reading. The reference writes three consecutive words as `stmia rB!, {r0, r1, r2}` and then `subs rB, #12`, undoing the writeback into a register that is dead at the next instruction. gcc 2.96 Thumb cannot emit that: the store_multiple peepholes in arm.md are TARGET_ARM only, and Thumb's sole multi-register store is the block move, which always pairs stmia with an ldmia from memory -- a by-value struct already sitting in r1-r3 is spilled to the stack and reloaded before the stmia. old_agbcc does the same. So no C source reaches this sequence, and no compiler emits a dead compensating subs; it reads as a hand-written kick macro. The remaining differences here are the register assignment that ascending-order merge forces, downstream of the same cause."
    },
    {
      "owner": "0800bc48",
      "floor_halfwords": 11,
      "axes": [
        "compiler",
        "shape",
        "expressiveness"
      ],
      "reason": "Blocked on the store merge, not on a reading. The reference writes three consecutive words as `stmia rB!, {r0, r1, r2}` and then `subs rB, #12`, undoing the writeback into a register that is dead at the next instruction. gcc 2.96 Thumb cannot emit that: the store_multiple peepholes in arm.md are TARGET_ARM only, and Thumb's sole multi-register store is the block move, which always pairs stmia with an ldmia from memory -- a by-value struct already sitting in r1-r3 is spilled to the stack and reloaded before the stmia. old_agbcc does the same. So no C source reaches this sequence, and no compiler emits a dead compensating subs; it reads as a hand-written kick macro. The remaining differences here are the register assignment that ascending-order merge forces, downstream of the same cause."
    },
    {
      "owner": "0800bdd4",
      "floor_halfwords": 7,
      "axes": [
        "compiler",
        "shape",
        "expressiveness"
      ],
      "reason": "Blocked on the store merge, not on a reading. The reference writes three consecutive words as `stmia rB!, {r0, r1, r2}` and then `subs rB, #12`, undoing the writeback into a register that is dead at the next instruction. gcc 2.96 Thumb cannot emit that: the store_multiple peepholes in arm.md are TARGET_ARM only, and Thumb's sole multi-register store is the block move, which always pairs stmia with an ldmia from memory -- a by-value struct already sitting in r1-r3 is spilled to the stack and reloaded before the stmia. old_agbcc does the same. So no C source reaches this sequence, and no compiler emits a dead compensating subs; it reads as a hand-written kick macro. The remaining differences here are the register assignment that ascending-order merge forces, downstream of the same cause."
    },
    {
      "owner": "0800c0f4",
      "floor_halfwords": 7,
      "axes": [
        "compiler",
        "shape",
        "expressiveness"
      ],
      "reason": "Blocked on the store merge, not on a reading. The reference writes three consecutive words as `stmia rB!, {r0, r1, r2}` and then `subs rB, #12`, undoing the writeback into a register that is dead at the next instruction. gcc 2.96 Thumb cannot emit that: the store_multiple peepholes in arm.md are TARGET_ARM only, and Thumb's sole multi-register store is the block move, which always pairs stmia with an ldmia from memory -- a by-value struct already sitting in r1-r3 is spilled to the stack and reloaded before the stmia. old_agbcc does the same. So no C source reaches this sequence, and no compiler emits a dead compensating subs; it reads as a hand-written kick macro. The remaining differences here are the register assignment that ascending-order merge forces, downstream of the same cause."
    },
    {
      "owner": "0800d304",
      "floor_halfwords": 9,
      "axes": [
        "compiler",
        "shape",
        "expressiveness"
      ],
      "reason": "Blocked on the store merge, not on a reading. The reference writes three consecutive words as `stmia rB!, {r0, r1, r2}` and then `subs rB, #12`, undoing the writeback into a register that is dead at the next instruction. gcc 2.96 Thumb cannot emit that: the store_multiple peepholes in arm.md are TARGET_ARM only, and Thumb's sole multi-register store is the block move, which always pairs stmia with an ldmia from memory -- a by-value struct already sitting in r1-r3 is spilled to the stack and reloaded before the stmia. old_agbcc does the same. So no C source reaches this sequence, and no compiler emits a dead compensating subs; it reads as a hand-written kick macro. The remaining differences here are the register assignment that ascending-order merge forces, downstream of the same cause."
    },
    {
      "owner": "0801965c",
      "floor_halfwords": 10,
      "axes": [
        "compiler",
        "shape",
        "expressiveness"
      ],
      "reason": "Blocked on the store merge, not on a reading. The reference writes three consecutive words as `stmia rB!, {r0, r1, r2}` and then `subs rB, #12`, undoing the writeback into a register that is dead at the next instruction. gcc 2.96 Thumb cannot emit that: the store_multiple peepholes in arm.md are TARGET_ARM only, and Thumb's sole multi-register store is the block move, which always pairs stmia with an ldmia from memory -- a by-value struct already sitting in r1-r3 is spilled to the stack and reloaded before the stmia. old_agbcc does the same. So no C source reaches this sequence, and no compiler emits a dead compensating subs; it reads as a hand-written kick macro. The remaining differences here are the register assignment that ascending-order merge forces, downstream of the same cause."
    },
    {
      "owner": "08019bac",
      "floor_halfwords": 15,
      "axes": [
        "compiler",
        "shape",
        "expressiveness"
      ],
      "reason": "Blocked on the store merge, not on a reading. The reference writes three consecutive words as `stmia rB!, {r0, r1, r2}` and then `subs rB, #12`, undoing the writeback into a register that is dead at the next instruction. gcc 2.96 Thumb cannot emit that: the store_multiple peepholes in arm.md are TARGET_ARM only, and Thumb's sole multi-register store is the block move, which always pairs stmia with an ldmia from memory -- a by-value struct already sitting in r1-r3 is spilled to the stack and reloaded before the stmia. old_agbcc does the same. So no C source reaches this sequence, and no compiler emits a dead compensating subs; it reads as a hand-written kick macro. The remaining differences here are the register assignment that ascending-order merge forces, downstream of the same cause."
    },
    {
      "owner": "0801a4fc",
      "floor_halfwords": 8,
      "axes": [
        "compiler",
        "shape",
        "expressiveness"
      ],
      "reason": "Blocked on the store merge, not on a reading. The reference writes three consecutive words as `stmia rB!, {r0, r1, r2}` and then `subs rB, #12`, undoing the writeback into a register that is dead at the next instruction. gcc 2.96 Thumb cannot emit that: the store_multiple peepholes in arm.md are TARGET_ARM only, and Thumb's sole multi-register store is the block move, which always pairs stmia with an ldmia from memory -- a by-value struct already sitting in r1-r3 is spilled to the stack and reloaded before the stmia. old_agbcc does the same. So no C source reaches this sequence, and no compiler emits a dead compensating subs; it reads as a hand-written kick macro. The remaining differences here are the register assignment that ascending-order merge forces, downstream of the same cause."
    },
    {
      "owner": "0801d980",
      "floor_halfwords": 12,
      "axes": [
        "compiler",
        "shape",
        "expressiveness"
      ],
      "reason": "Blocked on the store merge, not on a reading. The reference writes three consecutive words as `stmia rB!, {r0, r1, r2}` and then `subs rB, #12`, undoing the writeback into a register that is dead at the next instruction. gcc 2.96 Thumb cannot emit that: the store_multiple peepholes in arm.md are TARGET_ARM only, and Thumb's sole multi-register store is the block move, which always pairs stmia with an ldmia from memory -- a by-value struct already sitting in r1-r3 is spilled to the stack and reloaded before the stmia. old_agbcc does the same. So no C source reaches this sequence, and no compiler emits a dead compensating subs; it reads as a hand-written kick macro. The remaining differences here are the register assignment that ascending-order merge forces, downstream of the same cause."
    },
    {
      "owner": "080251d4",
      "floor_halfwords": 14,
      "axes": [
        "compiler",
        "shape",
        "expressiveness"
      ],
      "reason": "Blocked on the store merge, not on a reading. The reference writes three consecutive words as `stmia rB!, {r0, r1, r2}` and then `subs rB, #12`, undoing the writeback into a register that is dead at the next instruction. gcc 2.96 Thumb cannot emit that: the store_multiple peepholes in arm.md are TARGET_ARM only, and Thumb's sole multi-register store is the block move, which always pairs stmia with an ldmia from memory -- a by-value struct already sitting in r1-r3 is spilled to the stack and reloaded before the stmia. old_agbcc does the same. So no C source reaches this sequence, and no compiler emits a dead compensating subs; it reads as a hand-written kick macro. The remaining differences here are the register assignment that ascending-order merge forces, downstream of the same cause."
    },
    {
      "owner": "080284dc",
      "floor_halfwords": 8,
      "axes": [
        "compiler",
        "shape",
        "expressiveness"
      ],
      "reason": "Blocked on the store merge, not on a reading. The reference writes three consecutive words as `stmia rB!, {r0, r1, r2}` and then `subs rB, #12`, undoing the writeback into a register that is dead at the next instruction. gcc 2.96 Thumb cannot emit that: the store_multiple peepholes in arm.md are TARGET_ARM only, and Thumb's sole multi-register store is the block move, which always pairs stmia with an ldmia from memory -- a by-value struct already sitting in r1-r3 is spilled to the stack and reloaded before the stmia. old_agbcc does the same. So no C source reaches this sequence, and no compiler emits a dead compensating subs; it reads as a hand-written kick macro. The remaining differences here are the register assignment that ascending-order merge forces, downstream of the same cause."
    },
    {
      "owner": "08077394",
      "floor_halfwords": 11,
      "axes": [
        "compiler",
        "shape",
        "expressiveness"
      ],
      "reason": "Blocked on the store merge, not on a reading. The reference writes three consecutive words as `stmia rB!, {r0, r1, r2}` and then `subs rB, #12`, undoing the writeback into a register that is dead at the next instruction. gcc 2.96 Thumb cannot emit that: the store_multiple peepholes in arm.md are TARGET_ARM only, and Thumb's sole multi-register store is the block move, which always pairs stmia with an ldmia from memory -- a by-value struct already sitting in r1-r3 is spilled to the stack and reloaded before the stmia. old_agbcc does the same. So no C source reaches this sequence, and no compiler emits a dead compensating subs; it reads as a hand-written kick macro. The remaining differences here are the register assignment that ascending-order merge forces, downstream of the same cause."
    },
    {
      "owner": "08090824",
      "floor_halfwords": 9,
      "axes": [
        "compiler",
        "shape",
        "expressiveness"
      ],
      "reason": "Blocked on the store merge, not on a reading. The reference writes three consecutive words as `stmia rB!, {r0, r1, r2}` and then `subs rB, #12`, undoing the writeback into a register that is dead at the next instruction. gcc 2.96 Thumb cannot emit that: the store_multiple peepholes in arm.md are TARGET_ARM only, and Thumb's sole multi-register store is the block move, which always pairs stmia with an ldmia from memory -- a by-value struct already sitting in r1-r3 is spilled to the stack and reloaded before the stmia. old_agbcc does the same. So no C source reaches this sequence, and no compiler emits a dead compensating subs; it reads as a hand-written kick macro. The remaining differences here are the register assignment that ascending-order merge forces, downstream of the same cause."
    },
    {
      "owner": "08095160",
      "floor_halfwords": 10,
      "axes": [
        "compiler",
        "shape",
        "expressiveness"
      ],
      "reason": "Blocked on the store merge, not on a reading. The reference writes three consecutive words as `stmia rB!, {r0, r1, r2}` and then `subs rB, #12`, undoing the writeback into a register that is dead at the next instruction. gcc 2.96 Thumb cannot emit that: the store_multiple peepholes in arm.md are TARGET_ARM only, and Thumb's sole multi-register store is the block move, which always pairs stmia with an ldmia from memory -- a by-value struct already sitting in r1-r3 is spilled to the stack and reloaded before the stmia. old_agbcc does the same. So no C source reaches this sequence, and no compiler emits a dead compensating subs; it reads as a hand-written kick macro. The remaining differences here are the register assignment that ascending-order merge forces, downstream of the same cause."
    },
    {
      "owner": "08095290",
      "floor_halfwords": 11,
      "axes": [
        "compiler",
        "shape",
        "expressiveness"
      ],
      "reason": "Blocked on the store merge, not on a reading. The reference writes three consecutive words as `stmia rB!, {r0, r1, r2}` and then `subs rB, #12`, undoing the writeback into a register that is dead at the next instruction. gcc 2.96 Thumb cannot emit that: the store_multiple peepholes in arm.md are TARGET_ARM only, and Thumb's sole multi-register store is the block move, which always pairs stmia with an ldmia from memory -- a by-value struct already sitting in r1-r3 is spilled to the stack and reloaded before the stmia. old_agbcc does the same. So no C source reaches this sequence, and no compiler emits a dead compensating subs; it reads as a hand-written kick macro. The remaining differences here are the register assignment that ascending-order merge forces, downstream of the same cause."
    },
    {
      "owner": "080958a8",
      "floor_halfwords": 12,
      "axes": [
        "compiler",
        "shape",
        "expressiveness"
      ],
      "reason": "Blocked on the store merge, not on a reading. The reference writes three consecutive words as `stmia rB!, {r0, r1, r2}` and then `subs rB, #12`, undoing the writeback into a register that is dead at the next instruction. gcc 2.96 Thumb cannot emit that: the store_multiple peepholes in arm.md are TARGET_ARM only, and Thumb's sole multi-register store is the block move, which always pairs stmia with an ldmia from memory -- a by-value struct already sitting in r1-r3 is spilled to the stack and reloaded before the stmia. old_agbcc does the same. So no C source reaches this sequence, and no compiler emits a dead compensating subs; it reads as a hand-written kick macro. The remaining differences here are the register assignment that ascending-order merge forces, downstream of the same cause."
    },
    {
      "owner": "08097540",
      "floor_halfwords": 8,
      "axes": [
        "compiler",
        "shape",
        "expressiveness"
      ],
      "reason": "Blocked on the store merge, not on a reading. The reference writes three consecutive words as `stmia rB!, {r0, r1, r2}` and then `subs rB, #12`, undoing the writeback into a register that is dead at the next instruction. gcc 2.96 Thumb cannot emit that: the store_multiple peepholes in arm.md are TARGET_ARM only, and Thumb's sole multi-register store is the block move, which always pairs stmia with an ldmia from memory -- a by-value struct already sitting in r1-r3 is spilled to the stack and reloaded before the stmia. old_agbcc does the same. So no C source reaches this sequence, and no compiler emits a dead compensating subs; it reads as a hand-written kick macro. The remaining differences here are the register assignment that ascending-order merge forces, downstream of the same cause."
    },
    {
      "owner": "0809bb34",
      "floor_halfwords": 7,
      "axes": [
        "compiler",
        "shape",
        "expressiveness"
      ],
      "reason": "Blocked on the store merge, not on a reading. The reference writes three consecutive words as `stmia rB!, {r0, r1, r2}` and then `subs rB, #12`, undoing the writeback into a register that is dead at the next instruction. gcc 2.96 Thumb cannot emit that: the store_multiple peepholes in arm.md are TARGET_ARM only, and Thumb's sole multi-register store is the block move, which always pairs stmia with an ldmia from memory -- a by-value struct already sitting in r1-r3 is spilled to the stack and reloaded before the stmia. old_agbcc does the same. So no C source reaches this sequence, and no compiler emits a dead compensating subs; it reads as a hand-written kick macro. The remaining differences here are the register assignment that ascending-order merge forces, downstream of the same cause."
    },
    {
      "owner": "080b010c",
      "floor_halfwords": 6,
      "axes": [
        "compiler",
        "shape",
        "expressiveness"
      ],
      "reason": "Blocked on the store merge, not on a reading. The reference writes three consecutive words as `stmia rB!, {r0, r1, r2}` and then `subs rB, #12`, undoing the writeback into a register that is dead at the next instruction. gcc 2.96 Thumb cannot emit that: the store_multiple peepholes in arm.md are TARGET_ARM only, and Thumb's sole multi-register store is the block move, which always pairs stmia with an ldmia from memory -- a by-value struct already sitting in r1-r3 is spilled to the stack and reloaded before the stmia. old_agbcc does the same. So no C source reaches this sequence, and no compiler emits a dead compensating subs; it reads as a hand-written kick macro. The remaining differences here are the register assignment that ascending-order merge forces, downstream of the same cause."
    },
    {
      "owner": "080b0744",
      "floor_halfwords": 15,
      "axes": [
        "compiler",
        "shape",
        "expressiveness"
      ],
      "reason": "Blocked on the store merge, not on a reading. The reference writes three consecutive words as `stmia rB!, {r0, r1, r2}` and then `subs rB, #12`, undoing the writeback into a register that is dead at the next instruction. gcc 2.96 Thumb cannot emit that: the store_multiple peepholes in arm.md are TARGET_ARM only, and Thumb's sole multi-register store is the block move, which always pairs stmia with an ldmia from memory -- a by-value struct already sitting in r1-r3 is spilled to the stack and reloaded before the stmia. old_agbcc does the same. So no C source reaches this sequence, and no compiler emits a dead compensating subs; it reads as a hand-written kick macro. The remaining differences here are the register assignment that ascending-order merge forces, downstream of the same cause."
    },
    {
      "owner": "080f4028",
      "floor_halfwords": 14,
      "axes": [
        "compiler",
        "shape",
        "expressiveness"
      ],
      "reason": "Blocked on the store merge, not on a reading. The reference writes three consecutive words as `stmia rB!, {r0, r1, r2}` and then `subs rB, #12`, undoing the writeback into a register that is dead at the next instruction. gcc 2.96 Thumb cannot emit that: the store_multiple peepholes in arm.md are TARGET_ARM only, and Thumb's sole multi-register store is the block move, which always pairs stmia with an ldmia from memory -- a by-value struct already sitting in r1-r3 is spilled to the stack and reloaded before the stmia. old_agbcc does the same. So no C source reaches this sequence, and no compiler emits a dead compensating subs; it reads as a hand-written kick macro. The remaining differences here are the register assignment that ascending-order merge forces, downstream of the same cause."
    },
    {
      "owner": "resource_3b7_c_02000880",
      "floor_halfwords": 2,
      "axes": [
        "compiler",
        "shape"
      ],
      "reason": "The reference orders the counter increment before the volatile store and the argument load after it; the emitted pair is transposed."
    }
  ]
}
