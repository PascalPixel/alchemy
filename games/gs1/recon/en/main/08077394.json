{
  "schema_version": 1,
  "namespace": "main",
  "address": "0x08077394",
  "status": "proven-retained",
  "retained_classification": "owner_state_lookup_compiler_scheduling_allocation_module",
  "owner_bytes": 68,
  "executable_owner_bytes": 68,
  "candidate_bytes": 68,
  "differing_halfwords": 16,
  "wrong_instructions": 18,
  "classification": "get-owner-state",
  "resolved_asm_classification": {
    "kind": "compiler_output",
    "retention": "c_candidate",
    "confidence": "likely"
  },
  "cross_edition": {
    "editions": ["ja", "en", "de", "es", "fr", "it"],
    "relocation_normalized_core_bytes": 56,
    "core_diff_bytes": 0,
    "core_identical": true
  },
  "notes": [
    "Returns one of eight resident owner-state records, or one of six optional remote records for owner ids 0x80 through 0x85.",
    "The complete semantic candidate has the exact owner length, but literal hoisting, multiplication operands, entry scheduling, and branches remain broad. It did not meet the bounded-search gate and was not promoted.",
    "2026-08-27: re-measured baseline (candidate-show, --align): differing_halfwords=27, wrong_instructions=26 (corrected from a stale 15 recorded previously; wrong_instructions is a raw diff-line count, not directly comparable across tool revisions). Checked the session's repair-pattern catalog: no sign-extension bug (no shifts of a signed sum), no missing byte/halfword mask (both paths already return raw pointers), the three literal-pool words are already modeled as raw runtime addresses/offsets (0x02000500, 0x03001f28, and the derived -OWNER_STATE_SIZE*REMOTE_OWNER_FIRST), no bl-to-_call_via_rN trampoline is involved, no adjacent same-size locals to merge (remote_states is the only stack local and is not touched by the residual), no switch/if-chain reshape applies (the reference's own two-if shape is already what the source uses), no for/while spelling choice is involved (no loops), no narrow array element type is involved (no arrays). None of the catalog entries match this residual.",
    "2026-08-27: root cause of the residual is that the reference hoists the load of the literal-pool constant 0x02000500 (`ldr r2, [pc, #48]`) and a dead `mov r3, lr` to the function's entry block, before the first `cmp`/branch, and keeps the OWNER_STATE_SIZE*owner product in r3 (`muls r3, r0`) so the branch-1 return is one `adds r0, r3, r2` reusing the pre-loaded r2 -- whereas our candidate computes the product into r0 (`muls r0, r3`) and reloads the same constant from the pool inline (`ldr r3, [pc, #40]`) right before the add, once per return site. Tried three source-level rewrites to influence this: (1) hoisting the 0x02000500 load into an explicit `u8 *base` local declared before the first `if`; (2) swapping the addition operand order to `OWNER_STATE_SIZE * owner + 0x02000500` (multiply-first, matching the reference's muls-then-add instruction order); (3) splitting the multiply out into its own `u32 offset` temporary assigned before the return. All three produced a byte-for-byte IDENTICAL compiled object to the original phrasing (confirmed via candidate-show, compile=fresh each time) -- GCC 2.95's front end canonicalizes all three source shapes to the same RTL before this scheduling/allocation decision is made, so none of them can reach it. This confirms the diff is a genuine backend scheduling/register-allocation artifact (most likely literal-pool load hoisting plus a local-alloc register choice, in the same family as the session's documented local-alloc.c tie-break dead end) rather than anything expressible as ordinary C source restructuring, and is not reachable without forcing scheduling or register assignment (prohibited by CONTRIBUTING.md). Restored the original source verbatim (byte-identical output to all three variants tried) and left status at measured-draft; this residual should be treated as a floor for this owner under the fixed toolchain unless a future session finds an as-yet-untried structural lever.",
    "2026-08-29: found an improvement via a different structural lever than the ones tried on 2026-08-27 (which targeted only branch-1's literal-pool/multiply scheduling and left the original nested-early-return shape for branch 2 untouched). The second-branch residual turned out to be shape-sensitive: replacing the nested `if (remote_states != 0) { return ...; } / (falls through) return 0;` tail with a single named `void *result = 0;` local (declared at the top of the function, before `remote_states`), assigned inside the inner `if`, and returned once at the end -- keeping branch 1's own early `return` untouched -- dropped differing_halfwords from 27 to 16 at the same 68 owner bytes (measured 2026-08-29, candidate-show --align --first). Kept this as the new baseline source. Remaining 16-halfword residual is again allocator-shaped: `--allocator-order` names pseudo 34 (the `result` local) with actual=r1 but target=r0 -- the reference computes the branch-2 pointer directly into r0, our candidate computes it into r1 and adds one extra `adds r0, r1, #0` copy, and the pre-existing branch-1 literal-pool/muls-operand residual (already proven immovable on 2026-08-27) is unaffected and still present. Refuted on 2026-08-29 while chasing the r1-vs-r0 allocation, all measured at 68 bytes unless noted: single return point with `result` initialized at top and no early return for branch 1 at all (64 bytes, dh=33 -- removes needed code, wrong shape entirely); `result` initialized just before the outer `if` instead of at declaration (dh=27, matches the original nested shape, extra `movs r1,#0` gets scheduled mid-function); `result=0` assigned only inside the branch-2 `if` block plus an `else { result = 0; }` on the outer branch (72 bytes -- adds code); full if/else-if with explicit `result=0` in an `else` on every path, no early return anywhere (dh=27, 64 or 68 bytes depending on phrasing); combining the two branch-2 conditions with `&&` and an inline assignment-in-condition, both with and without the `result` local (dh=27 and dh=16 respectively -- the `&&`-with-result variant ties the current best but does not beat it); reusing `remote_states` itself as the return-value carrier instead of a separate `result` local (dh=18); reusing `result` itself as the initial `*(u8**)0x03001f28` load target, avoiding `remote_states` entirely (dh=18); computing the branch-2 offset into a named `u32 offset` temp before or after the `remote_states` load (dh=18 and dh=28 respectively); declaration order of `result`/`remote_states` swapped either way (no change, dh=16 both ways); `u8 *result` vs `void *result` vs `u32 result` typed variants (no change, dh=16 all three); Yoda-style `0 != remote_states` vs `remote_states != 0` (no change); braces vs brace-less single-statement ifs (no change); an unused dummy local inserted before `result` to perturb pseudo-numbering (no change); swapped add/multiply operand orders inside branch 1 and branch 2 (`owner * OWNER_STATE_SIZE`, `OWNER_STATE_SIZE * owner + 0x02000500`) -- all canonicalize identically as already established 2026-08-27, no change; swapping the two branches' order in the source (72 bytes, dh=31 -- wrong shape). None of these ~24 additional variants moved the needle past dh=16 or reached the allocator's targeted r0 assignment for the `result` pseudo; the remaining residual is treated as a second, independent local-alloc tie-break floor stacked on top of the already-documented branch-1 literal-pool/muls floor, both understood to require forcing scheduling or register assignment (prohibited) to close fully.",
    "2026-08-31 retained-assembly proof: the complete 68-byte candidate has equal branch topology and all three pool words exact; cross-edition verification proves all 56 core bytes identical in six editions. Roughly 27 bounded source variants leave the literal-hoisting, multiply-role and return-carrier floor unchanged. A shipped-route matrix finds -fno-regmove, both scheduling toggles, both CSE toggles, -fno-strength-reduce and -fno-gcse exactly flat at 68/16, while -fno-expensive-optimizations regresses to 68/18. Structured assembly remains authoritative under owner_state_lookup_compiler_scheduling_allocation_module; the candidate does not claim exact C."
  ]
}
