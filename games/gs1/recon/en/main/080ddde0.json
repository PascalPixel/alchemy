{
 "schema_version": 1,
 "namespace": "main",
 "address": "0x080ddde0",
 "status": "mapped",
 "measured": "2026-08-29",
 "map": {
  "head": "0x080ddde0",
  "head_symbol": "Region_080ddde0",
  "head_file": "games/gs1/asm/080ddde0.s",
  "head_prologue": "push {r5,r6,r7,lr}; mov r7,r11; mov r6,r10; mov r5,r9; push {r5,r6,r7}; mov r7,r8; push {r7}; (setup follows) -- full r8-r11 save. Note: tagged with the call-graph-reconstructed comment rather than an explicit FunctionHead_ label, but its genuine push+high-reg-save prologue is the real entry -- no earlier region branches into this address.",
  "true_epilogue": {
   "file": "games/gs1/asm/080de0d4.s",
   "note": "Ends `... bl sub_08004278 / movs r0,#46 / bl sub_08002dd8 / bl sub_080cdbc0 / add sp, #64 / pop {r3,r5,r6,r7} / mov r8,r3 / mov r9,r5 / mov r10,r6 / mov r11,r7 / pop {r5,r6,r7} / pop {r0} / bx r0`. 64-byte frame, four-high-register restore, same sub_08002dd8/sub_080cdbc0 cleanup idiom. This is the sole real epilogue."
  },
  "span": {
   "start": "0x080ddde0",
   "end_exclusive": "0x080de2f8",
   "size_bytes": 1304,
   "derivation": "end = start of next asm file games/gs1/asm/080de2f8.s, immediately following 080de0d4.s which owns the epilogue. size = 0x080de2f8 - 0x080ddde0 = 0x518 = 1304."
  },
  "member_regions_in_order": [
   {"addr": "0x080ddde0", "symbol": "Region_080ddde0", "file": "games/gs1/asm/080ddde0.s", "role": "real prologue + setup, tail branches to sub_080de0d4"},
   {"addr": "0x080de0d4", "symbol": "Continuation_080de0d4", "file": "games/gs1/asm/080de0d4.s", "role": "terminal continuation (header: '複数領域関数の終端継続部') ending in the real, shared epilogue"}
  ]
 },
 "domain_evidence": [
  "080de0d4's own header comment explicitly labels it the terminal continuation part of a multi-region function ('複数領域関数の終端継続部') -- direct textual confirmation of the pairing.",
  "grep across the corpus for the literal '080de0d4' finds exactly one external reference, the `b sub_080de0d4` tail branch inside games/gs1/asm/080ddde0.s -- confirming this is a private two-member cluster with no other predecessors or callers.",
  "This cluster sits immediately after the unrelated 080dd9c0/080dda3c/080ddb34 cluster (see companion dossier 080dd9c0.json) in the file listing; the two are adjacent but structurally independent -- neither references the other."
 ],
 "scoring_command": "out/cargo-target/release/compiler candidate-show games/gs1/recon/en/main/080ddde0.c --owner 080ddde0 --size 1304",
 "notes": [
  "Map only, per task scope -- no C source drafted, no translation-units.json edited.",
  "Head file uses the 'call-graph reconstructed, code+data mixed' comment style rather than an explicit FunctionHead_/Fragment_ label; classified as the true head from its genuine prologue shape and the absence of any external branch into it."
 ]
}
