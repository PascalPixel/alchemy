{
 "schema_version": 1,
 "namespace": "main",
 "address": "0x080a7440",
 "status": "measured-draft",
 "owner_bytes": 56,
 "executable_owner_bytes": 54,
 "alignment_bytes": 2,
 "candidate": {
  "routed": {
   "candidate_bytes": 52,
   "reference_bytes": 56,
   "differing_halfwords": 23,
   "wrong_instructions": 28
  },
  "best_bounded_axis": {
   "family": "old-agbcc",
   "flags": [
    "-fcall-used-r4"
   ],
   "candidate_bytes": 56,
   "reference_bytes": 56,
   "differing_halfwords": 14,
   "wrong_instructions": 28
  }
 },
 "cross_edition": {
  "code_and_pool_bytes": 54,
  "relocation_bytes": 4,
  "literal_bytes": 6,
  "core_bytes": 44,
  "core_identical": true,
  "editions": {
   "ja": "0x0809e384",
   "en": "0x080a7440",
   "de": "0x080aa2bc",
   "es": "0x080abcb0",
   "fr": "0x080abccc",
   "it": "0x080a74bc"
  }
 },
 "behavior": [
  "clears the shared menu selected-slot field",
  "runs the first character-selector interaction step",
  "returns cancellation unchanged or the selected owner index"
 ],
 "remaining": "The old-agbcc family restores the full owner extent, but its best bounded route still has a broad 14-halfword register-allocation residual. The owner is neither exact nor eligible for narrow permanent classification.",
 "notes": [
  "2026-08-31 exact-closure rerun: the apparent four missing candidate bytes are not missing behavior or a bad owner boundary. The 54 executable reference bytes plus two alignment bytes are independently accounted for, branch topology is equal, and both sides clear selected_slot, call Func_080a77a4(0), preserve -1, and otherwise return item_owner. The reference uniquely retains zero/result in r2 and emits an explicit post-call r0-to-r2 copy plus final r2-to-r0 copy; canonical GCC optimizes that carrier away and returns item_owner directly in r0.",
  "2026-08-31 finite raw-field probe: replacing the established InventoryMenuState fields with explicit u8-base accesses at +0x174/+0x21a grows the candidate to 58 bytes and regresses to 25 differing halfwords/32 wrong instructions, so the exact typed UI sibling context is already the best canonical source shape; reverted. Combined with the previously recorded shared-result and explicit-early-return probes and the complete shipped flag sweep, the bounded verdict remains canonical-GCC allocation/pool placement with no missing statement. The old-agbcc/-fcall-used-r4 56-byte route is not adoptable: it forces ABI allocation, still differs by 14 halfwords, and is not the canonical compiler route. Zero DONE bytes.",
  "2026-08-31: repaired a real control-flow bug in the draft: the selected-owner mapping guard had been placed after an unconditional return and was therefore unreachable. Moving the return after the guard and promoting the call result from s16 to its evidenced s32 lifetime restores the complete behavior and makes branch topology equal. The corrected canonical candidate is 52/56 bytes with 23 differing halfwords and 28 wrong instructions. A fresh sweep of the shipped allocation, scheduling, CSE, regmove, thread-jump, strength-reduction, rerun-CSE, and expensive-optimization axes found no improved route; the remaining residual is allocation and pool placement, not missing behavior.",
  "2026-08-31 bounded UI-family pass: tested two source structures named directly by the four missing instructions. First, sharing an initialized result carrier with the selected-slot clear (`result = 0; selected_slot = result`) does not recover the reference's long-lived r2 carrier: GCC hoists the 0x174 offset before the shared-state load, keeps zero in r0, removes the post-call result copy, and shrinks the candidate further to 48 bytes / 26 differing halfwords. Second, spelling cancellation as an explicit early return (`if (result == -1) return result; return item_owner`) normalizes to the existing 52-byte / 23-halfword candidate byte-for-byte. Transferable fact: GCC 2.96's optimizer erases both C-level return shapes here; the reference's r2 result carrier and r0-to-r2 copy cannot be inferred merely from an early-return spelling, while sharing the earlier zero lifetime actively regresses code placement. Both probes were removed.",
  "The name is bounded by the sole local caller OpenCharacterSelector and the selected-slot and item-owner fields in InventoryMenuState.",
  "All six editions have the same relocation-normalized instruction core; the trailing two bytes are owner alignment rather than executable instructions or pool data.",
  "Recovered only from retained assembly, approved local GS1 ROMs, local types and callers, and the approved compiler. This candidate claims zero DONE bytes."
 ]
}
