{
  "schema_version": 1,
  "namespace": "main",
  "address": "0x0801965c",
  "status": "measured-draft",
  "owner_bytes": 104,
  "candidate_bytes": 100,
  "differing_halfwords": 43,
  "wrong_instructions": 50,
  "classification": "copy-ui-render-string",
  "notes": [
    "Complete ordinary-C reconstruction of a bounded u16 render-string copy. It clears work offset 0x12b2, asks Func_08018038 to build the shared string at work offset 0xeb0, copies at most capacity-1 halfwords through the first zero, appends zero, and returns the copied count.",
    "The shared work pointer and +0xeb0 render buffer are independently established by the exact UiText_SetRenderString source. Multiple UI callers pass a stack u16 buffer and a fixed capacity, corroborating the bounded-copy interface.",
    "The initial indexed draft compiled to 112 bytes with different topology because it spilled the destination and recomputed both indexed addresses. The one permitted evidence-led correction adopted the reference's walking source pointer, fixed destination base, byte offset, and post-builder count initialization.",
    "After that correction canonical GCC 2.96 emits 100 bytes against 104 reference bytes, with 43 differing halfwords and 50 wrong instructions. The compiler still schedules the absolute-zero value early, folds a repeated source test into a second load, and does not preserve the reference's capacity in ip; no further source search was performed in this lane.",
    "This measured draft claims zero DONE bytes. Retained assembly remains authoritative; no inline assembly, fixed-register variable, frame forcing, copied instruction bytes, or assembly promotion is used."
  ]
}
