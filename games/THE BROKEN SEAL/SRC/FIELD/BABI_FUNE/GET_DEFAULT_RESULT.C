#include "TYPES.H"

/*
 * Babi Fune: the no-hit result. The veneer symbol Func_02000038 is shared
 * across many overlays (each binds its own runtime copy), so the name is
 * kept and the body just returns zero.
 */

s32 Func_02000038(void)
{
    return 0;
}
