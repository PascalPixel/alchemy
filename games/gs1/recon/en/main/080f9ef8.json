{
  "schema_version": 1,
  "namespace": "main",
  "address": "0x080f9ef8",
  "status": "measured-draft",
  "owner_bytes": 68,
  "executable_owner_bytes": 68,
  "candidate_bytes": 68,
  "differing_halfwords": 24,
  "wrong_instructions": 14,
  "classification": "stop-music-track",
  "resolved_asm_classification": {
    "kind": "compiler_output",
    "retention": "c_candidate",
    "confidence": "likely"
  },
  "cross_edition": {
    "editions": ["ja", "en", "de", "es", "fr", "it"],
    "relocation_normalized_core_bytes": 60,
    "core_diff_bytes": 0,
    "core_identical": true
  },
  "notes": [
    "2026-09-01 signedness and condition-shape probe: spelling the active-bit test as an s8 sign test emits ldrsb/cmp/bge and preserves the 24-halfword/14-wrong residual while making branch topology worse. Folding the channel-type mask into its initializer or assignment condition likewise ties or regresses; neither removes GCC's redundant post-ands compare. The readable u8 mask test and separate s32 compound mask remain canonical, and these axes are closed.",
    "2026-09-01 bounded flag-test probe: the exact in-repository MusicTrack_Finish witness emits ldrb/mov/tst from a genuinely multi-bit 0xc7 mask under its recorded compiler route. For this owner's one-bit 0x80 predicate, truth, equality-to-mask, and masked-ordering spellings all collapse to the same ldrb/lsrs/cmp sequence. The old-agbcc route regresses to 72 bytes, 34 differing halfwords, and 28 wrong instructions; nine shipped GCC 2.96 optimization/pass toggles are neutral at 68/24/14. The mask-spelling and compiler-route axes are therefore closed.",
    "2026-09-01: preserving the channel type as an s32 carrier and applying the 7-bit hardware-channel mask as a compound update reproduces the reference's r0/r3 roles through the mask. This improves the exact-extent candidate from 26 differing halfwords / 18 wrong instructions to 24 / 14. The remaining post-mask compare and entry flag-test shape still prevent adoption.",
    "2026-08-31: reconstructing the original translation-unit context exposed its local `_call_via_r3` veneer at 0x080f9ee8, rather than the global veneer bank, and re-auditing the shipped per-file ABI route showed that r4 remains callee-saved for this source. Registering both facts improves the exact-extent candidate from 31 differing halfwords / 42 wrong instructions to 26 / 18. The remaining residual is still a broad flag-test and allocation mismatch, so this remains Draft C and claims zero DONE bytes.",
    "2026-08-31: retaining the entry `track->flags` read in a named `u8 flags` carrier improves the candidate from 72/36/50 to the exact 68-byte extent with 31 differing halfwords and 42 wrong instructions. GCC changes the flag test from `ands`/`cmp` to an equivalent bit-extract `lsrs`/`cmp`; using an `s32` carrier emits the same result. An explicit outer early return is byte-identical to the original nested conditional and was reverted. The remaining residual is still broad register allocation plus the reference's `movs #128; tst` test shape, so this is retained as a measured Draft C improvement only.",
    "Stops every CGB channel owned by an active music track, invokes the configured oscillator-disable callback for hardware channel types, and clears both sides of the ownership link.",
    "The six editions retain an identical 60-byte relocation-normalized executable core inside the same 68-byte owner boundary.",
    "A fresh declared-translation-unit rebuild confirms the stored 68-byte, 24-differing-halfword, 14-wrong-instruction score. The owner remains Draft C and is not eligible for a permanent compiler classification."
  ]
}
