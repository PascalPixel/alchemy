[package]
name = "music-residuals"
version = "0.1.0"
edition = "2021"

[lib]
name = "music_residuals"
path = "src/lib.rs"

[dependencies]
music = { path = "../music" }
serde_json = { version = "1", features = ["preserve_order"] }
