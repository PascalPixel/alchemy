[workspace]

[package]
name = "audio-engine-data"
version = "0.1.0"
edition = "2021"

[lib]
name = "audio_engine_data"
path = "src/lib.rs"


[dependencies]
canonical-json = { path = "../canonical-json" }
serde_json = { version = "1", features = ["preserve_order"] }
