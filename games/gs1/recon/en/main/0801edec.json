{
 "schema_version": 1,
 "namespace": "main",
 "address": "0x0801edec",
 "status": "proven-permanent",
 "owner_bytes": 124,
 "candidate_bytes": 100,
 "differing_halfwords": 61,
 "wrong_instructions": 71,
 "translation_unit": "unknown",
 "compiler_route": "canonical-gcc296",
 "algorithm": "Reads global Data_03001e8c (a callable value cached from *0x03001e8c). If it is NULL, DMA-fills the caller's argument buffer with a fixed 0xe0e0 halfword pattern (fixed-source, count 0xa0, DMA3 control 0x810000a0) as a fallback -- classic GBA 'memset via DMA' idiom. If it is non-NULL, bump-allocates a 0x214-byte scratch buffer (Func_08004938 == Runtime_BumpAllocate), DMA3-copies a 0x214-byte ROM template from 0x080158e8 into it (control 0x84000085 = enable|32bit|133 words), calls through Data_03001e8c with argument (via the interworking call-via-register veneer at Func_080072fc), then frees the scratch buffer (Func_08002df0 == Sys_Free). The DMA-populated scratch buffer is never otherwise read; only its allocation/populate/free side effects occur before/after the indirect call.",
 "evidence": [
  "Literal pool value 0x03001e8c at the function's first pc-relative load exactly matches ADDR_03001E8C / Data_03001e8c, an already-named global used as a base pointer in numerous sibling owners (e.g. 0801ef68.c, 08017248.c).",
  "candidate-show independently symbolizes the bl target at Func_080072fc's linked address as `_call_via_r6` (or `_call_via_r7`, depending on candidate register allocation) in the emitted candidate.insns whenever a value is passed to it in the low register that becomes the branch target -- this is the compiler toolchain's own knowledge of the ARM/Thumb interworking call-via-register glue, not a guess made here. The same address is called from several sibling owners (08002fb0.s, 08003e10.s) each with a different value in the target register (a freshly returned pointer, a global), confirming it is a generic indirect-call veneer rather than a fixed named function.",
  "Func_08004938 / Func_08002df0 are already declared elsewhere in this tree as Runtime_BumpAllocate(s32 size) and Sys_Free(void *) respectively (games/gs1/include/types.h, games/gs1/include/runtime_mem.h), and are called with the identical three-word DMA3-setup idiom (source/destination/control written to 0x040000d4) in 08003e10.c, 08002fb0.s and other siblings.",
  "The DMA3 register base 0x040000d4 and the struct DmaChannel{source,destination,control} shape are the project's established idiom (games/gs1/recon/en/main/video_dma_family.h), already used unmodified by 08003e10.c for the exact same three-field-store pattern.",
  "The reference's second-branch literal for the allocation size is loaded from a 4-byte literal-pool entry (0x00000214) and is later recovered via a runtime `lsrs r5, r5, #2`, rather than being synthesized in place with a cheap shift-immediate sequence (movs+lsls) the way an ordinary compile-time integer constant is; GCC 2.96 on this route only emits a pool load plus a real runtime shift when the value is not compile-time foldable. 08003e10.c's own '(u32)LoadedRuntime_Size' pattern (an absolute-address linker symbol registered in games/gs1/recon/translation-units.json) produces exactly this shape for its 0xe0-byte case. This owner's 0x214 does not correspond to any absolute_symbols entry currently registered in translation-units.json; only games/gs1/recon/en/main/0801edec.c/.json are in scope for this pass, so a new absolute-size symbol could not be registered to test/confirm this."
 ],
 "credible_shapes_tried": [
  "plain non-volatile per-field DmaChannel struct stores (dma->source=...; dma->destination=...; dma->control=...;), matching 08003e10.c's own proven idiom: produces three separate single-register `str` instructions at each DMA setup site, never the reference's grouped `stmia r3!, {r0,r1,r2}` -- consistent with the compiler_backend_evidence below.",
  "caching the global into a local `void *routine` before branching (vs. re-reading Data_03001e8c at each use): re-reading forces an extra `ldr` immediately before the indirect call that the reference does not have; caching once matches the reference's single load-and-reuse shape and was kept.",
  "naming the allocation size with a plain `u32 size = 0x214;` local reused for both the Func_08004938 argument and the `(size >> 2)` control-word shift: GCC 2.96 fully constant-folds this at compile time (produces `movs r0,#133; lsls r0,#2` and a folded control literal), never the reference's pool-loaded 0x00000214 and runtime `lsrs r5, r5, #2` -- consistent with 0x214 being an unregistered linker/absolute-symbol value rather than an ordinary integer literal in the true source.",
  "if/else branch order: `if (Data_03001e8c == 0) { fallback-fill } else { main-dispatch }` reproduces the reference's `cmp r7,#0 / bne .L0` branch-and-fallthrough shape (fallback stays in the fallthrough position, dispatch is the taken branch target) exactly; the inverse order (`if (!= 0) { main } else { fallback }`) produced a mismatched branch/fallthrough assignment and was discarded."
 ],
 "compiler_backend_evidence": "Same conclusion as games/gs1/recon/en/main/08003e10.json, independently applicable here since this owner's main branch uses the identical DMA3 three-field-store idiom against the same struct shape and DMA base. Reading alchemy-gcc/gs1cc/gcc/config/arm/arm.md and arm.c: the STM-combining logic that could fold three independently-sourced register values into one `stmia r3!, {r0,r1,r2}` (store_multiple_sequence / *stmsi / *stmsi_postinc, arm.md ~4990-5083) is gated TARGET_ARM only and has no Thumb counterpart; the only Thumb multi-register store patterns (movmem8b/movmem12b, thumb_expand_movstrqi, thumb_output_move_mem_multiple) always emit a paired ldmia+stmia block copy, never a lone grouped store of independently computed values. No ordinary field-store or aggregate-store C shape on this Thumb/GCC 2.96 route can therefore reach the reference's standalone `stmia` at either DMA setup site.",
 "remaining": "61 of 124 reference halfwords differ, concentrated at (1) both DMA3 three-field setups, where the reference's ungrouped `stmia r3!, {r0,r1,r2}` is structurally unemittable from any ordinary C shape on this compiler route (see compiler_backend_evidence -- same finding as the 08003e10 family), and (2) the allocation-size handling, where the reference treats 0x214 as a non-foldable (linker-symbol-shaped) value requiring a fourth callee-saved register (r8, alongside r5/r6/r7), which this pass could not reproduce without registering a new absolute-size symbol in games/gs1/recon/translation-units.json -- out of scope for this owner's permitted touch set (games/gs1/recon/en/main/0801edec.c and its .json sidecar only). Candidate length (100 bytes) is short of the 124-byte reference primarily from the missing r8 push/pop pair and the grouped-store byte savings. Both call targets (Func_08004938, the call-via-register veneer at Func_080072fc, and Func_08002df0) match the reference exactly once reached.",
 "notes": [
  "Cold-start owner: games/gs1/recon/en/main/0801edec.c did not exist before this pass; games/gs1/asm/0801edec.s is the proven byte-exact retained assembly.",
  "No inline assembly, fixed-register variable, compiler barrier, forbidden ABI, copied instruction bytes, or compiler modification was used.",
  "This candidate claims zero DONE bytes.",
  "2026-08-27: reclassified measured-draft -> proven-permanent. This owner's retained assembly is already independently proven unreachable from ordinary C at the manifest/classification level: out/gs1-en/full/asm/manifest.json marks it kind=thumb_multiple_transfer_module, retention starting with keep_, confidence=proven, citing arm_md/arm_c line ranges showing GCC 2.96's Thumb backend can only emit a multi-register stmia paired immediately with a matching ldmia (movmem8b/movmem12b), and that ARM's standalone store_multiple/stmsi patterns are TARGET_ARM only -- a lone stmia from independently-computed register values (as this owner's reference has) is architecturally unreachable on this route, not an unexplored search space. This specific pattern was independently rediscovered by multiple dedicated passes this session (08004838, 0800bc48, 08003e10, 0808fecc, 080c0184, and partially 080e7404) before the shared manifest-level proof was found; recording the status correction here so future sessions don't repeat the rediscovery."
 ]
}
