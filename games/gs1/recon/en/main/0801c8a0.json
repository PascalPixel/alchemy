{
  "schema_version": 1,
  "namespace": "main",
  "address": "0x0801c8a0",
  "status": "proven-retained",
  "classification": "encoded_index_lookup_compiler_allocation_module",
  "owner_bytes": 132,
  "score": {
    "measured": "2026-08-31",
    "candidate_bytes": 132,
    "reference_bytes": 132,
    "differing_halfwords": 33,
    "wrong_instructions": 60,
    "class": "missing-extra-code",
    "note": "Both lookup passes and the complete owner extent are recovered with equal branch topology. All four pool words are exact and cross-edition verification proves the complete 116-byte core identical in every edition. A 10,000-candidate annotation-free search finds no decoded repair, while canonical and seven bounded shipped compiler routes find no exact or closer result. The remaining residual is equivalent output-pointer, entry-pointer, index, mask, and per-iteration halfword carrier allocation. Structured assembly remains authoritative under encoded_index_lookup_compiler_allocation_module; the candidate does not claim exact C."
  },
  "notes": [
    "Replaced an unrelated display-script family transplant with the reference's actual two-pass encoded-entry lookup and recovered its three-argument signature.",
    "The reference's first global access is Data_02000240 + 0x220 expressed as element 136 of a four-byte runtime view; the second is the adjacent 0x02000462 halfword re-read for each comparison pass.",
    "A 10,000-candidate annotation-free permutation request stopped safely because the allocator decoder found no unique repair. Canonical and seven bounded shipped-flag routes produced no exact result; -fno-regmove, -fno-schedule-insns2, and -fno-expensive-optimizations regressed."
  ]
}
