{
  "schema_version": 1,
  "namespace": "main",
  "address": "0x080b3050",
  "status": "measured-draft",
  "owner_bytes": 448,
  "retention": "c_candidate",
  "score": {
    "measured": "2026-08-30",
    "candidate_bytes": 448,
    "reference_bytes": 448,
    "differing_halfwords": 24,
    "wrong_instructions": 6,
    "branch_topology": "equal",
    "matched_prefix_instructions": 48,
    "note": "Exact-extent semantic reconstruction. The remaining six instruction choices are localized scheduling/rematerialization differences; adoption still requires zero differing halfwords."
  },
  "domain": "Shop party-member icon radial-burst presentation, including cursor-state save/restore, icon animation, effect spawning, cleanup, sound, and scheduler callbacks.",
  "expressed": [
    "dereference the 0x03001f2c ShopRuntime pointer cell before accessing runtime fields",
    "save and restore shop->cursor.anchor->kind while temporarily selecting kind 13",
    "use shop->party_action and shop->party_member_icons[member] through the established ShopRuntime layout",
    "read two eight-member signed-halfword coordinate arrays at runtime offsets 0x134 and 0x144",
    "register Func_080b2ffc with flags 0xc80 and remove the same callback during teardown",
    "spawn eighteen 0x48-byte radial-motion effects, expose the selected member after the sixth spawn, then update and clean twenty-four effect slots",
    "use the independently resolved cleanup flag byte at runtime offset 0x3f5 rather than the prior guessed 0x3f0 base"
  ],
  "verified_against_reference": [
    "fresh canonical candidate-show: 448 candidate bytes, 448 reference bytes, equal branch topology, 24 differing halfwords, 6 wrong instructions",
    "Func_080b2ffc and BattleEffect_UpdateRadialMotion identities resolve through already-adopted source owners",
    "aggregate-aware m2c independently confirms the 0x134/0x144 coordinate arrays, 0x3b0 effect base, 0x3f5 cleanup flag base, loop bounds, and pointer advances"
  ],
  "rejected": [
    {
      "shape": "single-expression cleanup flag shift",
      "result": "best retained cleanup form; GCC removes the shift but keeps the candidate at 448 bytes and 24 differing halfwords"
    },
    {
      "shape": "split cleanup flag load and <<= 24 statement",
      "result": "retains the reference shift but perturbs literal placement and regresses to 67 differing halfwords / 8 wrong instructions"
    },
    {
      "shape": "canonical route flag matrix over scheduling, regmove, CSE, rerun-CSE, and expensive-optimization toggles",
      "result": "no multi-instruction improvement; several routes regress materially, so no per-owner compiler override is justified"
    }
  ],
  "notes": [
    "A named callback_flags local is semantically ordinary and fixes the callback literal-load scheduling exactly, improving the best score from 26 to 24 differing halfwords.",
    "The remaining differences are position-store scheduling, one icon-offset rematerialization choice, and cleanup shift elimination/branch displacement. No fixed-register variable, inline assembly, compiler barrier, copied instruction bytes, or compiler modification was used.",
    "This candidate claims zero DONE bytes because differing_halfwords is not zero."
  ]
}
