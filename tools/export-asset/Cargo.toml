[workspace]

[package]
name = "export-asset"
version = "0.1.0"
edition = "2021"

[lib]
name = "export_asset"
path = "src/lib.rs"

[dependencies]
crc32fast = "1"
flate2 = { version = "1", default-features = false, features = ["rust_backend"] }
import-asset = { path = "../import-asset" }
png = "0.17.16"
