[workspace]

[package]
name = "remaining-survey"
version = "0.1.0"
edition = "2021"

[lib]
name = "remaining_survey"
path = "src/lib.rs"

[[bin]]
name = "remaining-survey"
path = "src/main.rs"

[dependencies]
serde_json = { version = "1", features = ["preserve_order"] }
