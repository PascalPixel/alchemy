{
  "owner": "main:0x080bbb0c",
  "span_bytes": 6332,
  "score": {
    "candidate_bytes": 6324,
    "differing_halfwords": 2630,
    "wrong_instructions": 1072,
    "code_hunks_normalized": "24 hunks / 33 rows (pool and phase excluded)",
    "class": "wrong",
    "note": "raw halfwords are phase (8-byte deficit against the reference distributed over the remaining named ref-extras); the normalized inventory is the compass"
  },
  "shape": {
    "calls": null,
    "branches": null,
    "loops": null,
    "memory_ops": null
  },
  "expressed": [
    "prologue and first argument block only",
    "case 11/0 pp subtract-floor tails with shared pp_store (.L85)",
    "case 2/12 heal add-cap-rederive shape",
    "case 2/3/6-7-9 shared hp_tail (.L77)",
    "case 6/7/9 full body: item kind-switch (uninitialised kind on items 4,5,10,11,16,17), per-nibble tables, cur-based floor",
    "effect-switch case order matching reference emission (64,4,61/62,63,59,58,9..6,13..10,5,56,57,3,17..14,18..)",
    "nibble case 0 moved to reference position (after case 2)",
    "effect ids rebound to the jump table (17 were scrambled), texts and fields verified against assembled pool words",
    "heal 61/62 u16/s16 dual-read shape with single Div",
    "63 max-held-in-register shape",
    "effect 4 status_141 block",
    "case 3 hit-arm after body with text 0x854",
    "27/69 true texts (0x84f/0x850/0x84c, 0x85f/0x85e)",
    "TEXT_SIDE as per-arm pushes (crossjump forms the shared tail; bhi/bls variance is branch-range)",
    "case 4/5 subtract-shape KO tail with own dealt/store",
    "slots walk as dual byte-offset walkers (off, j) matching both inductions",
    "saved list walked as u16",
    "effect tail re-reads action->effect, no n variable",
    "drain caps re-derive via u16 field pair",
    "single gated offset (zero when object->state==255)",
    "hit seeded from target_results, no result variable",
    "case 4/5: no power gate; KO arms inline in 4/5, 3, 6/7/9",
    "TAKE_PWR without a variable in the five re-reading bodies",
    "BUFF_CLEAR clears 12c,12d,12e,12f in order",
    "sleep-wake gate as nested ifs (fold-const kept off), text 0x883",
    "shared =1/=2 store tails (.L205/.L207) reproduced by plain stores + crossjump",
    "declaration block in reference spill order (early decl = high slot): frame 104->100, 16/21 slot usage counts exact",
    "multi-target: tid local, n=0, while((u32)++n<=19), full expression per iteration",
    "kind at function scope between power and cmd (ref slots: power=12 kind=8 cmd=4)",
    "DK_ATTACK: defense and action->power each read once into a local (def r1->fp, apwr r5 across the call), raw-proven",
    "multi-target scan compares target_id (sl) directly, no tid local",
    "saved-slot refresh: do-while, count-- before the call, *q++",
    "all 60 message ids are absolute Value_<hex> symbols: the reference pools 0x820/0x850/0x860/0x870, which thumb_shiftable_const proves a literal never does",
    "multi-target loop index via nested s32 idx (copy-prop folds it; emission unchanged, recorded)",
    "actor drain heals (EFX_DRAIN_HP/_HALF/_PP): add-cap-rederive through a block-scope heal local, single store at the end, full-test re-reads the max field -- size now equals the reference exactly",
    "ADJUST_ATKDEF/ADJUST_RES carry the turns field as an offset constant assigned to a local at the top: the assignment lives in a call-saved register across both pushes (306/308/310 exceed the movs immediate, so const-prop keeps the pseudo). r6 usage 84 -> 161 against the reference's 156; the attack handler now matches to two scheduler rows",
    "turns store is *(u8 *)((u8 *)target + toff) = 7 through that local",
    "do{}while(0) macro wrappers (CLAMP_MOD, TAKE_BONUS, APPLY_GUARD, TEXT_SIDE) emit NOTE_INSN_LOOP_BEG/END pairs that survive to sched2 and act as same-register scheduling barriers; bare-brace macros remove them. KO push pairs now match the reference order (r0 before r1 at the second push), tid-copy before cur=0",
    "dependence-table method validated end to end: rtl-schedule table + -fsched-verbose=5 traced the tie to insn 8949's barrier-induced dependence on the r1 copy",
    "falloff 5/6/8 select really is a switch: balanced tree, one selector load (earlier rejection was measured against stale insns)",
    "HEAL_60: volatile u16 mentions give the reference's four independent loads; maxv read once ahead of the if; div result added directly into heal (both restore-7 and heal-60)",
    "power routed through dmg at the three CalcPower sites: dmg = action->power; dmg = Battle_CalcPower(dmg, ...) -- the reference's ldrh into r5 plus copy to r0 is the accumulator, and the ldrh outranks the 128 build in the dependence table because dmg's chain is longer",
    "ADJUST_RES does NOT carry the early toff: the reference's res handlers build 310 after the pushes (movs #155 per body, crossjump shares only the lsls/strb tail). With the plain member store back, all four res bodies exist separately exactly as the reference lays them out. The early-toff pattern is ATK/DEF only.",
    "stub map: 13 of 14 out-of-line b-stubs already identical to the reference in content and predecessor count",
    "cross-jump depth rule measured: the final jump2 pass (after sched2) merges identical tails but never across a call insn -- the res bodies' movs #155 stays per-body for exactly that reason",
    "the reference's atk family is itself mixed (two early-toff in r6, two rematerialized late): one uniform toff macro under varying per-body register pressure",
    "walk: jsave local (the reference's lr pseudo) with i/jsave/j initialized before the 254-test, matching the reference's pre-if init cluster. Found via a 32-variant matrix scored on the walk window: base2/woff/val toggles are inert or harmful; preinit+jsave alone lifts global coloring (wrong 1555 -> 1481, best yet)",
    "walk 254-recheck reads through a union member (union Cell). gcse's exp_equiv_p refuses MEMs whose MEM_ALIAS_SET differ, and lang_get_alias_set gives union accesses set 0 while the plain s16 cast carries the short set -- the PRE hoist that fabricated our ldrh cache cannot unify them. Walk window 26 -> 14 rows; the jsave-in-lr copy (movs r6, lr shape) materializes",
    "found255 store index through a block local t = jsave + 102: arithmetic definitions survive copy propagation where plain copies fold, giving the reference's movs/adds/strh-indexed store",
    "hit assignment through a first local with per-arm stores: first = plan->target_results[slot]; if (first == -1) hit = HitCheck(...); else hit = first. The old shape stored hit before the test, spilling the ldrsb across the call; the reference defers the store into each arm. Clears the ldrsb spill and the HitCheck argument-order rows",
    "EFX_AGI_SET_DOWN4 stores -4 through an s32 local: the reference builds movs #4/negs in a register (sharable with the clamp constant) where the literal 252 truncates at expand",
    "the two damage-emphasis TEXT sites decide the text in a variable and push once (TEXT_SIDE_V): the reference's arms are argument-only stubs feeding one shared push, which crossjump cannot have made because the match would cross the bl. Per-site measurement keeps the other ten sites per-arm",
    "attack KO arm order: if (cur > 0) FINISH else KO at the attack case only (siblings measured better KO-first)",
    "the two emphasis sites interleave cur -= dmg between the text select and the push -- hand-written order the reference's arm blocks carry the subtraction in",
    "session arc: 236 -> 111 normalized reference rows across ~30 committed source facts; the fix-cascade-reread loop is the working method and each cycle costs one landscape shift",
    "all five subtract-then-text tails interleave: message select, subtraction, single push -- attack, case-6 emphasis, drain, PP damage, case 3. Found as one family across two reread cycles; wrong 1551 -> 1459",
    "multi-target scan is a goto loop with an idx local: no loop notes, so no strength reduction and no hoisting; the 748 rebuilds per iteration exactly as the reference emits (a stale-era rejection reversed by truthful measurement)",
    "scan head probe indexes through hidx = 748 (const-prop keeps constants wider than a movs immediate)",
    "case-6 KO text interleaves cur = 0; the attack subtracts between UNIT and VALUE -- per-site drift mapped one site at a time",
    "the four interleave sites subtract AFTER the text push in the source; the scheduler hoists the subs into the call setup and the movs #4 wins its tie at every site as the reference shows",
    "message-site census (per-id ldr counts against the assembled reference) is the body-merge oracle: it pinned the def merge to the DEF_UP pair and exposed the crit text arithmetic",
    "DEF_UP1 assigns its turns offset late; the shorter range wins the call-saved register, the body differentiates, jump2 stops merging -- the def bodies split",
    "the crit text is MSG_CRITICAL with += 1 for the player side; 0x823 is never loaded, matching the reference's single load and adds #1",
    "def-down pair splits with late turns offsets at both sites: all five 154-builds present",
    "pool-value census is fully clean (222=222 words, every 0x8xx and offset value loads equally); remaining deficits are copies, shared push tails, and seven alignment nops",
    "modifier family early/late map complete: ATK_DOWN1 and ATK_UP2 hold offsets across the pushes, the other six store plainly -- six pair-combinations measured, DOWN2+UP1 decisive",
    "permuter walk finds: two init orderings in the summon block adopted; the walker's reachable neighborhood at current weights is clean after ~53k evaluations",
    "PP-damage case reuses power for range*4 before the range!=4 test (reference computes lsls #2 unconditionally and spills to [sp,#12]); found by pret decomp-permuter in a 5-minute run, verified against the reference bytes",
    "summon walk holds (u8*)slots in a function-scope u8* local assigned at the loop head; the cast+type is the spelling that survives copy propagation (pret find, 9th spelling tried)",
    "the five ailment-family guards compare action->effect directly rather than the cached efx local (perm_expand_expr find, 23 rows)",
    "BytePtr, a gnu89 inline identity u8* helper over 13 raw-offset access sites: forces each base pointer through a call boundary; worth 35 rows on the cascade stack; likely a proxy for proper struct member access",
    "act, a second BattleAction pointer covering a 13-site scatter (target_flags, five effect reads, the classify pair, the switch head, and four power reads); worth 15 rows; one TAKE_PWR site expands with act while the other keeps the macro",
    "ATK_DOWN1 assigns turns=7 between the modifier decrement and the clamp, and stores the local",
    "ELEM_AT spells the stride as (range) * 2 * 2",
    "DK_ATTACK_X reads hp through an s16 intermediate declared after power, loaded before defense",
    "type-8 HP damage multiplies dmg * falloff, operand order",
    "the agility modifier local is u8",
    "PP drain display delta is dmg = heal; dmg = dmg - actor->pp; in two statements",
    "EFX_PP_RESTORE_7 loads pp before max_pp",
    "three more multiply operand orders: dmg * PpDmgFalloff, target->max_hp * kind, dmg * HpDmgFalloff",
    "EFX_AGI_SET_DOWN4 stores through an s8* local assigned before the value block",
    "EFX_RES_UP2 clamps through an s8* local assigned inside the first compare (ADJUST_RES expanded at this one site)",
    "HpHealFalloff base held in a s32* local assigned right after power=100, read as healtbl[offset]",
    "two more multiply operand orders: dmg * PpLossFalloff, dmg * healtbl",
    "GUARD1 sets guard_level through a local (g1 = 1)",
    "the hp store tail (dealt/store/recalculate) is duplicated plainly in the heal case, case 6, and case 3 -- no goto: crossjumping merges the suffix after each body's own ldrsh, which is exactly the reference's mid-statement share point; push census now 154=154 and the size gap is one pool word",
    "PpHealFalloff held in a s32* local (pptbl) assigned after the hit selection, before the +206 guard (10 rows)",
    "scan idx spelled ((n << 1) << 3) + 748; DK_PP_DRAIN's off computed in two statements (off = range*4; off += 72) giving the three-operand lsls; RES_DOWN2 clamps with -4 > x; the sleep guard masks with 3 & rand -- individually byte-neutral, kept as cascade-matched spellings",
    "EFX_DRAIN_PP holds old pp in a local and the clamp arm computes dmg = heal - old from the copy (ref r2 at 0x1568), not a field re-read",
    "HpDmgFalloff8 held in a second table local beside pptbl",
    "EFX_PP_LEECH's clamp re-reads target->pp through union Cell (fresh ldrsh where CSE would fold)",
    "PP-heal falloff multiply is dmg * pptbl[offset]",
    "DRAIN_PP clamp splits the delta: dmg = heal; dmg = dmg - old;",
    "DK_HP_DMG_9 reads hp into a u32 local before pass=1 (hpu), copied to cur after",
    "elem arm 2 rebuilds through BytePtr: entry copy lands at the loop head and the .L5 head reads [r4,r2] as the reference does; the ref's 2-insn rebuild (copy target; adds #36) still folds to a 1-insn tbl copy in every spelling tried (plain, tbl2 between arms, cast-copy chain) -- cse unifies plus(target,36) through register quantities",
    "classified via the sched2 dependence table: the x100 div-arg placement family ties at priority 71 and our movs #100 fills a load-latency stall the reference fills with allocation-born copies -- tie-locked behind coloring, the documented class",
    "swarm harvest (five verified families, hand-stacked): the modifier family stores through REAL struct members (attack/defense_modifier_turns = 7) -- BytePtr was a proxy exactly as recorded; ADJUST_ATKDEF expanded to plain statements at ATK_UP2",
    "the falloff-8 arm reads HpDmgFalloff8[offset] directly; the dmgtbl8 table local is gone",
    "DRAIN_PP uses a fresh amt local (not the outer dmg) and the clamp arm derives the display delta in one expression re-reading actor->pp",
    "the elem scan's second direction compares ELEM_AT(target, i) indexed by the loop counter -- no walked pointer; value is s32",
    "SEMANTIC FIX, reference-proven at 0x2ac-0x2b0: action 0x1f7 IS the split action -- the SPLIT_OFF/APPEARS binding was inverted in the draft",
    "the percentage heal adds Math_Div per arm (no amt join variable)",
    "heal-body power guards (cases 2 and 12) read the FIELD and assign pwr after: if (act->power == 0) break; pwr = act->power; -- CSE keeps one ldrh, the guard consumes the load register (ref ldrh r3/cmp r3 rows now match), and pwr materializes as the reference's copy insn. The five sibling bodies already used this idiom; the old TAKE_PWR single-variable shape loaded straight into pwr's register with zero copies, which the reference disproves. act vs action mention choice at both sites is emission-neutral (all four combinations byte-identical)",
    "attack KO arm is KO-first (if (cur <= 0) {...} else FINISH), mirroring case 3: jump2 then empties the attack FINISH arm into case 3's shared copy (b .L at the cmp) and the whole KO arm matches the reference row-for-row INCLUDING register texture (mov r3,sl / movs r6,#0 / bls polarity / per-arm FELLED ldr jumping into the shared movs #4 push). The 'FINISH-first measured better' rejection was landscape-dependent and is reversed; the FELLED crossjump cluster, the bhi/bls polarity, and the extra FELLED pool load are all resolved by this one arm-order fact",
    "heal bodies route power through dmg exactly like the three CalcPower siblings: dmg = act->power; dmg = Battle_CalcRestore(dmg, ...) -- pwr is gone. dmg's long-lived callee-saved home (r5) generates the reference's full copy chain (adds r5,r3,#0 from the guard load, adds r0,r5,#0 at the call, adds r5,r0,#0 for the result) and both heal-head clusters close row-for-row including register texture. Found via the greg/flow2 RTL dumps: pwr's separate 2-ref pseudo took r0 by arg-copy preference and reload_cse collapsed the chain; dmg's pseudo cannot take r0 because it crosses the Math_Div calls. The recorded expressed[60] sibling pattern was the answer all along"
  ],
  "unexpressed": [
    {
      "op": "attack-KO FELLED arm share",
      "count": 9,
      "where": "ref loads FELLED twice (attack's arm jumps into case 3's suffix through the pushes to hp_tail); ours three times. Suffixes are source-identical; the merge waits on register identity"
    },
    {
      "op": "def-body merge",
      "count": 9,
      "where": "same gate"
    },
    {
      "op": "walk head",
      "count": 7,
      "where": "ip copy, second 100, 255-cache; ~15 spellings measured"
    },
    {
      "op": "scan-2 rebuild and 077188/077190 scheduling",
      "count": 10,
      "where": "texture"
    },
    {
      "op": "remaining copy/pair texture",
      "count": 52,
      "where": "register identity following the above"
    },
    {
      "op": "single-return epilogue",
      "count": 1,
      "where": "our build emits pop/bx r1 plus a dead bx lr; the reference ends with one return; neither return-type nor return-statement changes explain it yet"
    },
    {
      "op": "bl Func_080bbabc",
      "count": 3,
      "where": "jump2 merges three text/dealt push copies the reference keeps; the reference's two dealt-heads color differently (r2 vs r4) which blocks its merge chain; ours color identically; root cause is per-body register pressure"
    },
    {
      "op": "adds #36 and movs #100",
      "count": 3,
      "where": "early elem scan keeps target+36 unCSEd and the summon walk materializes off=100 in a register; both resist direct respelling"
    },
    {
      "op": "coloring bifurcation",
      "count": 1,
      "where": "case-12 heal head: the reference colors the range copy into r0 and holds pwr through r3->r5->r0 (two explicit copies, adds r5,r3,#0 and adds r0,r5,#0); ours colors pwr directly into r0 and the range copy into r5. This single r0 assignment flips the dealt-head scratch (r4 vs r5 at insn row ~711), which is the register-identity gate on the FELLED crossjump cluster. The lever, if one exists, is whatever makes pwr lose r0: an extra ref on the range mention or a clobber across pwr's range. Every ternary/order respelling ties. ALLOCNO DATA (our build): pwr is pseudo 384, 'used 3 times across 8 insns; set 1 time; user var; pref LO_REGS'; it allocates mid-order in greg's priority list and takes r0 as the first non-conflicting register in REG_ALLOC_ORDER {3,2,1,0,...}. The reference's equivalent faced one additional conflict at its turn and landed r5. The lever is the conflict set at 384's allocation, i.e. which OTHER allocno spans that window -- a global-pressure question; every local respelling of the head (act vs action, ternary arms, load order) leaves the conflict set unchanged and ties. FINAL DEPTH: both builds occupy the same six registers {r0,r1,r3,r4,r5,r6} in the window; there is no extra conflict. The roles swap because the ALLOCATION ORDER differs: greg's priority list (';; 180 regs to allocate: ...') ranks pseudos by floor_log2(n_refs)*n_refs/live_length*size, and which of (pwr=384, the action reload, the range copy) allocates first decides who gets r3 and cascades to the FELLED crossjump gate. The next instrument is a differ over the two builds' allocate-order lists -- which needs the reference's pseudo structure reconstructed, i.e. a byte-exact-in-progress problem. Alternatively: any source change that alters 384's n_refs or live_length by one unit reorders it; the honest candidates are which mention of pwr exists (macro vs expansion) at EACH TAKE_PWR site jointly, not per-site. || SESSION UPDATE: the lever's first half is FOUND -- guard-on-field (now folded) gives pwr a copy-def pseudo and the ldrh/cmp rows match; residual is the one-vs-two copy depth and the r0 role swap, unchanged under -fno-regmove (allocation order, not regmove)"
    }
  ],
  "verified_against_reference": [
    "seven-instruction register-save prologue matches exactly"
  ],
  "blocked_by": "not blocked -- barely started",
  "rejected": [
    {
      "shape": "APPLY_PP_HIT compare-first in cases 0/11",
      "wrong": "reference subtracts then floors at zero"
    },
    {
      "shape": "HEAL_CUR cap-before-add in cases 2/12",
      "wrong": "reference adds, caps, re-derives display delta from the field"
    },
    {
      "shape": "walk pseudos jsave/probe/woff matching ref lr/ip + second 100",
      "wrong": 1788,
      "note": "coloring churn +306; ref provably has them, retry after other regions settle"
    },
    {
      "shape": "goto loop to suppress strength reduction",
      "wrong": 1574,
      "note": "goto did not stop SR; walker persists either way"
    },
    {
      "shape": "scan-2 walking tbl itself to break the CSE fold",
      "wrong": 1826,
      "note": "fold persists, plus churn"
    },
    {
      "shape": "muls operand order via source operand order (kind*max vs max*kind, dmg*tbl vs tbl*dmg)",
      "wrong": 1552,
      "note": "emission order is unchanged by source order -- rtx canonicalization picks it from pseudo numbering; the two sites stay swapped vs ref. switch vs else-if for the 5/6/8 falloff is also emission-identical"
    },
    {
      "shape": "walk pseudos jsave/probe/woff, second attempt after slots+r6 converged",
      "wrong": 1702,
      "note": "copy-prop folds all three again (no lr/ip in output); ref's movs lr,r4 / movs ip,r1 must come from reload or a source shape not yet found. Region is ~15 rows"
    },
    {
      "shape": "walk probe recomputed from work in the else-arm",
      "wrong": 1555,
      "note": "gcse folds it; output byte-identical"
    },
    {
      "shape": "volatile 255-probe in the walk",
      "wrong": 1577,
      "note": "blocks too much, +13 normalized rows"
    },
    {
      "shape": "def-body split via local pressure",
      "note": "toff allocation is the global allocno lottery: our atk toffs held r6 at commit 5d4afa4e6 and lost it again after unrelated edits; ref itself is mixed 3-early/5-late across the eight bodies. The split follows global coloring convergence, not local spelling"
    },
    {
      "shape": "-fno-gcse routed for the whole TU",
      "note": "fixes the walk fingerprint (single ldrsh, ip pseudo appears) but is globally worse: 872 vs 742 normalized bin rows. The reference was compiled with gcse on; its no-fold shapes have another source"
    },
    {
      "shape": "scan probe via BattleWorkPtr cell re-read",
      "wrong": 1455,
      "note": "best global scores ever, but the emitted loop reads the cell where the reference reloads its spilled work local -- wrong instructions aligned by luck, rejected"
    },
    {
      "shape": "volatile on the walk 254-recheck",
      "wrong": 1565,
      "note": "walk window 26->24, global flat; compasses disagree, parked"
    },
    {
      "shape": "permuter campaigns",
      "note": "16,500 candidates over halfword, instruction, and coloring-weighted fitness: zero improving mutations. Improving moves are coordinated multi-statement changes"
    },
    {
      "shape": "walk-engine permuter, 30000 candidates on the 66-row landscape (seed 77, keep-prob 0.6, pret walk engine)",
      "wrong": "no improvement: best=baseline 1146 rows / 2885 halfwords; 0 compile failures; run out/perm-w27"
    },
    {
      "shape": "pret find #2: function-scope s16* temp for slots base in the summon walk",
      "wrong": "absorbed by the power fix; identical insns on the new landscape"
    },
    {
      "shape": "cascade extras: hoisted text-id local (8f3), kind=(dummy=12) chain, affinity reuse at the agility push",
      "wrong": "marginal value 0 to -2 rows on the 313-row cascade stack; not folded"
    },
    {
      "shape": "cascade round-3 moved Summon_Refresh() from the summon case into DK_HP_HEAL for a 21-row gain",
      "wrong": "the reference bl at .L19 sits directly after the summon walk; the gain was alignment luck; cascade promotions now check the bl sequence"
    },
    {
      "shape": "return 0 at the fall-off end",
      "wrong": "264 rows, adds a movs r0,#0 the reference lacks"
    },
    {
      "shape": "void return type (header + definition)",
      "wrong": "267 rows; the dead trailing bx lr persists either way"
    },
    {
      "shape": "power = range * 4 before TAKE_BONUS in DK_PP_DMG (pret 18240)",
      "wrong": "disproved by .L64: the reference computes lsls r3,r1,#2 INSIDE the branch and reloads power from [sp,#12]; the 66-row gain was phase; the [sp,#12] spill actually belongs to the early elemental-power selection"
    },
    {
      "shape": "heal == (u16)actor->max_pp (pret fan find)",
      "wrong": "disproved by the normalized hunk at 2189: the reference reads the field signed (movs #54; ldrsh); the 63-row gain is phase compensation for three crossjump-merged pushes"
    },
    {
      "shape": "actor->pp = (s16)(heal & 0xFFFFFFFFFFFFFFFF) (cascade)",
      "wrong": "forces a copy the reference does not have: ref keeps heal in r5 with no copy insn; 7 rows of coloring imitation"
    },
    {
      "shape": "actor->hp store moved inside the heal==max arm (cascade)",
      "wrong": "worth 76 rows, ALL phase: every reference 0x821 site stores after the join (f1a, 15a8); a store-move lie invisible to the bl guard"
    },
    {
      "shape": "dmg = (heal = dealt) in EFX_DRAIN_PP (cascade)",
      "wrong": "ref 0x156a adds r5,r5,r6 is heal += dealt; the 92-row marginal was phase again"
    },
    {
      "shape": "text id 0x85f held in a char* from function start (cascade)",
      "wrong": "removing it from the stack IMPROVED the score by 2"
    },
    {
      "shape": "heal = (dmg = dealt) in DRAIN_PP (cascade, second spelling of the same clobber)",
      "wrong": "same lie as cycle 7; the permuter keeps rediscovering it because the phase gain is ~86 rows"
    },
    {
      "shape": "cur = 0 moved after break (cascade)",
      "wrong": "dead-code deletion of the KO zeroing; third lie form: value-flow changes invisible to both guards"
    },
    {
      "shape": "actor->pp = (s16)actor->max_pp after the join (cascade cycle 10)",
      "wrong": "wrong on the else path; 4 rows of phase; denylisted"
    },
    {
      "shape": "if/else through a temp for the range==4?100:power ternary",
      "wrong": "280 vs 272; the ternary stands"
    },
    {
      "shape": "two-pointer elem read (row = target + range*4; value = row+38)",
      "wrong": "274 vs 272 in both statement orders; the ref keeps adds #36 by another means"
    },
    {
      "shape": "first elem arm walks tbl destructively to force the second arm's +36 rebuild",
      "wrong": "278 vs 272, and the second arm's head compare reads tbl after the walk -- semantics broken; the ref's exact pointer lifetimes at .L3/.L6 still unread"
    },
    {
      "shape": "cascade cycle 11 stack",
      "wrong": "every mutation semantically false: (u16)max_hp compare, (u8)heal store truncation, clamp delta moved out of its arm, empty tautology if"
    },
    {
      "shape": "long-lived pooled-text locals (split_text, wake_text) as conflict-adders for pseudo 384's window",
      "wrong": "274 vs 272 both; a pooled constant spanning the function spills rather than conflicting; the reference's extra conflict at 384's turn is still unidentified"
    },
    {
      "shape": "field loads hoisted above the pwr guards (both heal bodies)",
      "wrong": "net -4 rows but locally false: the reference's hp load is in the fallthrough block after the guard's bne, a block boundary the scheduler cannot cross; the r4 action-reload flip it caused must have another driver"
    },
    {
      "shape": "dropping the maxu volatile pun in the percentage heal",
      "wrong": "301 vs 272: the reference carries both u16 reads; the earlier hunk was read with the diff sides confused"
    },
    {
      "shape": "pun order swap (old, heal, maxu, maxv)",
      "wrong": "byte-identical; CSE-neutral"
    },
    {
      "shape": "summon-walk head check with literal 100 plus a separate off=100 (the reference's double materialization, confirmed: movs r2,#100 AND movs r0,#100)",
      "wrong": "structurally right (+3 insns toward parity) but every placement measured: else-arm 281, cluster-head/mid/tail 607 -- the in-cluster position the reference shows recolors half the function; needs multi-site convergence first"
    },
    {
      "shape": "search instruments on the fixpoint: shape_sweep --pairs (9 shapes, 1 compiled, no improvement) and --descend (1019 compiles, converged; its one 'win' is a negated arm swap that scores 285 rows and breaks size parity on the canonical measure)",
      "wrong": "both bounded searches are dry on this owner; the descend driver's internal region metric is not the linked-byte compass"
    },
    {
      "shape": "simultaneous fold of the two proven structures (double-100 in-cluster + amt->tmp identity)",
      "wrong": "insn count reaches 2481/2483 (best ever) but raw rows 608 and normalized 45/40 both degrade: the pair alone does not converge the coloring; the storm needs the remaining tie-classified texture rows resolved too, which requires reading beyond what their dependence-table ties currently permit"
    },
    {
      "shape": "double-100 as two source mentions (both placements, alone and with the tmp identity)",
      "wrong": "all variants overshoot to 6340 (+8): the reference's movs r2,#100 + movs r0,#100 is reload REMATERIALIZING one constant pseudo at the head use, not a second mention; the committed single off=100 is structure-true and the split is a coloring artifact"
    },
    {
      "shape": "run30-r3 TEXT-opcode local (BATTLE_EVENT_TEXT held in a register across ~40 push sites)",
      "wrong": "reference builds movs r0,#4 at 74 sites within two insns of the bl (102 total movs #4); the opcode is rebuilt per site, never held. r3's biggest family was pure phase compensation"
    },
    {
      "shape": "run30-r3 Summon_FindSlot call hoist above the classify branch",
      "wrong": "the reference's bl Func_080b7514 sits INSIDE the taken arm (.L13, after the +206 classify bls); the hoist is a call-move lie the baseline-relative guard let through"
    },
    {
      "shape": "run30-r3 classify hoist (count = (u8)(act->effect+206) before hit selection)",
      "wrong": "reference computes the classify at 0x1a2, after the hit join .L12, exactly where the draft has it"
    },
    {
      "shape": "run30-r3 HitFalloff base local",
      "wrong": "reference loads the pool base fresh at the call site (ldr r2,[pc,N] directly before ldrb r2,[r2,r4])"
    },
    {
      "shape": "adjust load reordered before range (prologue order matches ref emission)",
      "wrong": 1039,
      "note": "emission does not follow source order in that window; scheduling owns it"
    },
    {
      "shape": "elem scan direction-2 as indexed tbl mention (v3) and as q-walker (three spellings)",
      "wrong": "indexed: giv init adds r2,r4,#2 -- the +2 still folds into the giv. q-walker: idx 2 and the walk shape appear (movs #2, [r2,r4]) but the preheader copies live tbl in one insn where the reference rebuilds target+36 in two; gcse unifies plus(target,36) through the live register in every spelling including a fresh (s16*)((u8*)target+36) init. cmd-reuse colors the walker into fp. Parked: walker+idx2 proven true, the init rebuild is the unsolved half. Grouping constants in source cannot help: RTL plus canonicalization rebuilds (t+4i)+38 regardless"
    },
    {
      "shape": "HEAL_60 pun orderings (maxu-first, hp-pair interleave, maxu-last) hunting the reference's direct ldrh r6 maxu load",
      "wrong": "2696 all; maxu still loads through a temp with an explicit copy; the swap variant's -2 hunk weight is unrelated noise"
    }
  ],
  "note": "Largest target in the list. No permanence marker anywhere in the span -- no Thumb multiple-transfer, no SWI, no veneer idiom, nothing in asm/classification.json -- so every byte is reachable as C.",
  "parking_lot": [
    "Pascal 2026-08-19: reduce the tooling from 162k lines to ~40k. Biggest masses: coverage-map 9.3k, permuter 5.9k, build-assets 5.4k, full-c-progress 4.5k, per-asset data crates ~2-4k each x dozens. Likely levers: merge the ~50 asset-builder crates into one table-driven builder, collapse the metrics/coverage stack, dedupe the disasm/canonicalizer copies across candidate-show/overlay/permuter. Separate campaign; do not start mid-owner.",
    "port pret's reuse-existing-variable temp mutation (perm_temp_for_expr with existing-var reuse) into alchemy_permuter; measured gap: 75k of our candidates found nothing, pret found the power=range*4 fix in minutes"
  ],
  "notes": [
    "cascade base sits 9 rows lower (254) via no-op block wrappers that perturb jump2's FELLED crossjump topology; semantically identical to the draft; the real source shape behind that CFG is still unread",
    "cascade campaign closed after 11 cycles: ~30 verified source facts folded (526 -> 272 rows, size parity), but cycles 9-11 yield only semantic lies in fresh spellings (u16/u8 width casts the reference reads signed, clamp arms hoisted, dead-code moves). The remaining 272 rows are coloring/crossjump topology; the named reading targets are the elem-scan pointer lifetimes (.L3/.L6), the FELLED merge pressure cluster, and the summon walk's off=100 materialization.",
    "state of the residual after exhaustive single-site probing: 272 raw rows / 40 normalized at full size parity. Every measured single-site change now either ties, degrades, or wins by a compensation the reference disproves. The residual is an interlocked global-coloring fixpoint: sites are individually correct-or-tied and the remaining differences are role assignments the allocation ORDER decides. Progress from here comes from finding sites that are still WRONG (not tied) -- the honest unread list: the summon-walk off=100 materialization, the elem +36 rebuild, and the ordering texture the sched2 dependence tables can classify as reachable or tie-locked, body by body, per the CONTRIBUTING method.",
    "mode_sweep: all approved flag variations score identically (2329hw at size parity); its classifier tags the residual [register=53,literal,cfg]. Every instrument in the catalog has now been aimed at this owner: candidate-show, census, both permuters, shape_sweep both modes, mode_sweep, RTL dumps at cse/combine/lreg/greg/sched2 with dependence tables, and the compiler source. The residual is flag-invariant pure allocation.",
    "combinatorial sweep over the five structure toggles completed: the raw-row optimum (268) uses the two block-boundary-disproven hoists; every reference-true combination scores 272-282; the committed draft is the truth-maximal point of the swept space",
    "independent fresh-context re-read (second reader): all 18 normalized hunks re-derived against the reference with no priors; verdicts match the record exactly (documented unexpressed items, jump2 register-identity gates seen from both directions at @591/@1641/@1741, reload/scheduler texture). Seven new hypotheses measured 272-292, none kept. New detail: @604's bls/bhi is a branch-range inversion of the same compare, and the dead trailing bx lr classifies as reload texture. The fixpoint conclusion is now two-reader confirmed.",
    "callee return-type sweep: BattleEvent_Push flipped to void scores 364 (u32 is measurement-confirmed; the ROM's coloring needs the r0 set at all 154 sites); Recalculate/UpdateRatios/Math_Div flips are byte-neutral. The interface layer is pinned. This exhausts the last mechanism-matched lever reachable from inside this translation unit.",
    "CORRECTION to the adjacent-reconstruction idea: translation units compile independently, so neighboring owners cannot move this owner's bytes -- only this TU's source and its headers can, and the headers are measurement-pinned. Most callees are 10-line veneers (permanent assembly) anyway. The fixpoint is fully internal: byte-exact requires an in-TU source shape both independent readers missed, or acceptance that the residual sits in the same measured-unreachable class as the six recorded scheduler-tie owners, at scale.",
    "rematch (AST-engine walk vs pret, same base, ~5 min): our engine ran 4000 candidates in 37s at 4.3% failure (vs pret's ~85 candidates per instance) and its best find, 1975hw at parity, is a break-move lie (kills dmg/=2 in the guard arm and falls case-tail into EFX_HEAL_60). The engine wins on speed and validity rate but lacks the cascade's bl/store guards; both searches now surface lies preferentially, confirming the honest gradient is exhausted. Wire the guards into the walk before any future run.",
    "100k-candidate guarded walk (largest search ever run on this owner, 16 min, 4.6% failure rate): best find 1618hw at parity decomposes to a Div-into-comparison move (301hw alone, semantically false: dmg keeps its untithed value downstream) plus interaction; the accompanying pointer-temp is byte-neutral in isolation. The jump guard held (no break moves in 100k candidates); the search now surfaces expression-motion lies instead. Total candidates across all engines this campaign: ~700k; honest finds stopped arriving after ~30.",
    "chained-walk key catalog (search state out/longwalk-N/best.c, NOT folded): round2 heal=(u16)actor->pp [409hw], round3 volatile u16 local for the defense-clamp 4 [820hw], round4 pp=(u16)target->pp [100hw]; now at 1000hw/470wrong from 2329/982. All three keys are value-preserving pseudo-class changes (zero-extend defs, memory-resident constant). Blunt u16 header flip for pp/max_pp measures 2831hw -- the fields are s16 with per-site unsigned mentions, the documented dual-read pattern; the walk is enumerating those sites. Locally each key's load width contradicts the adjacent reference read, so none is foldable yet; the open question is whether the true source's mention structure produces both the ref's widths AND these classes, or whether the keys proxy a different global driver.",
    "extended stock-flag sweep beyond the approved matrix (13 flags, raw-object probe): every deviation is neutral or worse except -fno-regmove, which scores 2152hw vs 2329 at exact size on this draft. It is NOT the original's flag: 2 of 25 exact owners change under it (they are exact with regmove on, so the original had it on). Its 177hw gain marks sites where OUR source shapes trigger regmove coalescing and the original's did not -- usable as a targeting map: diff the standard vs -fno-regmove emissions and read the differing sites against the residual.",
    "stock 2.96 test: rebuilt gs1cc with the pristine ASM_OUTPUT_ALIGN (the fork's only byte-moving deviation; all other fork commits are host-buildability, proven inert by the Red Hat i386 cross-check and the hash padding test). Score on this owner: identical, 2329hw/982wrong at 6332. The residual is a property of stock gcc 2.96, not of the fork. Chain rounds 4-5 plateau at ~1000hw search state.",
    "permuter A/B on the 998hw chain state, 30k candidates, same seed: control best 963hw vs --heat best 875hw (88 halfwords deeper, 2 rows better). Heat-biased site selection (LCS-backtracked differing-row positions weighting TempForExpr and ReorderStmts) is adopted as the default for chained runs; determinism with heat off is hash-identical to the pre-change engine. Cross-round journal import (--journal-from, backend-identity-keyed) landed alongside.",
    "the 100-matcher swarm cost ~26M agent tokens and its verify phase died at a usage limit; hand-harvest of its journal took the committed draft 2329 -> 1035 halfwords in under an hour of direct work. Eight matchers independently converged on the DRAIN_PP shape; convergence was the strongest usefulness signal in the claim list.",
    "run30 chain sweep (out/run30-r1..r3): guarded descent 2329->1007->902->759 as pre-harvest search state. r3's 143hw round includes a Summon_Refresh call move that passed the bl guard because thresholds are baseline-RELATIVE (a move can hide under existing divergence) -- guard gap for the engine owner. r3's honest remainder is interlocked coloring families (constant-72 local, call-result locals, a guard-level pointer spanning the switch) that do NOT transplant onto the harvested draft (frame +4, 2637hw when ported piecemeal); superseded by the harvest at 1035. The r4-* directories are the engine owner's test matrix, untouched.",
    "engine hardened (this session): bl/store guards now carry row signatures -- at equal divergence a candidate must have IDENTICAL call sequence and store multiset to the baseline, so lateral trades (the r3 Summon_Refresh class) die in the walk, in retention, in best.c selection, and in chain re-seeding. Journal bumped to v3 (legacy v2 rows import with unknown signatures). Guarded walk from the 1035 draft: 100,000 candidates, 712 compile failures, ZERO improving mutations -- the AST engine's honest single-lineage neighborhood at that baseline was exhausted; do not spend walk compute again until a structural fold lands",
    "run30-r3 759hw decomposed by reference reads: every named family was either already-committed, phase compensation, or a lie (chance+=30 moved after its use, heal+=dmg deleted in DRAIN_HP, double heal+=dmg in DRAIN_PP, amt=maxv dropping *30, p reset inside the walk loop, stale power/skip cross-case reads, three call moves). The vein is fully mined and disproven; nothing left to harvest from run30",
    "-fno-regmove targeting map recomputed on the guard-fold draft: exact size parity returns and 730hw/427wrong materializes, proving the guard fold's content. The two insns regmove removes (restoring parity) sit in the walk head (ch&0x7fff copy chain) -- meaning the true source sheds ~2 insns at our-extra sites instead. The named our-extra sites: the attack FINISH triple at 0x54c (movs #11/mov r1,sl/bl -- the reference's attack FINISH arm is an empty block jumping to case 3's shared copy at 0xb28), the KO-arm FELLED pool load + movs #4 at 0x56c-0x574 (bhi/bls polarity), and the epilogue dead bx lr. At parity the case-3/6 suffix region 0xb00-0xb64 ALREADY matches the reference byte-for-byte including cur in r6, so the old register-identity gate on the FELLED merge is GONE: what remains is why our jump2 does not empty the attack arm into the 0xb28 copy -- the next single reading target, likely interacting with the bhi/bls range polarity of the arm's TEXT_SIDE",
    "heal-head residual after the guard fold: ours emits ONE copy (adds r0,r3,#0 -- pwr coalesced into the arg) where the reference emits two (adds r5,r3,#0 then adds r0,r5,#0) with the range copy in r0 (ours r2). Same under -fno-regmove, so it is allocation order, not regmove: in the reference the range copy takes r0 first and pwr settles in r5, forcing the arg copy. The remaining lever is what makes the range copy prefer or win r0; every local respelling measured ties",
    "walk head at parity (v2nr): ref extras are movs r0,#100 at 0x218 (the documented reload rematerialization of the off=100 pseudo at the store use) and the rec flow cmp r0,#0 before the copy (ref compares the raw FindSlot result then copies, ours copies then compares) plus adds r3,r2,#0 at 0x30e; DRAIN_PP head loads target->pp straight into r6 (ldrh r6,[r7,#52]) where ours loads r3 and copies -- the longwalk u16-key family points here; both are read-verified leads for next session",
    "post-KO-first hunk inventory (the complete remaining distance, 36 hunks/48 rows): elem-scan idx split 0xec/0xf6 (parked, walker+idx2 proven); walk head 0x218-0x30e (double-100 rematerialization, ip/r4 order, rec compare-before-copy, adds r3,r2); heal-head copy depth at 0x648/0x990 (ref adds r5,r3,#0 + adds r0,r5,#0 vs our single coalesced copy, plus pool-base register roles); nibble reload slot texture 0x882-0x896; movs r6,#0 position 0x962; cmp #32/pool order 0xa94; DRAIN region head 0xe0e-0xe1e where the reference loads the u16 field straight into r6 (ldrh r6,[r7,#52]) and keeps r4 for r8 while ours loads r3 and copies -- the longwalk u16-key convergence site, first candidate for the next read",
    "allocation-order endgame, read-verified this session: the three remaining insn-count families are one mechanism. (1) heal heads: the reference's range copy holds r0 across the ternary compare, which conflicts pwr out of r0 and forces the r3->r5->r0 two-copy chain; ours gives the range copy r2 (the natural first-free scan), pwr ties to r0, one copy. (2) HEAL_60: the reference loads maxu straight into call-saved r6 while our load temp fails to coalesce -- downstream of the same ordering. (3) walk head double-100 is reload rematerialization, also allocation-owned. No local respelling reaches these: the next instrument is the greg allocno-order dump (scratch/rtl toolchain) on our build to identify which pseudo's priority must shift by one unit, then find the source mention that shifts it. Everything else in the 36-hunk inventory is either this class or the two parked reads (elem init rebuild, walk-head rec flow)",
    "RTL-dump instrument validated end to end this session: -da on the staged xgcc, read lreg costs/usage, flow2 post-reload form, and the greg allocation order line. The chain pwr(526)-used-2-across-7 -> r0-by-preference -> reload_cse collapse explained the missing copies and pointed to the dmg routing. Remaining inventory after the fold (24 hunks/33 rows): elem idx split (parked), walk head (double-100, ip/r4 order, rec flow, adds r3,r2), nibble reload slot texture 0x886, movs r6,#0 position 0x966, pptbl pool-load position 0x9ac, cmp #32 order 0xa98, two isolated beq inserts 0xc14/0xc94 (verify: possibly pool decode), and the HEAL_60 maxu head 0xe12-0xe22 where the reference schedules heal's ldrsh (scratch r6) before the maxu load so maxu lands r6 directly -- retry the pun orderings on THIS landscape, the earlier byte-identical sweep predates both folds"
  ]
}