{
  "schema_version": 1,
  "namespace": "main",
  "address": "0x08004838",
  "status": "proven-retained",
  "owner_bytes": 32,
  "executable_owner_bytes": 32,
  "candidate_bytes": 32,
  "differing_halfwords": 4,
  "wrong_instructions": 6,
  "classification": "thumb_multiple_transfer_module",
  "cross_edition": {
    "editions": [
      "ja",
      "en",
      "de",
      "es",
      "fr",
      "it"
    ],
    "relocation_normalized_core_bytes": 16,
    "core_diff_bytes": 0,
    "core_identical": true
  },
  "bounded_search": {
    "mode": "classic-walk",
    "candidates": 20000,
    "exact": false,
    "best_differing_halfwords": 4,
    "best_structural_rows": 5,
    "perm_general_statement_order": {
      "choices": 6,
      "attempted": 6,
      "best_differing_halfwords": 4,
      "note": "All 6 field-write statement orders tried via PERM_GENERAL over dma->source/destination/control (out/permute/08004838/report.json); the retained order (control, destination, source) is the unique floor at 4 -- every other order regresses to 6 by scrambling which hard register (r0/r1/r2) holds which field value relative to the reference."
    }
  },
  "notes": [
    "Publishes a source address, palette destination, and transfer control word through DMA3. The current standalone canonical score is 32/32 bytes, 4 differing halfwords, and 6 wrong instructions (matched_prefix=4: all four literal-pool loads into r3/r0/r1/r2 already match the reference byte-for-byte).",
    "The retained owner is already covered by the proven thumb_multiple_transfer_module matcher: its standalone three-register STMIA cannot be emitted from ordinary approved C by the target compiler. Confirmed directly against this route's backend (alchemy-gcc/gs1cc/gcc/config/arm/arm.c thumb_output_move_mem_multiple/thumb_expand_movstrqi and arm.md movmem12b/movmem8b/store_multiple): Thumb's only multi-register-stmia-emitting pattern (movmem12b/8b) always prints a paired ldmia immediately before the stmia as one indivisible output_asm_insn pair for a genuine memory-to-memory block move, and the dedicated ARM store-multiple expander/peepholes are gated TARGET_ARM only with no Thumb equivalent -- so a lone grouped stmia sourced from three independently computed register values (as the reference has, with no preceding ldmia) is unreachable from any ordinary C shape on this compiler route. A local struct-temporary copy and a static-inline struct-by-value-returning helper both confirmed empirically: each instead lowers through the paired movmem12b shape via a stack-materialized 12-byte object (extra sub sp/add sp the reference does not have), never the reference's unpaired form. No duplicate permanent classification was added.",
    "The remaining 4 differing halfwords are exactly the reference's stmia r3!,{r0,r1,r2} / subs r3,#12 / (trailing pool-alignment nop movs r0,r0) vs. this candidate's three discrete str's / (no alignment nop needed, since its 8-halfword body is already 4-byte aligned before the literal pool while the reference's 7-halfword body needs the pad) -- a compiler-level residual, not a source-shape mistake.",
    "2026-08-27: reclassified measured-draft -> proven-retained. This owner's retained assembly is already independently proven unreachable from ordinary C at the manifest/classification level: out/gs1-en/full/asm/manifest.json marks it kind=thumb_multiple_transfer_module, retention starting with keep_, confidence=proven, citing arm_md/arm_c line ranges showing GCC 2.96's Thumb backend can only emit a multi-register stmia paired immediately with a matching ldmia (movmem8b/movmem12b), and that ARM's standalone store_multiple/stmsi patterns are TARGET_ARM only -- a lone stmia from independently-computed register values (as this owner's reference has) is architecturally unreachable on this route, not an unexplored search space. This specific pattern was independently rediscovered by multiple dedicated passes this session (08004838, 0800bc48, 08003e10, 0808fecc, 080c0184, and partially 080e7404) before the shared manifest-level proof was found; recording the status correction here so future sessions don't repeat the rediscovery."
  ]
}
