{
 "schema_version": 1,
 "namespace": "main",
 "address": "0x08095dd0",
 "status": "proven-retained",
 "owner_bytes": 460,
 "executable_owner_bytes": 460,
 "candidate_bytes": 460,
 "differing_halfwords": 5,
 "wrong_instructions": 0,
 "classification": "battle_effect_compiler_scheduling_mismatch",
 "resolved_asm_classification": {
  "kind": "battle_effect_compiler_scheduling_mismatch",
  "retention": "keep_structured_asm",
  "confidence": "proven"
 },
 "cross_edition": {
  "editions": [
   "ja",
   "en",
   "de",
   "es",
   "fr",
   "it"
  ],
  "relocation_normalized_core_bytes": 284,
  "core_identical": true,
  "starts": {
   "ja": "0x0808cdb8",
   "en": "0x08095dd0",
   "de": "0x0809946c",
   "es": "0x0809ae5c",
   "fr": "0x0809ae5c",
   "it": "0x08095e5c"
  }
 },
 "domain": "Runs the locally linked phased-radial-particle callback across 24 effect slots, advances the active slots through states two and five, and restores the target object before cleanup.",
 "expressed": [
  "the source and target object lookup and early return",
  "the observed audio and motion sequence on the source object",
  "normalizing the source position and configuring all 24 effect slots",
  "installing BattleEffect_UpdatePhasedRadialParticle as each slot callback",
  "the delayed state-two and state-five transitions for active slots",
  "restoring the target object's callback and scale before effect cleanup"
 ],
 "bounded_search": {
  "safe_heat_walk": {
   "requested_candidates": 20000,
   "attempted_candidates": 20000,
   "compile_failures": 896,
   "exact_candidates": 0,
   "best_differing_halfwords": 11,
   "best_wrong_instructions": 0
  },
  "classic_exact_only_heat_walk": {
   "requested_candidates": 10000,
   "attempted_candidates": 10000,
   "compile_failures": 270,
   "exact_candidates": 0,
   "best_differing_halfwords": 9,
   "best_wrong_instructions": 0,
   "best_shape": "a do-while-zero spelling around EffectSlot_SetObjectMode"
  },
  "current_approved_compiler_classic_heat_walks": {
   "runs": 2,
   "seeds": [211, 811],
   "requested_candidates_per_run": 20000,
   "attempted_candidates": 40000,
   "compile_failures": 908,
   "exact_candidates": 0,
   "best_differing_halfwords": 8,
   "verdict": "generic AST mutation axis closed; lower structural-row variants regressed to nine differing halfwords and were rejected"
  }
 },
 "compiler_axes": {
  "tied_at_readable_baseline": [
   "routed",
   "-fno-schedule-insns",
   "-fno-sched-interblock",
   "-fno-sched-spec",
   "-fno-sched-spec-load",
   "-fno-sched-spec-load-dangerous",
   "-fno-strength-reduce",
   "-fno-expensive-optimizations",
   "-fregmove",
   "-O3"
  ],
  "regressed": [
   "-fno-schedule-insns2",
   "-fno-gcse",
   "-fno-rerun-cse-after-loop"
  ]
 },
 "residual": [
  {
   "scope": "arming the source-object callback",
   "candidate": "forms the 0x4000 argument, copies the object id, then materializes the zero argument",
   "reference": "materializes zero before forming 0x4000 and copying the independent object id",
   "equivalence": "the three instructions write distinct argument registers and neither reads another argument register",
   "resynchronization": "both streams have identical r0, r1, and r2 at the following call"
  },
  {
   "scope": "initializing the active-slot state-five loop",
   "candidate": "materializes state five before copying the slot base from sl",
   "reference": "copies the slot base from sl before materializing state five",
   "equivalence": "the reordered instructions write distinct r1 and r2 values and perform no memory access",
   "resynchronization": "both streams match before the first active-byte load"
  }
 ],
 "notes": [
  "The retained ordinary-C reconstruction emits the complete 460-byte owner extent with identical calls, branches, registers, memory operations, constants, literal words, and control flow outside two localized instruction-ordering hunks.",
  "A do-while-zero lifetime around the slot-object load and action call first improved the family candidate from ten to eight differing halfwords. Narrow C89 block locals for the two state constants then made the entire state-two loop exact and reduced the candidate to five differing halfwords without changing semantics.",
  "Relocation-aware comparison of the independently assembled retained owner proves all 284 unmasked core bytes identical across the six GS1 editions.",
  "The exact caller at 0x080955b0 selects this runner for page two, and this owner installs the locally named callback at 0x08095c08 on each of 24 slots before later advancing those slots to states two and five.",
  "Recovered only from retained owner assembly, approved local GS1 ROMs, local callers and exact battle-effect sources, and the approved compiler. No GS2 evidence or external game source was used."
 ],
 "rejected_experiments": [
  {
   "experiment": "materialize the callback zero argument in a fresh narrow block local",
   "result": "unlike reusing the long-lived value carrier, the fresh local is byte-neutral: GCC folds it to the same late r2 materialization and the candidate remains at five differing halfwords"
  },
  {
   "experiment": "give the state-five loop a fresh narrow active-slot pointer instead of reusing the function-scope slot_state carrier",
   "result": "the pointer receives the same r2 allocation and GCC still schedules the independent constant-five materialization first; the complete candidate remains byte-for-byte unchanged"
  },
  {
   "experiment": "regenerate the owner with aggregate-plus-exact-family m2c context from the seven exact siblings in unit-08095a44",
   "result": "m2c independently reconstructs the same direct callback call and separate state-loop carriers; after applying the known shared types, these are the same source dimensions already tested and expose no missing structural or type repair"
  },
  {
   "experiment": "sequence the state-five slot-base assignment before producing constant five in one comma expression",
   "result": "GCC correctly proves the assignments independent and emits the same five-halfword schedule; the expression adds no real data dependency"
  },
  {
   "experiment": "sweep GCC 2.96's shipped post-reload scheduler sub-options across both retained family members",
   "result": "enabling and disabling interblock scheduling, speculative scheduling, speculative loads, and dangerous speculative loads is byte-neutral for both owners (7 and 5 differing halfwords); the family mismatch is not controlled by those route flags"
  },
  {
   "experiment": "extract the two active-slot loops into a static inline helper taking the state value",
   "result": "changes the surrounding allocation and regresses to 48 differing halfwords / 52 wrong instructions"
  },
  {
   "experiment": "add the independently corroborated three-s32 prototype for ObjectMotion_ArmCallback",
   "result": "no change from the eight-halfword candidate"
  },
  {
   "experiment": "explicit s32 mark = N local (2, 5) replacing the inline constant in each block",
   "result": "a shared or function-scope carrier regresses to 13 differing halfwords / 4 wrong instructions; separate narrow block locals instead close the state-two hunk and preserve the reference r1 allocation"
  },
  {
   "experiment": "hoisting remaining=23 above WaitFrames(100) before the second block instead of inside it",
   "result": "no change from the in-block reorder; the scheduler window does not extend past the WaitFrames call"
  },
  {
   "experiment": "source-order, do-while-zero, nested-scope, and comma-expression barriers around the state-five slot-base copy",
   "result": "all compile identically at five differing halfwords; GCC schedules the independent r1 constant before the sl-to-r2 copy in this later allocation context"
  },
  {
   "experiment": "materialize the callback's zero argument through the existing value carrier",
   "result": "extends the carrier lifetime across the function and regresses to 232 differing halfwords / 72 wrong instructions"
  }
 ],
 "score": {
  "differing_halfwords": 5,
  "note": "Narrow block-local state carriers and source ordering make the state-two loop exact and reduce the candidate from eight to five differing halfwords. Two independent scheduling ties remain and wrong_instructions stays zero."
 }
}
