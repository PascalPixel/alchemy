{
  "schema_version": 1,
  "namespace": "main",
  "address": "0x08010424",
  "name": null,
  "status": "measured-draft",
  "owner_bytes": 316,
  "candidate_bytes": 292,
  "differing_halfwords": 142,
  "wrong_instructions": 194,
  "classification": "map-tile-copy-with-actor-marker-overlay",
  "domain": "For a dest rectangle (dstX,dstY,width,height), linearly copies 12-bit tile indices from a source region of the 0x02010000 map buffer into the dest region (preserving the destination's upper 4 bits via an 0xFFFFF000/0xFFF mask-merge), and for every dest cell that falls inside one of up to 3 actor 16x12-tile bounding boxes (read from *(u8*)Data_03001e70 + 0x104, a 48-byte-stride array of {s32 x, s32 y} Q12.20 world positions, each >>20 to tile units), also copies an 8-byte, two-row marker-icon cell from a table at 0x02020000 (indexed by the copied tile's 12-bit value) into a per-actor 2048-byte-stride overlay table at 0x06002800.",
  "notes": [
    "compiler-families.json clustered this owner with template-main-0801e260 (ui/window/clear_tile_attributes_in_rect.c) at 7989 bp, but the reference assembly has none of that template's bit-clearing shape (no &0x3FF/RENDER_TILE_ATTR_OFS logic, no simple width/height nested loop). This is a family-clustering false positive; the template gave essentially no usable structural starting point.",
    "The literal-pool constants (0x02010000, 0x02020000, 0x02020004, 0x06002800, 0x06002840) are the exact same constants this session found and used in the genuinely-related owner 0800ff54 (see games/gs1/recon/en/main/0800ff54.json), which the same family clustering pass separately (and correctly) linked to the already-exact games/gs1/src/map/render_palette_mapped_row.c / render_palette_mapped_column.c sibling family. That is strong, independently-corroborated evidence this owner belongs to the same map/marker-overlay subsystem, not the ui/window tile-attribute-clear subsystem.",
    "The Data_03001e70 + 0x104 workspace-array base offset is independently corroborated by games/gs1/src/unidentified/overlays/state_update/state_update_0082.c, an already-exact owner whose header comment documents 'the workspace record ... read at fixed offsets +0x106 and +0x10a from Data_03001e70 + 0x104', i.e. the same base address this owner's disassembly computes (movs r0,#130; lsls r0,r0,#1 => 260 = 0x104, added to *Data_03001e70). That owner's small s16 sub-fields at +2/+6 relative to +0x104 are consistent with (not necessarily identical to) this owner's per-actor record starting at that same base with a larger 48-byte stride; the exact struct layout beyond the s32 pair at offset 0/4 is not resolved.",
    "candidate-show settled at 292 candidate bytes vs 316 reference bytes and 142 differing halfwords across three siblings (08010424, 080105d4, 08010788 -- see their own dossiers). This is a substantial, evidence-grounded draft of the real algorithm rather than a template adaptation, but it is far from byte-exact: the exact source shape of the outer/inner loop nesting, the per-actor array's true field types (the state_update_0082.c comment leaves the sub-record layout only partially resolved), and the precise scaling arithmetic for the two overlay-table writes are all still open. This is a source-shape gap requiring dedicated reconstruction of this owner's real domain (map rendering / party marker overlay), not a good target for further family-transplant attempts against the assigned tile-attribute-clear template.",
    "Three near-identical siblings exist at 0x08010424, 0x080105d4, 0x08010788 (all in this assignment batch): 080105d4/08010788 are byte-for-byte near duplicates of each other and use SIGNED loop comparisons (bge/blt) throughout, while 08010424 uses UNSIGNED comparisons (bcs/bcc) for the same three loop-bound checks -- confirmed by testing this owner's C with s32 loop variables (matches 080105d4/08010788's branch mnemonics) versus u32 loop variables (matches this owner's). This owner's retained candidate therefore declares dstX/dstY/width/height/x/y as u32 rather than s32; the other two siblings' candidates keep them signed."
  ],
  "rejected_experiments": [
    {
      "experiment": "store the three actor tile positions in two separate arrays (s32 tileX[3]; s32 tileY[3];) rather than one array of {x,y} structs",
      "result": "worse (149 differing halfwords) -- the reference's unrolled 3-iteration copy loop stores x then y for the SAME actor 8 bytes apart (interleaved pairs), which only an array-of-pairs layout reproduces; separate x/y arrays put all three x values before all three y values in memory, a different stack layout the compiler schedules differently."
    },
    {
      "experiment": "first draft used dstX/dstX+width for the outer (height) zero-guard instead of dstY/dstY+height",
      "result": "a real logic bug (would skip on the wrong condition for callers where width and height differ), fixed before scoring; correcting it did not change the halfword count in this candidate's current form, confirming the two guards were already scored as structurally equivalent shapes by the tool, but the fix is required for the candidate to be behaviorally honest regardless of score."
    }
  ]
}
