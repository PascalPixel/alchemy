{
 "schema_version": 1,
 "namespace": "gs1-en-main",
 "address": "0x080175c0",
 "status": "measured-draft",
 "owner_bytes": 96,
 "score": {
  "measured": "2026-08-30",
  "candidate_bytes": 96,
  "reference_bytes": 96,
  "differing_halfwords": 2,
  "wrong_instructions": 4,
  "class": "frame-context",
  "note": "The ordinary-C source now emits every reference instruction except the prologue and epilogue stack adjustment: GCC reserves 8 outgoing bytes while the reference reserves 16. The two used outgoing argument slots and all control flow are exact. The remaining unused eight-byte frame context must be explained from source or translation-unit evidence; it remains Draft C and claims zero DONE bytes."
 },
 "notes": [
  "Recovered from the retained owner, the aggregate-aware m2c seed, the prepared template pack, and the approved compiler.",
  "Explicit byte-offset construction and two proven early-return guards reproduce the reference address schedule and return-zero carrier; the canonical score improved from 47 to 2 differing halfwords.",
  "The owner uses only its first two incoming arguments, so the unsupported three unused parameters were removed from the provisional signature.",
  "2026-08-31 bounded closure, compiler axis: -O3, -Os, -fno-defer-pop, -fno-function-cse, -fno-regmove, and -fno-schedule-insns all preserve the exact 96-byte candidate and the same two-halfword 8-versus-16-byte frame residual. -O1 shortens the candidate to 92 bytes with 41 differing halfwords, -fno-omit-frame-pointer emits 108 bytes with 52 differing halfwords, and -fno-schedule-insns2 preserves 96 bytes but regresses to 23 differing halfwords. No tested shipped route explains the frame while preserving the otherwise exact body.",
  "2026-08-31 bounded closure, source-context axis: a plain unused u32[2] local is eliminated and leaves the residual unchanged. Marking that array volatile makes the owner mechanically byte-exact by forcing eight otherwise unreferenced stack bytes, but no reference instruction reads, writes, or takes the address of those bytes and no caller evidence identifies such an object. The volatile padding probe was therefore rejected as an evidence-free frame-forcing device and removed. This owner remains Draft C pending authentic translation-unit or source evidence for the extra eight bytes.",
  "2026-08-31 frame-context backend proof: the shipped ARM target defines STACK_BOUNDARY as 32 bits and ACCUMULATE_OUTGOING_ARGS. Its epilogue restores exactly get_frame_size() plus current_function_outgoing_args_size (alchemy-gcc/agscc/gcc/config/arm/arm.h:694,1335; arm.c:6892-6896), while calls.c derives the preferred boundary from that four-byte target boundary and records only the maximum required outgoing area. This owner's sole six-argument call requires two stack words, so the ordinary source has an eight-byte outgoing area and zero live local-frame bytes. The reference's sixteen-byte adjustment therefore proves eight additional bytes of source or translation-unit frame context, not an allocator or scheduling choice. Because no reference instruction observes those bytes, their object type and semantics are not recoverable from this owner. The only DH0 probe, a volatile u32[2], manufactures that unknown context and remains inadmissible padding. Bounded verdict: exact ordinary C is blocked until authentic shared-TU/source evidence identifies the extra local object; retain the legitimate DH2 draft.",
  "No inline assembly, register forcing, compiler barriers, copied instruction bytes, or external decompilation source was used."
 ]
}
