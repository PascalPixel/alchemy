{
  "schema_version": 1,
  "namespace": "gs1-en-main",
  "address": "0x080d0ad4",
  "status": "measured-draft",
  "owner_bytes": 1036,
  "score": {
    "measured": "2026-09-01",
    "candidate_bytes": 980,
    "reference_bytes": 1036,
    "differing_halfwords": 499,
    "wrong_instructions": 510,
    "class": "missing-extra-code",
    "topology": "equal",
    "note": "The drafting wave never measured this reconstruction: aggregate m2c stopped on concrete type/context errors and substituted an unrelated 576-byte shop template, producing the stale compiler_unemittable label. Repairing those errors, consolidating the shared global aggregate, and restoring the missing EffectPosition output argument yields an equal-topology 980-byte candidate for the complete 1036-byte owner."
  },
  "repairs": [
    "Recovered the missing second argument to Func_080e396c from the retained r1=sp+108 call setup and corroborated it against the adopted EffectPosition interface. The prior one-argument call plus a pointer fabricated beyond sp0 was semantically false. The owner now passes a real 8-byte EffectPosition and reads its x result. GCC overlays this short-lived output with later locals, so the candidate remains a 104-byte frame and the byte score moves to 980/1036, 499 differing halfwords, and 510 wrong instructions; the correction is retained because call truth outranks incidental score.",
    "Recovered 0x03001eec as one three-word BattleEffectWorkGlobals aggregate and the facing word as aggregate-minus-108. The reference performs exactly this base-relative walk; replacing four unrelated absolute-global expressions improves differing halfwords 500 to 493 and wrong instructions 500 to 492 while preserving equal topology. The candidate shrinks because GCC now folds duplicate literal loads, exposing stack-object identity as the next residual rather than justifying restoration of the false global model.",
    "Removed the unused generic m2c compatibility preamble from this owner. Only the field-access helper remains; the deletion is compile-neutral and keeps the semantic draft reviewable.",
    "Recovered the three stack records at sp+0x48, sp+0x54, and sp+0x60 as three-word arrays. This resolves m2c's nonexistent sp64 name and preserves the two projected-coordinate words used by the particle seed.",
    "Typed Func_080b5098 as returning void ** and recovered the member id as a signed halfword at state+0x24+2*member rather than dereferencing arithmetic on void *.",
    "Recovered the sprite-cel lookup at 0x080ede5c as a u16 table indexed by the doubled cel value.",
    "Replaced evidence-mined absolute aggregate placeholders with the actual shared battle-work cells at 0x03001eec/ef0/ef4, facing at 0x03001e80, callback cache at 0x03001f08, and the two hardware register writes. This improved the compiling draft from 996 bytes and 510 differing halfwords to 1020 bytes and 500 differing halfwords."
  ],
  "family": {
    "template": "games/gs1/src/battle/effects/member_orbit/run.c",
    "evidence": "The owner uses the same heap-cache cursor, work+0x7780/0x7784/0x7824/0x7828 fields, kind-46/kind-47 rectangle callbacks, scheduler bracket, member-id array, screen projection records, and call-via-register draw convention. Its body is distinct: a member-staggered three-point interpolation effect with a 24-step inner interpolation and a member_count*20+72 frame bound."
  },
  "bounded_verdict": {
    "result": "materially_advanced_not_exact",
    "remaining": "The fresh candidate has equal conditional-branch topology but remains 56 bytes short after consolidating the falsely independent globals and restoring the missing EffectPosition output argument. The aligned residual still begins in the 120-byte reference frame versus the candidate's 104-byte frame and includes the larger function-work layout, spill allocation, and literal materialization. It is not eligible for adoption or compiler classification; the next pass must reconstruct the complete scalar-and-vector work aggregate before allocation work.",
    "done_bytes": 0
  },
  "provenance": [
    "games/gs1/asm/080d0ad4.s",
    "out/gs1-en/waves/draft/packs/080d0ad4/m2c/080d0ad4.c",
    "out/gs1-en/waves/draft/packs/080d0ad4/m2c/target.canonical.s",
    "games/gs1/src/battle/effects/member_orbit/run.c",
    "approved local compiler and ROM only"
  ]
}
