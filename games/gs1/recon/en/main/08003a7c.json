{
 "schema_version": 1,
 "namespace": "main",
 "address": "0x08003a7c",
 "status": "proven-permanent",
 "owner_bytes": 96,
 "translation_unit": "video-dma-queue",
 "compiler_route": "canonical-gcc296",
 "canonical_complete_unit": {
  "candidate_bytes": 96,
  "reference_bytes": 96,
  "differing_halfwords": 35,
  "wrong_instructions": 38,
  "matched_prefix": 1
 },
 "behavior": "When the queue is nonempty, copies the fixed transfer-command executor to temporary RAM, invokes it with the queue and count, then clears the count.",
 "probe": {
  "hypothesis": "An ordinary-C DMA descriptor representation can publish the source, destination, and control words with the reference standalone three-register transfer.",
  "acceptance": "Canonical complete video-DMA queue composition is byte-exact and all eleven existing exact owners remain byte-exact.",
  "tested": [
   "direct scalar, compound-aggregate, volatile, nonvolatile, and post-increment DMA register stores",
   "local, VLA-backed, and by-value three-word descriptors",
   "count, zero, declaration-lifetime, and padding variants"
  ],
  "result": "No canonical form emits the reference standalone three-register store followed by base restoration. Scalar and post-increment forms use scalar or one-register stores; aggregate forms use a twelve-byte staging copy with LDMIA/STMIA and grow to 108 or 112 bytes. Removing the existing padding probe regresses to 92 bytes and 44 differing halfwords."
 },
 "remaining": "The current VLA and call behavior are credible, but the reference needs a standalone DMA publication with the count, zero, and control-base lifetimes allocated differently. The approved compiler's volatile multiple-transfer and BLKmode paths explain the tested alternatives, but do not prove that no ordinary-C source can emit the owner. Retain structured assembly; no permanent compiler classification is claimed.",
 "notes": [
  "All measurements compile the complete declared video-dma-queue reconstruction composition once with canonical GCC 2.96.",
  "No inline assembly, fixed-register variable, compiler barrier, forbidden ABI, copied instruction bytes, compiler modification, or per-owner flag override was used.",
  "This candidate claims zero DONE bytes.",
  "2026-08-27: reclassified measured-draft -> proven-permanent. This owner's retained assembly is already independently proven unreachable from ordinary C at the manifest/classification level: out/gs1-en/full/asm/manifest.json marks it kind=thumb_multiple_transfer_module, retention starting with keep_, confidence=proven, citing arm_md/arm_c line ranges showing GCC 2.96's Thumb backend can only emit a multi-register stmia paired immediately with a matching ldmia (movmem8b/movmem12b), and that ARM's standalone store_multiple/stmsi patterns are TARGET_ARM only -- a lone stmia from independently-computed register values (as this owner's reference has) is architecturally unreachable on this route, not an unexplored search space. This specific pattern was independently rediscovered by multiple dedicated passes this session (08004838, 0800bc48, 08003e10, 0808fecc, 080c0184, and partially 080e7404) before the shared manifest-level proof was found; recording the status correction here so future sessions don't repeat the rediscovery."
 ]
}
