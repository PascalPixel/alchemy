{
  "schema_version": 1,
  "namespace": "gs1-en-main",
  "address": "0x080fa1ac",
  "name": "MusicTrack_ClearModulationState",
  "status": "measured-draft",
  "owner_bytes": 26,
  "score": {
    "measured": "2026-08-26",
    "candidate_bytes": 26,
    "reference_bytes": 26,
    "differing_halfwords": 10,
    "wrong_instructions": 20,
    "class": "allocation",
    "command": "compiler candidate-show games/gs1/recon/en/main/080fa1ac.c --owner 080fa1ac --family old-agbcc --flags -O1",
    "note": "Structurally identical to the reference at old-agbcc -O1: all thirteen instructions match one-for-one in mnemonic, operand shape, and branch structure. The only residual is one uniform register rotation -- the candidate's serial scratch register is r0 where the reference uses r2 (and consequently r2/r3 where the reference uses r3). Under the canonical gcc296 route this owner if-converts and takes a frame; it is an agbcc -O1 family member like the recorded AGBCC_OPTIMIZE_O1_SOURCES."
  },
  "remaining": [
    {
      "op": "register allocation",
      "count": 10,
      "where": "one uniform rotation: candidate r0->reference r2 for the serial scratch pseudo and r2->r3 for the loaded flags byte. r0 is never touched by the reference despite the dead first parameter."
    }
  ],
  "rejected_experiments": [
    {
      "experiment": "canonical gcc296 route (original candidate and template-text spellings)",
      "result": "if-converts the diamond (hoisted 12 with conditional 3 overwrite) and emits a push/pop frame; 24-32 bytes, never the reference's 13-instruction frameless diamond"
    },
    {
      "experiment": "template-text |= arms under old-agbcc -O2",
      "result": "correct diamond and crossjumped or-tail but 28 bytes (extra zero materialization) and r0 scratch"
    },
    {
      "experiment": "non-void declaration (implicit-int style) to reserve r0",
      "result": "no change; the dead parameter's register is still reused"
    },
    {
      "experiment": "returning the first parameter to keep r0 live",
      "result": "30 bytes; generates return moves the reference does not have"
    },
    {
      "experiment": "-ffixed-r0 with -O1",
      "result": "28 bytes; forces an extra parameter move"
    },
    {
      "experiment": "sibling counter-evidence",
      "result": "byte-exact set_tone_type (main:080fb6ec), same dead-first-parameter signature, freely uses r0 as scratch from its first instruction -- the r2 preference here is owner-specific, not a family or flag property"
    }
  ],
  "notes": [
    "Recovered only from the retained owner bytes in the approved local ROM, the adopted audio family sources and headers, and the approved compilers.",
    "This candidate claims zero DONE bytes."
  ]
}
