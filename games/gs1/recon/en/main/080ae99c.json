{
  "schema_version": 1,
  "namespace": "main",
  "address": "0x080ae99c",
  "status": "measured-draft",
  "owner_bytes": 84,
  "candidate_bytes": 84,
  "differing_halfwords": 10,
  "wrong_instructions": 16,
  "register_gate_step4_probe": {
    "date": "2026-08-28",
    "result": "exhausted-not-a-closure",
    "baseline": {"candidate_bytes": 84, "reference_bytes": 84, "differing_halfwords": 10},
    "decoder_signal": "data-load-hard-affinity-bound-to-incoming-tile-register",
    "decoder_evidence": [
      "Candidate data pseudo 38 and its address temporary prefer and receive incoming r2, evicting tile to r4; x then takes r5 and variant remains r3.",
      "The reference needs the data chain in incoming r3, evicting variant to r5 while tile remains r2 and x takes r4.",
      "The call tail is exact; every mismatch belongs to this entry allocation family."
    ],
    "requested_edit": "move the Data_03001f2c load chain's hard affinity from incoming tile r2 to incoming variant r3",
    "bounded_results": [
      {"shape": "inline-resource-selection-helper", "candidate_bytes": 84, "differing_halfwords": 10, "effect": "baseline-equivalent"},
      {"shape": "inline-data-load-helper", "candidate_bytes": 84, "differing_halfwords": 10, "effect": "boundary disappeared before RTL"},
      {"shape": "inline-object-create-helper", "candidate_bytes": 84, "differing_halfwords": 16, "effect": "changed call coloring but left the data chain in r2"},
      {"shape": "typed-state-conditional-resource", "candidate_bytes": 84, "differing_halfwords": 15, "effect": "exact-corpus transplant changed branch polarity but not data affinity"},
      {"shape": "typed-state-conditional-call-operand", "candidate_bytes": 84, "differing_halfwords": 15, "effect": "same regression as the named conditional resource"}
    ],
    "supporting_exact_owner": "main:080ad5b4",
    "decision": "Neither the proven inline boundary nor the one exact-C entry-coloring analogue moved the data chain to r3. The source and retained assembly remain unchanged; this probe does not count toward the five required closures."
  },
  "notes": [
    "2026-08-31 exact-closure rerun: allocator-order again reports the same reciprocal entry coloring (x pseudo 32 candidate r5/reference r4; tile pseudo 34 candidate r4/reference r2; data pseudos 38/39 candidate r2/reference r3), with equal branch structure, identical pools, and an exact post-call tail. The exact sibling 080ae9f0 uses the same readable shared-data/resource-selection source shape and differs only by its branch-local tile offsets, so it supplies no missing source construct for this owner.",
    "2026-08-31 shipped-pass probe: -fno-regmove, -fno-schedule-insns, -fno-cse-follow-jumps, -fno-gcse, -fno-expensive-optimizations, -fno-strength-reduce, -fno-thread-jumps, and -frerun-cse-after-loop all preserve the 84-byte/10-halfword residual; -fno-schedule-insns2 regresses to 11. ABI fixed/call-used register flags were not used because they force allocation and are disallowed. Together with the previously recorded 48,522 finite candidates and named ABI-order/source-shape probes, this leaves no evidence-backed ordinary-C repair. Verdict remains measured allocation-uncovered, zero DONE bytes.",
    "2026-08-31 ABI-order probe: GCC creates the four incoming argument pseudos in ABI order (r0 through r3). Preserving `variant` and then reassigning the fourth parameter as either an integer-cast or pointer-typed Data_03001f2c carrier is optimized back to identical RTL/output (10 differing halfwords); a K&R definition with reordered parameter declarations also retains ABI-order pseudo creation. Semantic-neutral parameter reuse therefore cannot move the data chain's hard affinity from incoming tile r2 to variant r3.",
    "This owner and 080ae9f0 share the same resource-selection, object-creation, failure, and initialization tail.",
    "The complete ordinary-C candidate emits the exact reference length. Sharing 080ae9f0's data-base/resource-selection shape improves this owner from 35 differing halfwords / 22 wrong instructions to 10 / 16 while keeping its post-call tail exact.",
    "The earlier 30,000-candidate search plus a joint sibling matrix over raw pointer versus selected pointer, direct versus split variant lifetime, branch statement order, addend order, carrier widths, declaration order, approved GCC pass flags, -O1/-O3/-Os, and old-agbcc produced no exact candidate. The fixed-route shared-data shape is the unique readable improvement.",
    "A last-mile heat-guided classic walk evaluated 13,522 additional generated candidates from the improved source (8,670 in the first capped process and 4,852 in a journal-seeded continuation). None improved the four normalized structural rows / sixteen raw rows / ten differing halfwords baseline and no exact candidate appeared.",
    "Further joint source axes after that walk included signed and unsigned variant carriers, explicit offset selection before the resource load, duplicated versus joined branch loads, and pre/post resource tile adjustment ordering. Signedness is byte-neutral; the explicit offset join regresses to 80 bytes / 41 differing halfwords / 26 wrong instructions; the readable shared-data baseline remains best.",
    "The remaining exact-extent residual is candidate-show class allocation: the candidate preserves tile in r4, x in r5, and the data base in r2; the reference preserves variant in r5, x in r4, tile in r2, and the data base in r3. Although semantically equivalent and tail-exact, ten differing halfwords across a sixteen-instruction entry allocation family are broader than the repository's narrow proven subset gate.",
    "080ae9f0 has an independently recorded all-six 82-byte normalized core. This preceding sibling has the same EN tail but no exact-object correspondence row, so compiler cross-edition cannot independently validate its full regional owner; no family-wide or regional permanent claim is inferred from adjacency.",
    "Explicit compiler cross-edition --object proof remains unavailable for this non-exact owner because the command requires a claimed exact object/correspondence row. The existing 080ae9f0 proof can validate the tail family only, not this owner's entry allocation.",
    "No external source, copied instruction bytes, inline assembly, fixed-register variable, compiler barrier, or compiler modification was used.",
    "A further 5,000-candidate safe heat-guided walk biased by the exact 080bbb0c source produced no improvement or exact output. Together with the prior 43,522 generated candidates, that closes the missing-temporary search axis until a new allocation model is established.",
    "2026-08-27: four additional targeted structural variants were compiled and scored directly against candidate-show: (1) reading Data_03001f2c inline in both branches instead of through one hoisted `data` local regresses sharply to 37 differing halfwords / 22 wrong instructions (drops the y-holding callee-saved register entirely); (2) reordering the three local declarations (object/resource/data) ties at 10/16 with no emitted change; (3) an explicit early `s32 v = variant;` copy taken before the `data` load ties at 10/16; (4) an explicit early `s32 t = tile;` copy used only at the call site ties at 10/16. None flips which of {tile, variant} keeps its entry hardreg versus which register the data-pointer temp claims. This corroborates the existing note that the residual is a persistent allocation coloring choice, not a reachable source-shape difference under the approved compiler route."
  ]
}
