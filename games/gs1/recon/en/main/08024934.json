{
  "schema_version": 1,
  "namespace": "main",
  "address": "0x08024934",
  "status": "measured-draft",
  "owner_bytes": 2124,
  "candidate_bytes": 2068,
  "differing_halfwords": 1041,
  "wrong_instructions": 1103,
  "classification": "structural-reconstruction",
  "resolved_asm_classification": {
    "kind": "compiler_output",
    "retention": "c_candidate",
    "confidence": "likely"
  },
  "notes": [
    "2026-08-30: promoted the largest remaining unnamed clean compiler-output owner from the prepared aggregate-aware wave. After repairing missing stack fields and typed global reads, the canonical compiler produces 2068 bytes for the 2124-byte reference; topology differs and 1041 halfwords differ, so this remains a measured draft and claims zero DONE bytes.",
    "The candidate implements a large four-member menu and status interaction loop over the 0x03001e8c UI runtime. It creates several windows, gathers party records, draws per-member state, processes directional input, and returns the selected member or the 0xff cancel sentinel. A permanent name is deferred until its callers establish which menu owns the interaction.",
    "The reference reserves a 372-byte frame while the aggregate-aware draft currently recovers only 136 bytes. The next structural pass should reconstruct the complete party-record and window scratch arrays before attempting register or scheduling convergence.",
    "No instruction bytes, inline assembly, fixed-register variables, compiler barriers, or external decompilation sources were used."
  ]
}
