[package]
name = "build-full"
version = "0.1.0"
edition = "2021"

[dependencies]
compiler-core = { path = "../compiler-core" }
serde_json = { version = "1", features = ["preserve_order"] }

[lib]
name = "build_full"
path = "src/lib.rs"
