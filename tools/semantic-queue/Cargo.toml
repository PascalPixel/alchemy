[workspace]

[package]
name = "semantic-queue"
version = "0.1.0"
edition = "2021"

[lib]
name = "semantic_queue"
path = "src/lib.rs"

[dependencies]
serde_json = { version = "1", features = ["preserve_order"] }
