{
 "schema_version": 1,
 "namespace": "main",
 "address": "0x0808fe38",
 "status": "proven-retained",
 "owner_bytes": 120,
 "executable_owner_bytes": 120,
 "candidate_bytes": 126,
 "differing_halfwords": 51,
 "wrong_instructions": 47,
 "cross_edition": {
  "object_bytes": 120,
  "relocation_bytes": 12,
  "literal_bytes": 28,
  "relocation_normalized_core_bytes": 80,
  "core_diff_bytes": 0,
  "core_identical": true,
  "starts": {
   "ja": "0x08086e38",
   "en": "0x0808fe38",
   "de": "0x080934a4",
   "es": "0x08094ea4",
   "fr": "0x08094ea4",
   "it": "0x0808fea4"
  }
 },
 "domain": "Allocates and clears the shared display-transition state, seeds established control fields, and schedules its two runtime callbacks.",
 "expressed": [
  "allocates the 0x540-byte display-transition state",
  "clears the complete state with a fixed-source DMA transfer",
  "stores the requested mode and initializes the trailing control fields",
  "schedules the update and presentation callbacks at their original priorities"
 ],
 "bounded_search": {
  "mode": "screen-only",
  "attempted": 0,
  "exact": false,
  "reason": "The complete readable candidate is six bytes long with 51 differing halfwords and fails exact-extent and <=20-halfword admission."
 },
 "notes": [
  "Independent object analysis bounds this owner at 120 executable bytes and proves all 80 relocation-normalized core bytes identical across the six approved GS1 editions.",
  "The residual spans argument lifetime, pool pointer allocation, and volatile DMA/literal scheduling rather than an immediately resynchronizing compiler hunk.",
  "No full search or permanent classification was warranted, and zero DONE bytes are claimed.",
  "Recovered only from retained owner assembly, approved local GS1 ROM correspondence, recovered battle-effect caller 080983a0, and the approved compiler. No GS2 evidence, external source, copied instruction bytes, fixed-register variable, inline assembly, compiler barrier, or compiler modification was used.",
  "2026-08-27: reclassified measured-draft -> proven-retained. This owner's retained assembly is already independently proven unreachable from ordinary C at the manifest/classification level: out/gs1-en/full/asm/manifest.json marks it kind=thumb_multiple_transfer_module, retention starting with keep_, confidence=proven, citing arm_md/arm_c line ranges showing GCC 2.96's Thumb backend can only emit a multi-register stmia paired immediately with a matching ldmia (movmem8b/movmem12b), and that ARM's standalone store_multiple/stmsi patterns are TARGET_ARM only -- a lone stmia from independently-computed register values (as this owner's reference has) is architecturally unreachable on this route, not an unexplored search space. This specific pattern was independently rediscovered by multiple dedicated passes this session (08004838, 0800bc48, 08003e10, 0808fecc, 080c0184, and partially 080e7404) before the shared manifest-level proof was found; recording the status correction here so future sessions don't repeat the rediscovery."
 ]
}
