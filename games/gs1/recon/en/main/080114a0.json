{
  "schema_version": 1,
  "namespace": "main",
  "address": "0x080114a0",
  "status": "proven-retained",
  "retention": "keep_structured_asm",
  "owner_bytes": 200,
  "candidate_bytes": 200,
  "differing_halfwords": 71,
  "classification": "map_tile_block_compiler_allocation_module",
  "cross_edition": {
    "core_bytes": 188,
    "relocation_bytes": 4,
    "literal_bytes": 8,
    "core_diff_bytes": 0,
    "core_identical": true
  },
  "domain": "Refreshes the current two-by-two map tile block on two layers and stops as soon as the tile updater reports a blocked cell.",
  "notes": [
    "Recovered from the complete retained owner, its adjacent 080113e4 sibling, the local m2c output, and the approved compiler. No external source or GS2 evidence was used.",
    "The ordinary C emits exactly 200 bytes and 95 instructions with equal branch topology and the same two literal words. Every call, argument, width, offset, constant, early-exit edge, and loop bound agrees.",
    "The remaining residual is a reciprocal allocation of the two converted origins, layer byte offset, layer/row/column counters, and tile-index carriers. The instruction operation sequence is otherwise equal; the allocator decoder reports reciprocal register-role swaps and no unique source repair.",
    "The shipped -fno-expensive-optimizations route improves 79 to 71 differing halfwords at exact extent. Regmove, both schedulers, GCSE, CSE follow/skip, rerun CSE, strength reduction, thread jumps, and every composition with the evidenced route preserve that floor or regress. A routing regression test keeps the flag off adjacent 080113e4.",
    "Relocation-aware six-edition analysis uniquely locates the owner and proves all 188 normalized core bytes identical. One Thumb call relocation and two literal words account for the normalized fields.",
    "Local aggregate, array, volatile-array, explicit layer-offset, declaration-order, and loop-spelling probes do not reproduce the reference allocation without worsening the frame or residual. Fixed-register variables, compiler barriers, inline assembly, and volatile forcing are disallowed, so structured assembly remains the byte-exact implementation."
  ]
}
