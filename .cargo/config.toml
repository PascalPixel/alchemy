# One Cargo artifact tree for every in-repo invocation — make targets, direct
# `cargo run --manifest-path tools/...` calls, and agent shells alike — kept
# under ignored out/ beside the other build outputs rather than in tools/.
# `make clean` prunes it; nothing rebuilds outside it.
[build]
target-dir = "out/cargo-target"
