{
  "schema_version": 1,
  "namespace": "main",
  "address": "0x0801b810",
  "status": "measured-draft",
  "owner_bytes": 408,
  "executable_owner_bytes": 408,
  "candidate_bytes": 508,
  "differing_halfwords": 253,
  "wrong_instructions": 240,
  "template_family": "template-main-0801b4ec",
  "template_source": "games/gs1/src/menu/selection/move_forward.c",
  "domain": "The 'move selection backward' mirror of sibling 0801b664, on the same menu-state struct (state+916/924/0x39e/0x3a2/0x348, the same count/selection/index/status/list fields the template names) and the same helper set (Func_0801b9a8/Func_0801b9ec/Func_0801ba68/Func_080030f8/Func_0801b010/Func_0801bd98), but with the linked-list restripe running in the opposite direction (a descending 64-then-minus-16 step versus the forward sibling's ascending step) and a different early-exit branch structure for the small-slot case.",
  "behavior": [
    "Func_0801b9a8(state, index); status=33; WaitFrames(1)",
    "reads the table-slot halfword at state+916; when <=5 it just decrements index (wrapping to 0xffff at 0) and falls to the shared tail",
    "when >5: if the selection-table value or index is nonzero, handles either an 'index==1 && selection!=0' step-back case (writes state+8=8, decrements the selection-table value, calls Func_0801ba68(state,0), possibly clears state+10, writes state+62) or otherwise just decrements the table-slot value at state+916",
    "when both are zero: walks the state+0x348 linked list restriping each interior node's position from a descending base (starts at 64, steps -16) and status=12, busy-waits for the head node's y/base fields to settle, computes an offset from (count!=5)?(count-5):0, writes it at two more state fields, calls Func_0801bd98 per node the same way as the forward sibling, then re-walks the list a second time restriping base/status with an ascending step of 16 starting from that offset",
    "shared tail identical to the forward sibling: status=1; Func_0801b9ec(state,index); WaitFrames(1); Func_0801b010(list_head+10 halfword, 0); WaitFrames(1)"
  ],
  "bounded_search": {
    "mode": "screen-only",
    "attempted": 1,
    "exact": false,
    "reason": "One complete-behavior candidate was written and scored (508 vs 408 bytes, 100 bytes over, 240 wrong instructions). Several branches in this owner (the 'index==1 && selval!=0' step-back arm and the offset-computation loop before the second list restripe) were read under real time pressure and are lower-confidence than the corresponding parts of sibling 0801b664; a careful re-derivation of those two regions from the raw assembly, followed by the same live-range-reduction pass noted for 0801b664, is the realistic path to closing this gap."
  },
  "remaining": "Same situation as sibling 0801b664: substantially larger and more involved than this family's other members, needing a dedicated follow-up pass rather than this batch's bounded per-owner budget. The struct correspondence to the already-adopted move_forward.c template is solid; the two lower-confidence branches called out above are the first things to re-verify against the raw assembly before attempting exactness.",
  "notes": [
    "Struct field names are the same placeholders used in sibling 0801b664's dossier; only 'next' (offset4) and the count/selection/index/status/list field identities (matched to the template) carry real confidence.",
    "Recovered only from retained owner assembly, the already-adopted move_forward.c template, cross-reading against sibling 0801b664's own disassembly, and the approved compiler. No GS2 evidence, external source, copied instruction bytes, fixed-register variable, inline assembly, compiler barrier, or compiler modification was used. Zero DONE bytes are claimed."
  ]
}
