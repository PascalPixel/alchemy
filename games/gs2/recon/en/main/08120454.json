{
  "schema_version": 1,
  "namespace": "gs2-en-main",
  "address": "0x08120454",
  "name": "BattleResolveAction",
  "status": "measured-draft",
  "owner_bytes": 8300,
  "score": {
    "measured": "2026-08-26",
    "candidate_bytes": 8280,
    "reference_bytes": 8300,
    "differing_halfwords": 3908,
    "wrong_instructions": 2608,
    "class": "wrong",
    "command": "compiler candidate-show games/gs2/recon/en/main/08120454.c --owner 08120454 --size 8300 --rom roms/gs2-en.gba",
    "note": "Raw halfwords are phase-dominated: the entry region differs only by a register-allocation shuffle (r1/r2/r5 permutation, one mov sl placement), long runs align between clusters, and the large +/- runs are literal-pool words placed at different sites. The 20-byte length gap is pool-word count, not missing logic."
  },
  "shape": {
    "architecture": "Twelve-ROM master: games/gs1/recon/ja/main/080b2b0c.c is the shared body, written against ~60 BATTLE_* hook macros (362 uses). GS1 editions bind the hooks to GS1 semantics; games/gs2/recon/ja/main/battle_delta.h rebinds them with the sequel's grown logic (extra effect/heal/KO/guard cases) plus per-edition literal headers and ~200 per-edition callee retargets. BATTLE_SECOND_ACTOR reshapes struct BattlePlan in games/gs1/include/battle_types.h.",
    "constraint": "The master body must stay byte-exact for gs1-en: measured 6332/6332 at 0 differing halfwords when wrapped as Func_080bbb0c against roms/gs1-en.gba. Every GS2-side repair must live in the hook bodies, the delta headers, or GS2-only conditionals -- never in unguarded shared text.",
    "gs1_status": "main:080bbb0c is production-exact and relink-proven byte-exact in all six GS1 editions (exact-main-builds, location_methods asaaaa). Six of the twelve ROMs stand.",
    "gs2_editions": "All six GS2 editions are core-identical for this owner (games/gs2/recon/cross-edition.json): one exact edition source closes all six through the existing literal/retarget headers."
  },
  "unexpressed": [
    {
      "op": "register allocation",
      "where": "entry region: candidate permutes r1/r2/r5 against the reference and parks sl one instruction late; the added GS2 hook bodies shift pressure and LUIDs across the whole owner"
    },
    {
      "op": "literal pool placement",
      "where": "pools sit at different sites (first large divergence around the 0x780 branch cluster); the reference's pool anchors imply different branch-range tensions than the candidate's grown blocks produce; the 20-byte extent gap is five pool words"
    }
  ],
  "notes": [
    "The JA-native GS1 candidate (games/gs1/recon/ja/main/080b2b0c.c wrapped as Func_080b2b0c) measures 6332/6332 at 272 differing halfwords against roms/gs1-ja.gba; since the production EN object relinks byte-exact at the JA address, those 272 halfwords are JA-side binding/literal gaps in the wrapper, not core differences.",
    "Recovered only from retained/located owner bytes in the approved local ROMs, the adopted GS1 EN source, in-repository headers, and the approved compiler.",
    "This candidate claims zero DONE bytes."
  ]
}
