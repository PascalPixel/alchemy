[workspace]

[package]
name = "full-c-progress"
version = "0.1.0"
edition = "2021"

[lib]
name = "full_c_progress"
path = "src/lib.rs"

[dependencies]
compiler-core = { path = "../compiler-core" }
serde_json = { version = "1", features = ["preserve_order"] }
