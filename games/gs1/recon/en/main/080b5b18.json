{
 "schema_version": 1,
 "namespace": "gs1-en-main",
 "address": "0x080b5b18",
 "status": "measured-draft",
 "owner_bytes": 240,
 "score": {
  "measured": "2026-08-26",
  "candidate_bytes": 240,
  "reference_bytes": 240,
    "differing_halfwords": 9,
    "wrong_instructions": 6,
    "class": "allocation_uncovered",
  "command": "cargo run --offline --quiet --release --manifest-path tools/compiler/Cargo.toml -- candidate-show games/gs1/recon/en/main/080b5b18.c",
  "note": "Loops over each active party member (Func_080b6a60) and for each one zeros state+0x131..state+0x148 via a 3-byte countdown loop plus 22 individual byte stores, then calls Func_08077010. The reference's two interleaved even/odd store-pointer chains prove the zeroed range skips exactly one offset: 0x140 (not 0x142, an earlier hand-guess this session that measured byte-identically to 0x140's omission on raw counts alone -- the +2/+3 stride-jump position in the aligned disassembly is what actually distinguishes them, and 0x140 is the position that resolves every previously-marked instruction in the store-chain region). The u8 zero=0 local is hoisted to function scope (not per-iteration), matching the reference's r9-resident constant kept live across the whole loop."
 },
 "remaining": [
  {
   "op": "register allocation",
   "count": 109,
   "where": "prologue/loop register-allocation mismatch: candidate keeps the owners-pointer/count in r5/r9 with an extra movs/adds pair and issues the bge trip-check after loading the pointer, vs reference's r8/sl allocation with an early single-register move and cmp/bge issued before the mov sl step; plus an ldrsh-vs-ldrh signedness difference on the owners[i] load and a differing final pop/bx register pair. None of these are affected by the field-list choice -- raw differing_halfwords stays pinned near 109 across nearly all field-list variants because the extra prologue instructions phase-shift almost the entire body's byte offsets against the fixed-position reference comparison."
  }
 ],
 "rejected_experiments": [
  {
   "experiment": "23-way sweep: each of state[0x132..0x148] omitted singly",
   "result": "0x140 is the unique minimum (10 mismatched rows in the aligned diff) versus 12-14 for every other mid-range single omission and worse wrong_instructions (34-42) for boundary omissions (0x132, 0x148)"
  },
  {
   "experiment": "3 shifted-range variants (e.g. 0x131-0x147 skip 0x13f; 0x133-0x149 skip 0x141; 0x132-0x149 skip two offsets)",
   "result": "all worse than the fixed 0x132-0x148 range with 0x140 omitted -- one produced the wrong total length (236 vs 240)"
  },
  {
   "experiment": "u8 zero declared per-iteration inside the for loop instead of hoisted to function scope",
   "result": "regresses wrong_instructions from 32 to 118 -- this hoist is load-bearing, matching the reference's r9-resident constant kept live across the whole function"
  }
 ],
  "notes": [
    "2026-08-31 type and topology closure: changing the enumerated owner buffer from s16 to u16 reproduces the reference's ldrh and collapses the residual from 109 differing halfwords / 32 wrong instructions to 18 / 6. Reconstructing the reference's guarded do-while owner traversal then places the zero-carrier materialization after the empty-list guard and improves the exact-size candidate further to 9 / 6 with equal topology. The remaining six instructions differ only in the scheduling of the r9 zero carrier versus the r5/r6 loop carriers; the allocator decoder reports no unique source repair, so this is parked as allocation-uncovered rather than forced.",
    "Recovered only from the retained owner assembly, approved local ROM, sibling owner 080b6a60 (BattleParty_PrepareActiveOwners, this session's own reconstruction) and its established Func_08077008/Func_08077010 vocabulary, in-repository headers, and the approved compiler.",
  "This candidate claims zero DONE bytes."
 ],
 "sr_route_evidence": {
  "verdict": "COINCIDENTAL (downgraded from SUGGESTIVE)",
  "measured": "2026-08-29",
  "note": "asm/080b5b18.s lines 33-39 (.L1): third instance of the shared do-while byte-fill idiom seen in 080b75dc.s:27-38 and 080b5f0c.s:27-32 (subs r1,#1 / strb r0,[r3,#0] / subs r3,#1 / cmp r1,#0 / bge.n), same in-place pointer-decrement addressing rather than indexed base+offset. Static-asm signal only; dual-compile (-fno-strength-reduce vs default) not performed for this owner.",
  "dual_compile": "measured 2026-08-29: the pointer-decrement fill loop compiles byte-identically with and without -fno-strength-reduce (verified on 080b75dc; the idiom is not SR-gated), so the sweep improvement is a register-pressure side effect, not route evidence."
 }
}
