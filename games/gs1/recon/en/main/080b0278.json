{
  "schema_version": 1,
  "namespace": "main",
  "address": "0x080b0278",
  "status": "measured-draft",
  "owner_bytes": 460,
  "candidate_bytes": 396,
  "differing_halfwords": 228,
  "wrong_instructions": 173,
  "classification": "inn-checkin-state-machine",
  "family": {
    "id": "template-main-080b3284",
    "template_owner": "main:080b3284",
    "template_source": "games/gs1/src/inn/check_in.c",
    "note": "Same Inn domain and several shared call idioms (Func_080b010c, Func_080b04dc(message), Func_080150f8 resource-window lookup, Func_080b0a20 border setup), but this owner is a much larger state-machine loop (dispatching on a mode2 value returned by Func_08015380 across 5 branches, looping via Func_080b0a20+Func_080b04dc(0xca4) until an unmatched mode2 exits) rather than the template's single linear stay-completion flow."
  },
  "notes": [
    "This is the largest and structurally most complex owner in this batch (460 bytes, a real state machine with 5+ branches). I traced the full control flow from the raw assembly: mode clamping (`if (mode>=Func_080b26c8(mode) || mode<0) mode=0;`), Func_080b26cc/Func_080b010c setup, a byte store to state+0x3a9 from Func_080b2764(mode), a mode==16||17||18 flag write to state+940, an object/resource lookup and Func_080150f8 resource-window call with a Func_08015010 fallback when the window is 0, a Func_080150c8 call building a second UI object with fields [4]/[5] set, a Func_080b0a20 border setup, then a Func_08015380-driven loop dispatching mode2 0/1/2/3/default with different Func_080b04dc message calls and sub-function calls per branch, re-arming the border and looping except on the unmatched-default branch which calls Func_08015018/Func_080b0204 and returns 0.",
    "Confidence is LOWER on this owner than the others in this batch: the exact byte offsets used via literal-pool loads (e.g. 0x3a9, 0x3aa used as a byte field, the message ids 0xc9b/0xca7/0xca9/0xcb7/0xcb8/0xcb9/0xca4/0xca5) were assigned from the two raw `.4byte` pool dumps in first-appearance textual order, which is a reasonable but unverified heuristic for a function this size with many pool entries feeding closely-spaced load sites -- I did not simulate the assembler's literal-pool placement to confirm each `ldr [pc,#N]` resolves to the specific word I assigned.",
    "The resulting score (173 of roughly 210 total instructions wrong) reflects both this constant-mapping uncertainty and the substantial register-allocation differences expected in a function this size and branchy. Given the size, this should be treated as a documented best-effort control-flow trace rather than a near-miss candidate -- the highest-value next step is independently re-verifying the pool-word-to-load-site mapping (e.g. by bisecting smaller sub-expressions) before investing in register-level tuning.",
    "Adoption requires 0 differing_halfwords."
  ]
}
