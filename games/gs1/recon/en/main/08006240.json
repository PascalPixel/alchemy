{
  "schema_version": 1,
  "namespace": "main",
  "address": "0x08006240",
  "name": "SerialRuntime_HandleTransferInterrupt",
  "status": "exact",
  "owner_bytes": 280,
  "candidate_bytes": 280,
  "differing_halfwords": 0,
  "wrong_instructions": 0,
  "classification": "exact-c",
  "translation_unit": "serial-runtime",
  "compiler_route": "canonical-gcc296",
  "source_shape": {
    "hardware_snapshot": "A volatile eight-byte serial-register capture is copied through one restricted, stable local union view.",
    "state_lifetimes": "Distinct send, receive, and loop-tail state views preserve the original register lifetimes across the interrupt handler.",
    "receive_loop": "The explicit loop join keeps channel-halfword and channel-word offsets in the reference control-flow shape.",
    "compiler_result": "GCC 2.96 emits the reference stack capture, shared literal pool, register allocation, receive-loop schedule, and timer-control tail byte for byte."
  },
  "proof": {
    "complete_translation_unit": "280/280 bytes, zero differing halfwords",
    "production_source": "280/280 bytes, zero differing halfwords",
    "compiler_flags": "canonical; no per-owner override"
  },
  "notes": [
    "The final source is ordinary C: it uses no inline assembly, fixed-register variable, compiler barrier, forbidden ABI attribute, copied instruction bytes, compiler modification, or flag override.",
    "The GCC 2.96 __restrict spelling expresses the single stable local snapshot view; every later snapshot access derives from that view.",
    "Recovered only from retained owner assembly, approved local ROM correspondence, in-repository callers and types, local m2c structural output, and the approved GCC 2.96 compiler.",
    "The owner is byte-identical in all six editions at the corresponding translated address."
  ]
}
