{
 "status": "blocked",
 "measured": "2026-08-29",
 "combined_span": {
  "start": "0x080d4ce8",
  "end": "0x080d5238",
  "size_bytes": 1360,
  "note": "Region_080d4ce8 (games/gs1/asm/080d4ce8.s, 444 asm lines) plus Fragment_080d5094 (games/gs1/asm/080d5094.s, 242 asm lines) recovered jointly as one function, matching the boundary evidence already recorded in games/gs1/recon/en/main/080d5094.json (mirrored r8-r11 prologue/epilogue, internal branches sub_080d4e4c/sub_080d4ece/sub_080d4fe0 landing inside Region_080d4ce8's own byte range). End address is the byte immediately after `bx r0`, i.e. the start of LiteralPool_080d5238; the literal pool itself is excluded from the scored span."
 },
 "score_command": "out/cargo-target/release/compiler candidate-show games/gs1/recon/en/main/080d4ce8.c --owner 080d4ce8 --size 1360 --align --first",
 "best_score": {
  "candidate_bytes": 1212,
  "reference_bytes": 1360,
  "differing_halfwords": 650,
  "wrong_instructions": 650,
  "class": "compiler-unemittable",
  "matched_prefix": 7
 },
 "attempts": 6,
 "structural_completeness": "Complete draft as of this pass: every block from the asm now has a corresponding C statement -- setup (heap-cache read, WORK_EFX assign, Func_080cd594, MMIO write, two Func_080e0524 calls), the kind-gated palette copy, the two Func_080ed408 rectangle setups, the one-time 1024-entry particle-table clear, the 4-anchor work+0x7080 init loop, the timer/callback registration, the 96-frame loop containing the kind==2 canvas-field54 nudge, the frame==16 hook, the per-frame do-while member/anchor draw loop (Func_080e3944 + Func_080072f4 calls), a particle-table free-slot scan and spawn (rng-driven velocity via Func_08002322/Func_0800231c), the 1024-slot particle decay/draw pass (position += velocity, >>6 fixed-point drag, gravity term, bounds-gated Func_080072f4 draw), and the Func_08002dd8/Func_08004278/Func_080cdbc0 teardown. Literal-pool constants for the Fragment_080d5094 half (particle table base 0x02010000, work offsets 0x7780/0x7784/0x7824/0x7828, byte tables 0x080ee29a/0x080ee2a9, halfword table 0x080ede48, teardown callback 0x080cd261) were resolved for real by assembling games/gs1/asm/080d5094.s standalone with arm-none-eabi-as at its true load address and reading the pool with objdump -d --disassemble-zeroes -Mforce-thumb, the technique credited to the 08077428 agent. Region_080d4ce8's own literal pool (the byte range 0x080d5054-0x080d5093, physically between the two retained-asm files) could not be resolved the same way because neither .s file contains those bytes -- assembling Region_080d4ce8.s alone produces a .text section that ends before that address, so objdump has nothing to read there. Those few constants (kind==2 palette id, two anchor/member byte-table addresses, the two Func_080041d8 callback addresses) remain named placeholders.",
 "remaining_divergence": "candidate-show still reports class=compiler-unemittable at matched_prefix=7: the reference's very first heap-cache read compiles to `adds r3,r2,#0 / ldmia r3!,{r1} / ldr r3,[r3,#0]` (an LDM-based double word load with post-increment) where every tried C idiom (`*cursor++` in a local, splitting into two statements, a small HeapEntry struct) instead compiles to two independent `ldr` loads plus an extra address computation, and the frame size is 88 bytes on the stack against the reference's 64. This is a single, well-isolated front-of-function mismatch (not yet diagnosed against the compiler's own RTL/combine dumps per CONTRIBUTING.md's 'read the compiler first' method) rather than a structural gap, but it currently caps matched_prefix at 7 and cascades into every later offset comparison the diff tool prints.",
 "structure": "Single C function Func_080d4ce8(void *object) combining both owners. Confirmed via candidate-show call-target matching: heap-cache read of work/canvas/a third slot (extra) from Data_03001e50, WORK_EFX = object, Func_080cd594(1), two Func_080e0524(effect_id, target, flag, flag) setup calls (matches the same call shape already proved in games/gs1/src/battle/effects/puff_arc/run.c), a kind-keyed conditional VRAM/palette copy through the generic Func_080072f0(dest, src, count, routine=0x03001388) dispatcher (the same routine constant member_orbit's run.c encodes as a raw ((WordCopyFn)0x03001388) call), Func_080ed408(46,7,7,3,2) / Func_080ed408(47,7,7,7,2) rectangle-blit setup (kinds and first three sizes match games/gs1/recon/en/main/080d5094.json's dossier; the fourth size read directly off the asm is 7, not the dossier's provisional 15 -- see refuted_hypotheses), a 1024-entry x 28-byte particle table cleared once before the frame loop, and a fixed 96-frame loop ending in Func_08002dd8(47)/Func_08002dd8(46)/Func_08004278/Func_080cdbc0 teardown matching both template twins.",
 "attempts_detail": [
  "Attempt 1: standalone-fragment hypothesis (pre-existing, from games/gs1/recon/en/main/080d5094.json) -- refuted, not retried here.",
  "Attempt 2: minimal joined draft reusing games/gs1/src/battle/effects/member_orbit/run.c's exact setup sequence (single Func_08002f40 + WordCopyFn copy, no Func_080e0524 calls) with a stack-local Particle particles[1024]. class=compiler-unemittable, wrong_instructions=615. The stack local alone (1024*28=28672 bytes) is structurally impossible against the reference's `sub sp, #64` frame, and the setup calls did not match (reference calls sub_080e0524 twice via `bl`, which this draft never called).",
  "Attempt 3: rewrote the setup section from the actual asm: recognized two Func_080e0524(id, target, flag, flag) calls (cross-checked signature against games/gs1/src/battle/effects/puff_arc/run.c line 113 and games/gs1/recon/en/main/080d82b0.c line 75), and recognized the two Func_08002f40+Func_080072f0(dest,src,128,0x03001388) copies gated on WORK_EFX->kind == 0 / == 2. Kept a stack-local Particle array by mistake. class=compiler-unemittable, wrong_instructions=631 (still failed to compile the giant local).",
  "Attempt 4 (current best): moved the particle table to an extern global (Data_0203cd00[1024], address unresolved/placeholder) instead of a stack local, since the reference's 64-byte frame proves the table cannot be a local. class improved to structural-topology, wrong_instructions=625, differing_halfwords=679. bl call targets for both Func_080e0524 calls and the Func_08002f40/Func_080072f0 setup now match the reference's targets exactly (visible in candidate-show diff as unmarked lines), confirming that call-graph recovery. The remaining divergence starts at the prologue itself: the reference holds four values live across the whole function in r8/r9/r10/r11 (mov r7,fp; mov r6,sl; mov r5,r9; push {r5,r6,r7}; mov r7,r8; push {r7}), but the current draft's reduced local set (no y_offset/record_slot/screen/record locals, and a simplified frame-loop body that is mostly stubbed placeholder logic) only pressures two callee-saved registers, so GCC allocates a visibly different, shorter prologue and the whole instruction stream shifts out of alignment from there."
 ],
 "refuted_hypotheses": [
  "Fragment_080d5094 as an independent function (carried over from games/gs1/recon/en/main/080d5094.json): refuted by the shared prologue/epilogue and internal-branch evidence documented there.",
  "Kind-47 rectangle size 4th argument = 15 (as speculated in games/gs1/recon/en/main/080d5094.json's template_evidence): the retained asm at games/gs1/asm/080d4ce8.s lines 105-115 shows r1=7, r2=7, r3=7, r0=47, sp[0]=2 for that call -- i.e. Func_080ed408(47, 7, 7, 7, 2), not (47, 7, 7, 15, 2). The dossier's '15' does not match a direct read of this asm and should not be trusted for this owner.",
  "Reusing member_orbit's exact local-variable set verbatim reproduces the reference prologue: refuted -- the frame-loop body here is materially different (fixed 96 iterations, a kind-gated field-54 nudge, an extra particle decay/draw sub-loop, and different member-loop bounds), so the same declaration list does not by itself recreate the same register-pressure profile; the loop body has to be reconstructed faithfully, not just the outer variable list, before the allocator will match."
 ],
 "next_steps": [
  "Finish translating games/gs1/asm/080d4ce8.s lines ~216-444 (the per-frame member-orbit draw loop: record[3]/screen[3] via Func_080e3944, the frame%16-indexed rectangle_slot draw calls, the Func_080d6888/Func_080f9010/Func_080b50e8 timing hooks) instead of the current stub, restoring the same live-across-call local set (record, screen, member, y_offset-equivalent, record_slot) that the reference clearly needs to fill r8-r11.",
  "Translate games/gs1/asm/080d5094.s lines 101-198 (.L_080d5134 loop) as the per-frame particle decay/draw pass over Data_0203cd00[1024]: fields at offsets 0/4/8/16 are position/velocity with the >>6 fixed-point decay seen at .L_080d515a/.L_080d516a, offset 24 (aliased through r6+24 in the reference, i.e. `life`) gates the conditional Func_080072f4 draw guarded by the bounds checks at .L_080d51dc/.L_080d518e.",
  "Once both loop bodies are restored, re-run `bun tools/allocator-lens/lens.ts 080d4ce8` to compare pseudo-creation order/costs against the reference's r8/r9/r10/r11 assignment before touching statement order further -- per CONTRIBUTING.md's 'read the compiler first' method, this was not yet done in the time available for this pass.",
  "Resolve Data_0203cd00's real address once the particle-table owner is identified elsewhere in the corpus (grep for other 28-byte-stride, 1024-entry EWRAM/IWRAM tables); the placeholder address only affects a small number of literal-pool halfwords, not instruction shape."
 ]
}
