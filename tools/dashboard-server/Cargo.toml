[workspace]

[package]
name = "dashboard-server"
version = "0.1.0"
edition = "2021"

[lib]
name = "dashboard_server"
path = "src/lib.rs"

[dependencies]
coverage-map = { path = "../coverage-map" }
music = { path = "../music" }
serde_json = "1"
walkdir = "2"
