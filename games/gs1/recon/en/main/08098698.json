{
 "schema_version": 1,
 "namespace": "main",
 "address": "0x08098698",
 "status": "measured-draft",
 "owner_bytes": 432,
 "candidate_bytes": 438,
 "differing_halfwords": 207,
 "wrong_instructions": 229,
 "family": {
  "exact_c_callers": [
   "main:080985a8",
   "main:080985fc"
  ],
  "exact_c_caller_bytes": 240,
  "aggregate_reusable_bytes": 672
 },
 "cross_edition": {
  "core_bytes": 356,
  "core_diff_bytes": 0,
  "core_identical": true,
  "starts": {
   "ja": "0x0808f68c",
   "en": "0x08098698",
   "de": "0x0809bd40",
   "es": "0x0809d730",
   "fr": "0x0809d730",
   "it": "0x08098730"
  }
 },
 "notes": [
  "The exact ObjectVisual_CopyAttributes operation at games/gs1/src/object/visual/copy_attributes.c proves the 10-bit visual-attribute merge uses an explicit 0xfffffc00 carrier and left/right extraction, not independent 16-bit 0xfc00/0x03ff masks. Transplanting that already-exact operation and correcting the following high-nibble copy to byte offset 9 (the high attribute byte, as the retained owner shows) removes six surplus candidate bytes and the two false mask literals. The remaining broad register cascade precedes this now-exact semantic block; fresh candidate is 438/432 bytes with equal conditional-branch structure under the allocator decoder.",
  "Complete typed producer shared by both exact-C burst-particle entry paths. It spawns twenty-four objects around the current battle target, copies the embedded visual child traits, configures scale, velocity, animation and mode, then randomizes each object around the battle origin with variant-specific positioning.",
  "The exact 84-byte main-object path and exact 156-byte full-effect path establish the family entry conditions, random-vector convention, waits, sounds, and sole call contract; the family covers 672 executable bytes.",
  "The first readable model compiled to 444 bytes against the 432-byte owner. Exact-corpus stamping has reduced it to 438 bytes; the broad candidate still claims zero bytes and is not eligible for permanent classification.",
  "Independent relocation-aware matching proves all 356 normalized core bytes identical at unique JA, EN, DE, ES, FR, and IT anchors. Fifteen call relocations and four literal fields explain the regional raw differences.",
  "Recovered only from retained GS1 assembly, the local exact-C callers and particle/object analogues, the approved compiler, and the six local GS1 editions. No GS2 or external source was used."
 ]
}
