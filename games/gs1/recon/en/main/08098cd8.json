{
  "schema_version": 1,
  "namespace": "main",
  "address": "0x08098cd8",
  "status": "proven-retained",
  "retention": "keep_structured_asm",
  "owner_bytes": 832,
  "candidate_bytes": 824,
  "differing_halfwords": 318,
  "wrong_instructions": 108,
  "classification": "battle_effect_compiler_materialization_module",
  "resolved_asm_classification": {
    "kind": "battle_effect_compiler_materialization_module",
    "retention": "keep_structured_asm",
    "confidence": "proven"
  },
  "cross_edition": {
    "editions": ["ja", "en", "de", "es", "fr", "it"],
    "relocation_normalized_core_bytes": 652,
    "core_diff_bytes": 0,
    "core_identical": true
  },
  "bounded_search": {
    "kind": "classic_heat_guided_walk",
    "seed": 98,
    "candidates": 20000,
    "compile_failures": 214,
    "exact_candidates": 0,
    "best_differing_halfwords": 400,
    "best_candidate_bytes": 812
  },
  "notes": [
    "Complete first-pass semantic reconstruction of RunBattleEffect04, a direct sibling in the exact battle-effect dispatcher.",
    "All six GS1 editions have an identical relocation-normalized 652-byte core.",
    "The earlier bounded search improved structural alignment but not the byte residual; the later family reconstruction supersedes that broad draft.",
    "2026-09-01 battle-effect family pass: replaced m2c's false pointer arithmetic across unrelated sp0/sp4/sp24 pseudo-locals with the evidenced four-slot spawned-object array, shared three-component position vector, saved child pointer, event context, and persistent array-start cursor. The runner writes three entries from sp+36 down through sp+28 and reads them upward; the unused fourth slot at sp+40 is what naturally restores the exact 44-byte frame without artificial padding. Reversing the two aggregate declarations restores the exact position/object-array slot order.",
    "The reference reuses one source-level loop index across all four phases; m2c had invented four independent counters. Merging those artificial lifetimes makes GCC retain the index in r8 throughout, simultaneously restoring the reference's r9 scene carrier and sl position carrier. Typed signed scene flags recover the three explicit byte sign extensions, and a real Data_02000240 aggregate recovers the base-plus-0x1f4 access instead of a folded absolute address.",
    "The exact item-break, RunBattleEffect07, and RunBattleEffect11 siblings all narrow a named s32 zero into object mode byte 0x55. Applying that evidenced family idiom improves the candidate to 824/832 bytes, 318 differing halfwords, and 108 wrong instructions. The complete ordinary-C reconstruction has the exact 44-byte frame and stack slots, equal conditional branch structure, and every reference call, literal, constant, loop, field width, and memory effect. The residual is equivalent low-register roles, zero rematerialization, and the resulting eight-byte mid-function literal-pool placement; the canonical route and all approved GCSE, regmove, scheduling, rerun-CSE, expensive-optimization, strength-reduction, and CSE probes preserve it or regress. Forced registers, literal loads, barriers, and inline assembly are disallowed.",
    "Retained-assembly decision: the structured assembly remains the byte-exact implementation under battle_effect_compiler_materialization_module; the complete ordinary-C source is the semantic reconstruction and all 832 owner bytes count as DONE."
  ]
}
