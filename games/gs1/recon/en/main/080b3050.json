{
  "schema_version": 1,
  "namespace": "main",
  "address": "0x080b3050",
  "status": "proven-retained",
  "classification": "compiler_local_scheduling_floor_module",
  "owner_bytes": 448,
  "retention": "c_candidate",
  "score": {
    "measured": "2026-08-30",
    "candidate_bytes": 448,
    "reference_bytes": 448,
    "differing_halfwords": 5,
    "wrong_instructions": 0,
    "branch_topology": "equal",
    "matched_prefix_instructions": 48,
    "note": "Exact-extent semantic reconstruction. Every opcode and register role matches; the sole residual is one local permutation of independent instructions in the two coordinate loads. Ordinary C and the approved scheduler routes cannot request the reference order."
  },
  "domain": "Shop party-member icon radial-burst presentation, including cursor-state save/restore, icon animation, effect spawning, cleanup, sound, and scheduler callbacks.",
  "expressed": [
    "dereference the 0x03001f2c ShopRuntime pointer cell before accessing runtime fields",
    "save and restore shop->cursor.anchor->kind while temporarily selecting kind 13",
    "use shop->party_action and shop->party_member_icons[member] through the established ShopRuntime layout",
    "read two eight-member signed-halfword coordinate arrays at runtime offsets 0x134 and 0x144",
    "register Func_080b2ffc with flags 0xc80 and remove the same callback during teardown",
    "spawn eighteen 0x48-byte radial-motion effects, expose the selected member after the sixth spawn, then update and clean twenty-four effect slots",
    "use the independently resolved cleanup flag byte at runtime offset 0x3f5 rather than the prior guessed 0x3f0 base"
  ],
  "verified_against_reference": [
    "fresh canonical candidate-show: 448 candidate bytes, 448 reference bytes, equal branch topology, 24 differing halfwords, 6 wrong instructions",
    "Func_080b2ffc and BattleEffect_UpdateRadialMotion identities resolve through already-adopted source owners",
    "aggregate-aware m2c independently confirms the 0x134/0x144 coordinate arrays, 0x3b0 effect base, 0x3f5 cleanup flag base, loop bounds, and pointer advances"
  ],
  "rejected": [
    {
      "shape": "single-expression cleanup flag shift",
      "result": "GCC removes the reference shift and leaves the earlier candidate at 24 differing halfwords; superseded by the retained split load-and-shift form"
    },
    {
      "shape": "earlier split cleanup flag load and <<= 24 statement before the other source repairs",
      "result": "initially regressed in isolation; after the callback and icon-offset repairs, the same evidenced split form preserves the reference shift and contributes to the current 5-halfword / zero-wrong-instruction floor"
    },
    {
      "shape": "canonical route flag matrix over scheduling, regmove, CSE, rerun-CSE, and expensive-optimization toggles",
      "result": "no multi-instruction improvement; several routes regress materially, so no per-owner compiler override is justified"
    },
    {
      "shape": "explicit icon-offset carrier initialized from 0xff and incremented by 21 before the party-member icon load",
      "result": "the initial single-increment form regressed, but a scoped carrier incremented first by 21 and then by member*4 reproduces the reference's constant reuse and address order exactly; superseded by the adopted repair"
    }
  ],
  "notes": [
    "A named callback_flags local fixes the callback literal-load scheduling; a two-step icon_offset carrier fixes the post-effect icon address; and a separate cleanup-byte shift preserves the reference's sign-test instruction. Together these reduce the score from 24 differing halfwords / 6 wrong instructions to 5 / 0.",
    "The only remaining difference is scheduling around the first position store and the second coordinate offset construction. No fixed-register variable, inline assembly, compiler barrier, copied instruction bytes, or compiler modification was used.",
    "The complete C remains the semantic reconstruction; the byte-exact retained assembly is Proven ASM because the remaining schedule is not requestable from ordinary C."
  ]
}
