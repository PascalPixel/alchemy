{
  "schema_version": 1,
  "namespace": "main",
  "address": "0x0802281c",
  "status": "measured-draft",
  "owner_bytes": 160,
  "candidate_bytes": 172,
  "differing_halfwords": 85,
  "wrong_instructions": 66,
  "classification": "battle-layout-draw-party-panels",
  "domain": "Draws the party-layout panel and highlights each requested unit whose placement slot is present in the four-entry battle formation.",
  "notes": [
    "The complete ordinary-C draft accounts for the initial party-size query, base panel draw, 0xff-terminated four-unit input scan, four-slot signed placement lookup, per-slot panel draw, and zero return.",
    "Reading each requested unit id at the reference loop point removes the cached carrier and its redundant halfword re-truncation, reducing the candidate from 176 to 172 bytes and the aligned residual from 86/68 to 85 differing halfwords and 66 wrong instructions. The volatile read is evidence-bearing: the retained owner reloads that input halfword on each placement-slot iteration. The remaining residual is structural/register allocation rather than an unknown operation.",
    "A diagnostic -fno-gcse compile reaches 164 bytes, 58 differing halfwords, and 46 wrong instructions, but the same flag is neutral for adjacent owners 08022768 and 08021d88. It is therefore not registered as a translation-unit route or used to compensate for missing source context.",
    "The permanent name is intentionally limited to the locally observed battle-layout behavior; no story, country, era, or effect identity is inferred.",
    "Recovered only from games/gs1/asm/0802281c.s, the approved local GS1 ROM/compiler, the adjacent Func_08022768 draft and retained assembly, exact local battle-layout callees, and the repository-generated m2c workbench for this owner's own bytes. No external decompilation or target-specific source was used.",
    "This measured draft claims zero DONE bytes. Retained assembly remains authoritative; no fixed-register variable, inline assembly, compiler barrier, forced scheduling, or copied instruction bytes is used."
  ]
}
