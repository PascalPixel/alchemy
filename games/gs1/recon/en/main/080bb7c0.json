{
  "schema_version": 1,
  "namespace": "main",
  "address": "0x080bb7c0",
  "status": "measured-draft",
  "owner_bytes": 280,
  "executable_owner_bytes": 280,
  "candidate_bytes": 274,
  "differing_halfwords": 130,
  "wrong_instructions": 114,
  "family": {
    "template_owner": "main:080c10e8",
    "template_source": "games/gs1/src/battle/presentation/set_actor_modes.c",
    "note": "The assigned set_actor_modes.c template does not match at all; the real sibling is the family's OWN other unresolved member main:080bb65c (BattlePresentation_WaitForAdvance, already an unadopted measured draft), which shares this owner's exact 0x0400004a/0x04000052 window-register writes, the same *(u16*)(prompt+8)&0xfc00 / *(u16*)(prompt+6)&0xfe00 packed-sprite-tile idiom, and the same Func_08015048/Func_080040b4/Func_08003dec/Func_08003f3c call sequence, but with a fixed tile source (Func_080153f0(0) called once) instead of a per-frame animated phase, an (x, y) parameter pair instead of a hardcoded position, and a single exit flag test (Data_03001c94 & 0x303) instead of the sibling's three OR'd conditions."
  },
  "domain": "Waits for the presentation event queue, then renders an animated prompt sprite at a caller-supplied (x, y) position each frame until Data_03001c94's low status bits are set, before releasing the sprite.",
  "expressed": [
    "resolves a fixed tile source via Func_080153f0(0) once, before waiting for Func_08015048() to signal ready",
    "allocates the prompt sprite via Func_080040b4(0x80)",
    "each frame: writes the 0x0400004a/0x04000052 window registers, clears the prompt's tile/position words, packs the sprite tile index into prompt+8 and an x-derived field into prompt+6 (both via the sibling's exact bit-mask pairs 0x3ff/0xfc00 and 0x1ff/0xfe00), derives prompt+4 from y and the same 0x03001e40 status word the sibling reads, and pushes the sprite via Func_08003dec(prompt, 240)",
    "exits the per-frame loop once Data_03001c94 & 0x303 is nonzero, otherwise waits one frame and repeats",
    "releases the sprite and waits one final frame"
  ],
  "bounded_search": {
    "mode": "manual-respell",
    "attempted": 1,
    "exact": false
  },
  "bounded_checks": [
    "the reference reserves extra stack space and computes two additional stack pointers (sp+12 and sp+14) beyond the 12-byte prompt buffer at sp+8 that this draft does not account for; the draft's prompt is addressed purely as one 12-byte base+offset buffer (matching the sibling 080bb65c's own recovered shape), so this owner's true local layout is evidently not a single flat struct the way the sibling's is",
    "the reference keeps the (x, y) parameters live in registers through the initial Func_080153f0(0) call and only spills them to the stack (x at sp+0, y at sp+4, in parameter order) immediately before the wait loop; the draft's parameters get spilled at function entry and to swapped stack slots (y at sp+0, x at sp+4), so the parameter-lifetime shape needs a genuine source rewrite (e.g. deferring first use of x/y) rather than a signature change"
  ],
  "notes": [
    "Reaches within 6 bytes of the reference's 280-byte extent and reproduces every constant this draft could identify (0x80, 0x0400004a/0x04000052, 0x40000000, 0x3ff/0xfc00, 0x1ff/0xfe00, 0xfffc, 248, 240, 0x303) and the full call sequence and branch structure, but the local-variable/stack layout is evidently more elaborate than the flat 12-byte prompt buffer recovered here.",
    "This looks like a source-shape gap rather than a compiler-internals residual: the extra sp+12/sp+14 pointers the reference computes point to real, not-yet-identified locals (or a differently-shaped prompt aggregate) that this draft is missing, and closing that gap should collapse a large share of the current 130-halfword residual at once. Worth a focused follow-up that starts from those two extra stack slots.",
    "Recovered only from retained owner assembly, the family's own already-drafted sibling main:080bb65c (games/gs1/recon/en/main/080bb65c.c), and the approved compiler. No GS2 evidence, external source, copied instruction bytes, fixed-register variables, inline assembly, or compiler modification was used."
  ]
}
