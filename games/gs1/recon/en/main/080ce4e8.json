{
  "schema_version": 1,
  "namespace": "gs1-en-main",
  "address": "0x080ce4e8",
  "status": "measured-draft",
  "owner_bytes": 884,
  "score": {
    "measured": "2026-08-31",
    "candidate_bytes": 888,
    "reference_bytes": 884,
    "differing_halfwords": 420,
    "wrong_instructions": 362,
    "class": "missing-extra-code",
    "topology": "equal",
    "note": "First compiling owner-specific reconstruction. The drafting wave's aggregate m2c failed on void-pointer member-id reads and undersized stack records, then scored an unrelated 576-byte family template. Repairing the real draft recovers the complete conditional-branch topology and an extent within four bytes of the retained owner."
  },
  "repairs": [
    "Recovered sp+0x30 and sp+0x3c as three-word position records, matching the three fields written/read around Func_08004cb4 and Func_080e3944.",
    "Typed Func_080b5098 and recovered both state member-id reads as signed halfwords at state+0x24+2*member rather than invalid dereferences of void-pointer arithmetic.",
    "Recovered the 512-entry particle pool at 0x02010000 as 28-byte records; the aggregate draft's typed pointer arithmetic incorrectly scaled each explicit +0x1c step by the aggregate size.",
    "Replaced fictional calls into 0x080072f0/0x080072f8 with the established family semantics: word copies through 0x03001388, rectangle drawing through heap-cache callback 47, and magnitude calculation through 0x030001d8.",
    "Used linker-resolved Value_000000a9 and Value_000000bb resource identifiers, preserving the reference's literal-pool loads rather than movs immediates."
  ],
  "family": {
    "template": "games/gs1/src/battle/effects/member_orbit/run.c",
    "evidence": "Same 0x03001eec work/canvas cursor, heap kinds 46/47, callback pair, scheduler callbacks, scanline table, member list, 28-byte particle records, projection helpers, and teardown. This owner is a distinct member-staggered 512-particle convergence effect, not a resized orbit source."
  },
  "bounded_verdict": {
    "result": "materially_advanced_not_exact",
    "remaining": "The candidate is four bytes long and retains broad register/stack allocation differences after an exact conditional topology recovery. It is not eligible for integration or compiler classification. The next pass should reconcile the 76-byte candidate frame with the 72-byte reference frame and then re-audit the particle-loop temporaries.",
    "done_bytes": 0
  },
  "provenance": [
    "games/gs1/asm/080ce4e8.s",
    "out/gs1-en/waves/draft/packs/080ce4e8/m2c/080ce4e8.c",
    "out/gs1-en/waves/draft/packs/080ce4e8/m2c/target.canonical.s",
    "games/gs1/src/battle/effects/member_orbit/run.c",
    "approved local compiler and ROM only"
  ]
}
